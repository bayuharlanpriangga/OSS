# OSS — Orias Statistik System — Architecture Map & Split Roadmap

> **File ini adalah "peta jalan" pemisahan kode OSS dari monolith → modular.**
> Dibuat supaya setiap sesi Claude (atau kamu sendiri) langsung tahu:
> 1. Fitur X ada di file mana (sekarang / setelah dipisah)
> 2. Bagian mana yang **sudah** dipisah dan mana yang **belum**
> 3. Aturan main biar hasil pisahnya konsisten, gak bentrok, dan gak merusak app yang jalan

---

## 0. CARA PAKAI FILE INI (WAJIB DIBACA CLAUDE SEBELUM MENGERJAKAN APAPUN)

1. **Sebelum mulai kerja apapun di OSS**, baca dulu seluruh file ini untuk tahu konteks & status terkini.
2. Cari fitur/section yang diminta user di **Bagian 3 (Peta Fitur → File)**. Kolom **Status** memberitahu apakah kode itu:
   - `⬜ BELUM` → kodenya masih nyampur di `app.js` (dan `style.css`) lama, lokasi ditandai dengan baris perkiraan.
   - `✅ SUDAH` → kodenya sudah dipindah ke file target di kolom **File Target**. Kerjakan di file itu, JANGAN cari lagi di `app.js`.
   - `🟡 SEBAGIAN` → sebagian sudah dipindah, sebagian masih nyampur (dijelaskan di kolom Catatan).
3. **Setiap kali kamu (Claude) selesai memisahkan satu bagian ke file baru**, kamu WAJIB:
   - Update kolom **Status** bagian itu jadi `✅ SUDAH` (atau `🟡 SEBAGIAN` kalau belum tuntas).
   - Isi kolom **File Target** dengan path file yang benar.
   - Tambahkan 1 baris baru di **Bagian 6 — Riwayat Pemisahan (Changelog)** dengan format yang sudah dicontohkan.
   - Update urutan `<script>` di `index.html` sesuai Bagian 4.
   - **Jangan** tandai ✅ kalau belum benar-benar dites jalan (minimal: dibuka di browser, tidak ada error di console).
4. **Nomor baris di kolom "Lokasi di app.js (baseline)" akan berubah/tidak valid lagi** begitu ada bagian lain yang sudah dipindah keluar (karena app.js jadi lebih pendek). Baris itu HANYA valid untuk baseline snapshot tanggal **7 Agustus 2026** (18.345 baris). Kalau butuh cari lokasi pasti, **selalu grep nama fungsi/marker komentarnya**, jangan percaya nomor baris mentah-mentah setelah ada perubahan.
5. Kerjakan **satu bagian per sesi** (sesuai urutan prioritas di Bagian 5, atau sesuai permintaan user). Jangan coba pisahkan banyak sekaligus — itu penyebab kena limit.
6. Semua fungsi tetap **global** (attached ke `window` atau var global biasa, TIDAK pakai ES module `import/export`) kecuali user secara eksplisit minta migrasi ke ES modules. Ini supaya `onclick="..."` inline di HTML yang di-generate lewat `innerHTML` tetap jalan tanpa refactor besar-besaran.

---

## 1. KONDISI SAAT INI (Baseline Snapshot — 7 Agustus 2026)

| File | Baris | Peran |
|---|---|---|
| `index.html` | 295 | Shell HTML kosong — hampir semua konten di-render lewat JS ke `#app-content`. Berisi loading screen, watercolor canvas, top bar, side nav, dialog system, PWA banner. |
| `style.css` | 1.235 | Semua styling (1 file, sudah cukup rapi per-section pakai comment header, belum perlu dipecah prioritas tinggi). |
| `app.js` | **18.345** | **MONOLITH** — semua logic: engine statistik, semua UI render, semua export, semua state. Ini target utama pemisahan. |
| `manifest.json` | 46 | PWA manifest — sudah rapi, tidak perlu dipecah. |
| `sw.js` | 114 | Service worker (cache strategy) — sudah rapi, tidak perlu dipecah. |

**Fokus roadmap ini = memecah `app.js`.** `style.css`, `manifest.json`, `sw.js` dianggap sudah "selesai" dan tidak masuk roadmap split (kecuali nanti diminta).

---

## 2. TARGET STRUKTUR DIREKTORI (Tujuan Akhir)

```
/OSS/
├── index.html
├── manifest.json
├── sw.js
├── style.css
└── js/
    ├── core/
    │   ├── watercolor-bg.js       (animasi canvas background)
    │   ├── router.js              (page router + app tab switching)
    │   ├── icons.js                (SVG ICON SYSTEM / objek IC)
    │   ├── dialog.js               (OSS Dialog Engine — pengganti confirm/alert/prompt)
    │   ├── loading-screen.js       (loading screen + dismiss logic)
    │   ├── html-helpers.js         (HTML Helpers umum: sigBadge/stCard/mkTable/selOpts — C4,
    │   │                            dipilih sbg "util bersama" alih-alih digabung ke state.js)
    │   └── state.js                (global state: vars, dataArr, currentASub, aState, dll)
    │
    ├── stats-engine/
    │   ├── stats-core-basic.js     (descriptive, t-test, ANOVA 1-way, korelasi, regresi dasar)
    │   ├── stats-core-advanced.js  (matrix ops, multiple reg, EFA, CFA, MANOVA, canonical corr)
    │   ├── stats-nonparametric.js  (Mann-Whitney, Kruskal, Wilcoxon, Fisher, Cohen's Kappa)
    │   ├── stats-distributions.js  (helper distribusi: tCrit, betaInc, gammaSer, erf, normInv, fCDF, chiCDF)
    │   ├── stats-glm-hlm.js        (GLM Poisson/NegBin, HLM 2-level/3-level/ICC)
    │   ├── stats-bayesian.js       (Bayesian t-test, Bayesian correlation, posterior)
    │   ├── stats-discriminant-cluster.js (LDA, K-Means, Hierarchical Clustering)
    │   ├── stats-survival.js       (Kaplan-Meier, Log-Rank, Cox PH)
    │   ├── stats-mediation-sem.js  (Mediation, Moderation, SEM/lavaan syntax)
    │   ├── stats-metaanalysis.js   (Meta-analysis: forest plot data, funnel plot data)
    │   ├── stats-poweranalysis.js  (Power analysis, sensitivity, power plot)
    │   ├── stats-timeseries.js     (Time series analysis)
    │   ├── stats-roc.js            (ROC curve + ROC compare)
    │   ├── stats-imputation.js     (Multiple Imputation / MICE)
    │   └── stats-crosstab.js       (Chi-Square Test of Independence untuk Crosstab)
    │
    ├── data/
    │   ├── dataset-manager.js      (MULTI-DATASET SYSTEM — tab dataset, array `datasets`)
    │   ├── data-view.js            (tab Data: tabel, sort, search, select rows, windowed render)
    │   ├── spreadsheet-mode.js     (SPREADSHEET / BULK EDIT MODE)
    │   ├── variable-view.js        (tab Variables: edit mode, picker TYPE/MEASURE/ROLE)
    │   ├── data-transform.js       (Compute/Transform, Recode, Filter Cases)
    │   ├── weight-cases.js         (Weight Cases)
    │   └── missing-data.js         (Missing Data Analysis — Little's MCAR, pattern matrix)
    │
    ├── analyze/
    │   ├── analyze-form-render.js  (dispatcher renderASub() + fungsi render<Nama>Form() per sub-tab)
    │   └── analyze-subtab-ui.js    (sliding pill indicator, sub-tab switching UI)
    │
    ├── charts/
    │   ├── svg-charts-core.js      (chart generik: scree/histogram/boxplot/scatter/QQ/residual/corr-matrix + palet PAL2)
    │   ├── forest-funnel-plot.js   (Forest Plot & Funnel Plot untuk meta-analysis)
    │   ├── sem-mediation-diagram.js(diagram path SEM & mediation)
    │   ├── interaction-plot.js     (interaction plot Two-Way/Three-Way ANOVA)
    │   ├── power-plot.js           (Power Curve & Sensitivity Curve — E8)
    │   ├── moderation-plot.js      (Moderation plot, Simple Slopes, Johnson-Neyman — E9)
    │   ├── roc-plot.js             (ROC Curve & ROC Compare — E10)
    │   ├── survival-plot.js        (Kaplan-Meier & Forest Plot Cox — E11)
    │   ├── discriminant-cluster-plot.js (Discriminant scatter, Cluster scatter, Elbow, Dendrogram — E12)
    │   ├── bayesian-plot.js        (prior vs posterior Bayesian — E13)
    │   └── imputation-plot.js      (Convergence Plot MICE — E14)
    │
    ├── output/
    │   ├── output-view-shell.js    (tab group system Output View, dispatcher utama)
    │   ├── output-render-basic.js  (render hasil: descriptive s/d regression/GLM/MANOVA)
    │   ├── output-render-advanced.js(render hasil: HLM, EFA/CFA, SEM, discriminant, cluster,
    │   │                             power, ROC, survival, Bayesian, time series, meta-analysis)
    │   ├── output-power-render.js  (renderPowerOutput khusus)
    │   └── apa-copy.js             (fitur copy tabel format APA)
    │
    ├── export/
    │   ├── export-word.js          (generate & download .doc, per-output & bulk)
    │   ├── export-excel.js         (export ke Excel via SheetJS)
    │   └── export-dialog.js        (dialog pilih output yang mau diexport)
    │
    ├── session/
    │   ├── session-save-restore.js (save/restore session snapshot, dirty-flag tracking)
    │   └── oss-filesystem.js       (save/open file `.oss` — File System Access API + fallback input)
    │
    └── ui-misc/
        ├── ai-view.js              (tab AI — chat history & UI)
        ├── syntax-view.js          (tab Syntax — tampilan syntax ala SPSS)
        ├── pivot-view.js           (tab Pivot table)
        ├── cmd-palette.js          (Command Palette / Ctrl+K)
        ├── custom-select.js        (Custom Select — modal bottom sheet picker)
        ├── activity-loading-popup.js (popup "sedang memproses...")
        ├── pwa-install.js          (register SW, install prompt, tombol Install App)
        └── badges.js               (updateBadges — badge N=cases, missing, dll)
```

> Catatan: struktur ini **target**, bukan berarti harus persis segini banyaknya di awal. Kalau kepanjangan, boleh digabung 2-3 file jadi 1 dulu (misal semua `stats-*` kecil digabung dulu), yang penting **konsisten dengan tabel di Bagian 3** dan ditandai di changelog.

---

## 3. PETA FITUR → FILE (Detail per Section)

Legend status: `⬜ BELUM` · `🟡 SEBAGIAN` · `✅ SUDAH`

### A. Core / Bootstrap

| # | Fitur / Section | Lokasi di `app.js` (baseline 7 Agu 2026) | File Target | Status |
|---|---|---|---|---|
| A1 | Watercolor Canvas Animation (background) | baris 1–112 | `js/core/watercolor-bg.js` | ✅ SUDAH |
| A2 | Page Router (`appReady`, fixed-position page isolation) | baris 113–172 | `js/core/router.js` | ✅ SUDAH |
| A3 | SVG Icon System (objek `IC`) | baris 173–212 | `js/core/icons.js` | ✅ SUDAH |
| A4 | OSS Dialog Engine (custom confirm/alert/prompt) | baris 3358–3423 | `js/core/dialog.js` | ✅ SUDAH |
| A5 | Loading Screen dismiss logic | baris 18313–akhir | `js/core/loading-screen.js` | ✅ SUDAH |
| A6 | App Tab Switching + Side Nav active state + `TAB_LABELS` | baris 16421–16519 | `js/core/router.js` (gabung dengan A2) | ✅ SUDAH |
| A7 | Update Badges (N=cases, missing, dll) | baris 16521–16543 | `js/ui-misc/badges.js` | ✅ SUDAH |

> ⚠️ **Temuan A7 (sudah diselesaikan)**: ada **2 fungsi `updateBadges()`** di `app.js` — dead code lama (~baris 3315, isi `badge-vars`/`badge-out`/`wc-badge`) dan versi aktif (~baris 16150, isi `badge-cases`/`tb-cases`/`tb-miss` + panggil `renderDsSidebar()`). User dikonfirmasi (2026-09-13): **pakai versi aktif, versi lama dihapus permanen** (bukan cuma dipindah). Mirip kasus H1/H2 (`saveSession`) yang masih menunggu keputusan serupa di Bagian H.

### B. Statistics Engine (fungsi murni komputasi, tanpa DOM)

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| B1 | Distribusi helper (`tCrit`, `lnG`, `betaCF`, `incBeta`, `gammaSer/CF`, `erf`, `normInv`, `quantile`, `fCDF`, `chiCDF`, dll) | baris 217–417 (nyampur dgn B2) | `js/stats-engine/stats-distributions.js` | ✅ SUDAH |
| B2 | Descriptive, T-Test (indep/paired/one-sample), One-way ANOVA + Tukey HSD, Pearson/Spearman, Linear Regression sederhana | baris 418–998 | `js/stats-engine/stats-core-basic.js` | ✅ SUDAH |
| B3 | Matrix ops (matMul/matT/matDet/matInv), Multiple Regression, Repeated Measures ANOVA | baris 598–996 | `js/stats-engine/stats-core-advanced.js` | ✅ SUDAH |
| B4 | Nonparametric: Mann-Whitney (+exact dist), Kruskal-Wallis, Wilcoxon, Cronbach's Alpha, Cohen's Kappa, Fisher Exact | baris 1038–1362 | `js/stats-engine/stats-nonparametric.js` | ✅ SUDAH |
| B5 | Format helper (`f4`, `f1`, `pFmt`, `dLabel`, `effLabel`), Cohen's d CI, eta² CI | baris 1362–1410 | gabung ke `js/stats-engine/stats-core-basic.js` | ✅ SUDAH |
| B6 | GLM Univariate, Bonferroni, Partial Correlation, Canonical Correlation | baris 1411–1703 | `js/stats-engine/stats-glm-hlm.js` (GLM) + `stats-core-advanced.js` (partial/canonical corr) | ✅ SUDAH |
| B7 | Posthoc: LSD, Bonferroni, Holm-Bonferroni | baris 1704–1783 | `js/stats-engine/stats-core-basic.js` | ✅ SUDAH |
| B8 | EFA (Exploratory Factor Analysis) + Varimax rotation, CFA (Confirmatory) | baris 1784–2182 | `js/stats-engine/stats-core-advanced.js` | ✅ SUDAH |
| B9 | MANOVA (Pillai, Wilks, Hotelling-Lawley, Roy) | baris 2183–2423 | `js/stats-engine/stats-core-advanced.js` | ✅ SUDAH |
| B10 | Two-Way ANOVA (Factorial 2×k) | baris 2424–2550 | `js/stats-engine/stats-core-basic.js` | ✅ SUDAH |
| B11 | Three-Way ANOVA (Factorial A×B×C) + sparse cell warning tiering | baris 2551–3252 | `js/stats-engine/stats-core-basic.js` | ✅ SUDAH |
| B12 | Meta-Analysis core (effect size pooling, heterogeneity, forest/funnel data) | baris 9790–10028 | `js/stats-engine/stats-metaanalysis.js` | ✅ SUDAH |
| B13 | Mediation computation core | baris 10137–10320 | `js/stats-engine/stats-mediation-sem.js` | ✅ SUDAH |
| B14 | Power Analysis computation | baris 10321–10685 | `js/stats-engine/stats-poweranalysis.js` | ✅ SUDAH |
| B15 | Moderation Analysis computation | baris 10686–11017 | `js/stats-engine/stats-mediation-sem.js` | ✅ SUDAH |
| B16 | Multiple Imputation (MICE — PMM/Normal) | baris 11018–11162 | `js/stats-engine/stats-imputation.js` | ✅ SUDAH |
| B17 | ROC Curve + ROC Compare | baris 11163–11321 | `js/stats-engine/stats-roc.js` | ✅ SUDAH |
| B18 | Survival Analysis (Kaplan-Meier, Log-Rank, Cox PH) | baris 11322–11785 | `js/stats-engine/stats-survival.js` | ✅ SUDAH |
| B19 | SEM core computation + generate lavaan syntax | baris 11848–12420 | `js/stats-engine/stats-mediation-sem.js` | ✅ SUDAH |
| B20 | Bayesian Statistics Engine | baris 12421–12955 | `js/stats-engine/stats-bayesian.js` | ✅ SUDAH |
| B21 | Discriminant Analysis (LDA) computation | baris 12956–13209 | `js/stats-engine/stats-discriminant-cluster.js` | ✅ SUDAH |
| B22 | Cluster Analysis (K-Means & Hierarchical) computation | baris 13210–13761 | `js/stats-engine/stats-discriminant-cluster.js` | ✅ SUDAH |
| B23 | Weight Cases (logic non-UI) | baris 13762–13818 | `js/data/weight-cases.js` | ✅ SUDAH |
| B24 | Hierarchical Regression toggle helper + Logistic Regression | baris 6141–6274, 16789–16849 | `js/stats-engine/stats-core-advanced.js` (hanya `logisticReg`, lihat catatan) | 🟡 SEBAGIAN |
| B25 | GLM Poisson/NegBin, HLM 2-level/3-level/ICC render+compute (nyampur) | baris 6910–7476 | `js/stats-engine/stats-glm-hlm.js` | ✅ SUDAH (2026-09-16) |
| B26 | Time Series Analysis | ~~baris 9323–9656~~ (grep marker: `TIME SERIES (ARIMA + Decomposition)`, di dalam analyze-form-render) | `js/stats-engine/stats-timeseries.js` | ✅ SUDAH |
| B27 | Chi-Square Test of Independence (Crosstab) — statistic, df, p, Cramér's V, deteksi expected freq <5 | *(tidak tercatat di baseline asli — ditemukan saat scan B4)* | `js/stats-engine/stats-crosstab.js` | ✅ SUDAH |

> Catatan penting untuk B: banyak fungsi ini ada di dalam **1 IIFE besar** (mirip namespace `Stat`). Saat dipisah, pastikan cara ekspos fungsinya (`window.Stat = {...}` atau taruh semua langsung di global scope) **konsisten** — jangan campur 2 gaya beda di file yang berbeda, biar gampang dipanggil dari file lain.
>
> **Strategi yang dipakai (dikonfirmasi jalan di B1):** fungsi-fungsi di dalam `var SE = (() => {...})()` dipindah keluar IIFE jadi **fungsi global biasa** (bukan ES module), lalu di akhir IIFE (yang masih tersisa di app.js) ada `return {...}` yang isinya cuma nama-nama fungsi tsb — karena semua sudah jadi global, `return {mean, std, ...}` tetap jalan (JS fallback ke global scope). Jadi `SE.mean(...)` dkk dari luar tetap jalan seperti biasa TANPA perlu ubah pemanggil di tempat lain. Lanjutkan pola ini untuk B2 dst.
>
> ⚠️ **Temuan baru saat scan struktur B (belum diperbaiki, tunggu keputusan user sebelum lanjut ke B2/B3):**
> 1. **`matMul`, `matT`, `matDet`, `matInv` dideklarasikan 2x dengan signature BEDA** — versi pertama (dipakai `multipleReg`, sekitar baris lama 416–468) pakai bentuk `matMul(A,m,n,B,p)` dll; versi kedua (dipakai EFA/CFA/`manovaProper`, sekitar baris lama 1906–1944) pakai bentuk `matMul(A,B)` 1-2 argumen. Karena deklarasi fungsi kedua **menimpa** yang pertama di scope yang sama, kemungkinan besar `multipleReg` saat ini memanggil versi matriks yang salah (signature tidak cocok) → **kemungkinan bug aktif di fitur Multiple Regression**. Belum disentuh — butuh keputusan user: mau di-rename biar tidak tabrakan (misal `matMulReg` vs `matMulSq`), atau dicek dulu apakah memang masih berfungsi normal di app.
> 2. **Ditemukan saat mengerjakan B2/B5 (belum diperbaiki, cuma dicatat — sesuai instruksi user "biarkan dulu apa adanya"):** ternyata ada **2 deklarasi `pFmt` lain** di luar IIFE `SE` yang berbeda dari `pFmt` versi B5 yang baru dipindah — satu di dalam scope lokal modul lain (sekitar baris app.js saat ini ~8987, isi `if(p<0.001) return '<0.001'; ...`), satu lagi top-level global (sekitar baris ~14920). Karena scoping JS (local shadowing di dalam fungsi/IIFE masing-masing), kemungkinan ini **tidak** menimbulkan bug langsung seperti kasus `matMul`/`f1` (beda scope, bukan overwrite di scope yang sama) — tapi tetap technical debt yang sebaiknya dirapikan nanti. Belum disentuh.
> 3. **`f1` dideklarasikan 2x dengan isi beda total** — versi pertama (~baris lama 1154) `f1(v){return f4(v,1);}` (format angka 1 desimal), versi kedua (~baris lama 2506) `f1(x){return isFinite(x)?Math.round(x*10)/10:'—';}` (bulatkan 1 desimal, beda cara handle non-finite). Versi kedua yang aktif dipakai sekarang. Belum dihapus — sama kasusnya dengan `updateBadges`/`fCDF`, tunggu konfirmasi user.

> ⚠️ **Temuan B21 (belum diperbaiki)**: di `computeLDA` (`js/stats-engine/stats-discriminant-cluster.js`), baris `wilksP=pFmt(1-SE.chi2CDF?SE.chi2CDF(chiSq,dfW_chi):NaN)` kemungkinan bug operator-precedence — `1-SE.chi2CDF` (angka dikurangi function) selalu jadi `NaN`, dan `NaN` di posisi kondisi ternary selalu falsy, jadi `wilksP` kemungkinan **selalu** bernilai `pFmt(NaN)='—'`, tidak pernah benar-benar menghitung p-value Wilks' Lambda. Kemungkinan bug aktif di fitur Discriminant Analysis, mirip kasus `matMul` di atas. Belum disentuh, tunggu konfirmasi user.

> ⚠️ **Temuan B23 / C3 (sudah diselesaikan)**: dua baris tabel yang tampak terpisah — B23 "Weight Cases (logic non-UI)" dan C3 "Weight helpers (dipakai lintas fitur)" — ternyata menunjuk ke **kode yang sama persis** (`getNEff`/`getWeightedRows`, cuma 1 pasang fungsi di app.js, bukan 2 pasang di 2 lokasi berbeda seperti tersirat baseline). Dipisah sekali ke `js/data/weight-cases.js`, kedua baris ditandai selesai. Temuan tambahan (belum diperbaiki): `getWeightedRows()` dicek grep di seluruh app.js — **tidak pernah dipanggil** kecuali disebut di komentar dokumentasi; tidak ada fungsi analisis statistik manapun yang benar-benar menerapkan weighting lewat fungsi ini. Fitur Weight Cases kemungkinan baru menghitung N efektif secara kosmetik (lewat `getNEff()`), belum benar-benar mempengaruhi hasil analisis. Tunggu konfirmasi user.

> ⚠️ **Temuan B24 (SEBAGIAN, belum dipindah semua sesuai tabel — lihat penjelasan)**: tabel B24 menulis "Hierarchical Regression toggle helper + Logistic Regression" sebagai 1 baris. Setelah dicek, ini sebenarnya 2 hal beda sifat:
> 1. `logisticReg` (computation murni, IRLS binary + multinomial logistic regression) — **sudah dipindah** ke `js/stats-engine/stats-core-advanced.js` sebagai fungsi global, pola sama B1-B23.
> 2. `toggleHrBlock` ("Hierarchical Regression toggle helper") — ternyata murni state helper yang memanggil `renderASub()` (trigger UI), **pola PERSIS SAMA** dengan `semAddLatent`/`semRemoveLatent`/dst di B19 yang sengaja TIDAK dipindah ke stats-engine karena itu "UI state", bukan "computation". Supaya konsisten, `toggleHrBlock` (+ `runHierarchicalReg`, UI wiring) **TETAP DIBIARKAN di app.js**, menyimpang dari teks literal tabel roadmap. Kalau user mau tetap ikuti tabel literal, tinggal bilang.
> 
> Temuan tambahan: fungsi `logisticReg` sebelumnya berbagi scope dengan deklarasi LOKAL `function f1(x){...}` (isi persis sama dengan `f1` global dari B5, temuan duplikat `f1` B1) — setelah `logisticReg` dipindah keluar, `f1` lokal ini sekarang **benar-benar jadi dead code** (sebelumnya masih dipakai `logisticReg`). Belum dihapus, sama kasusnya dengan duplikat `f1` yang sudah dicatat sejak B1.

### C. Data Management (tab Data & Variables)

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| C1 | Multi-Dataset System (array `datasets`, tab dataset) | ~~baris 3253–3357~~ (grep marker: `MULTI-DATASET SYSTEM`, baris 65–195 di app.js snapshot sesi ini, 12.603 baris) | `js/data/dataset-manager.js` | ✅ SUDAH (2026-09-18) |
| C2 | Analyze sub-tab state helper (`aState`, field helpers) | ~~baris 3454–3573~~ (grep marker: `Analyze sub-tab state`, baris 76–177 di app.js snapshot sesi ini, 12.382 baris) | `js/analyze/analyze-subtab-ui.js` | ✅ SUDAH (2026-09-20) |
| C3 | Weight helpers (dipakai lintas fitur, didefinisikan awal) | baris 3550–3573 | `js/data/weight-cases.js` | ✅ SUDAH (digabung dengan B23 — lihat catatan) |
| C4 | HTML Helpers umum (`sigBadge`, dll) | ~~baris 3977–3999~~ (grep marker: `HTML HELPERS`, baris 562–580 di app.js baseline 7 Agustus/snapshot upload sesi ini) | `js/core/html-helpers.js` | ✅ SUDAH (2026-09-20) |
| C5 | Custom Select — Modal Bottom Sheet picker | ~~baris 4000–4125~~ (grep marker: `CUSTOM SELECT — MODAL BOTTOM SHEET`, baris 583–705 di app.js snapshot sesi ini setelah C2/C4) | `js/ui-misc/custom-select.js` | ✅ SUDAH (2026-09-20) |
| C6 | Data View — tabel utama, sort/search/select, windowed rendering | ~~baris 4131–4267~~ (grep marker: `DATA VIEW`, baris 486–622 di app.js snapshot sesi ini, 12.248 baris) | `js/data/data-view.js` | ✅ SUDAH (2026-09-20) |
| C7 | Spreadsheet (Bulk Edit) Mode (+ sisa fungsi Data View, lihat catatan) | ~~baris 4268–4898~~ (grep marker: `SPREADSHEET (BULK EDIT) MODE`, baris 493–1122 di app.js snapshot sesi ini, 12.118 baris) | `js/data/spreadsheet-mode.js` | ✅ SUDAH (2026-09-20) |
| C8 | Variable View — edit mode, inline add/change, picker TYPE/MEASURE/ROLE | ~~baris 4899–5692~~ (grep marker: `VARIABLE VIEW`, baris 503–1053 di app.js snapshot sesi ini, 11.497 baris) | `js/data/variable-view.js` | ✅ SUDAH (2026-09-20) |
| C9 | Sliding pill indicator untuk sub-tabs | ~~baris 5693–5736~~ (grep marker: `Sliding pill indicator`, baris 756–790 di app.js snapshot sebelum digabung) | `js/analyze/analyze-subtab-ui.js` | ✅ SUDAH (2026-09-20, digabung) |
| C10 | Transform (Compute), Recode, Filter Cases | ~~baris 6493–6557~~ (grep marker: `Transform & Compute`, baris 6406–6548 di app.js snapshot sesi ini, 10.930 baris — blok fisik 143 baris, lebih panjang dari estimasi baseline 65 baris) | `js/data/data-transform.js` | ✅ SUDAH (2026-09-20) |
| C10★ | *(temuan, tidak ada di roadmap awal)* Imputation UI action — `runImpute`/`imputeAllVars` | baris 6454–6468 di app.js snapshot pas ditemukan (10.797 baris) | `js/data/imputation.js` (file baru, keputusan user) | ✅ SUDAH (2026-09-20) |
| C8★ | *(temuan, tidak ada di roadmap awal)* Analyze Tab Shell — `renderAnalyze(el)` (sidebar grup + daftar sub-tab) | baris 514–754 di app.js snapshot pas dipindah (10.787 baris) | `js/analyze/analyze-tab-shell.js` (file baru, keputusan user) | ✅ SUDAH (2026-09-20) |
| C11 | Missing Data Analysis (Little's MCAR, pattern matrix render) | ~~baris 8348–8615~~ (grep marker: `MISSING DATA ANALYSIS`, baris 3109–3371 di app.js snapshot sesi ini, 10.551 baris — bukan fungsi berdiri sendiri, tapi 1 cabang `else if` di tengah `renderASub()`/D1, lihat catatan) | `js/data/missing-data.js` | ✅ SUDAH (2026-09-20) |

### D. Analyze — Form Renderer (raksasa, disarankan dipecah lebih halus lagi nanti)

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| D1 | Switch besar render form per `currentASub` (descriptive → metaanalysis, 46 cabang sub-tab) | ~~baris 531–4129~~ (grep marker: `function renderASub`, 3.381 baris badan fungsi + 8 fungsi meta-analysis yang nempel setelahnya `svgMetaForestPlot`/`svgFunnelPlot`/`metaSetModel`/`metaSetEffect`/`metaAddStudy`/`metaRemoveStudy`/`metaLoadExample`/`runMetaAnalysis`, di app.js snapshot sebelum sesi ini, 10.070 baris) | `js/analyze/analyze-form-render.js` | ✅ SUDAH (2026-09-19) — pindah fisik + breakdown per-cabang tuntas: dispatcher `renderASub()` 217 baris + 45 fungsi `render<Nama>Form()` (+1 di `missing-data.js`) |

> **D1 ini ~3.600 baris sendirian** (bukan fungsi tunggal, tapi 1 fungsi `renderASub()` berisi ~50 cabang `else if(currentASub===...)`). **Rencana awal**: dipecah bertahap per beberapa cabang tiap sesi (bukan sekaligus) — tiap cabang dibungkus jadi fungsi berdiri sendiri `render<Nama>Form()` (pola sama seperti C11/`renderMissingDataAnalysis`), lalu dipindah ke `analyze-form-render.js`; call-site di `renderASub()` (tetap di app.js) jadi 1 baris `html+=render<Nama>Form();`. 6 cabang pertama (`descriptive`, `ttest`, `onesamp`, `paired`, `rmanova`, `anova`) sempat dipisah dengan pola ini di sesi-sesi sebelumnya.
>
> **Perubahan strategi (2026-09-21)**: alih-alih lanjut per-cabang dari `anova2`, user memindahkan **seluruh badan `renderASub()` sekaligus** (byte-exact, termasuk ~44 cabang yang belum dipisah + 8 fungsi meta-analysis yang nempel setelahnya) dari `app.js` ke `analyze-form-render.js`, lalu menghapus blok itu dari `app.js`. Jadi sekarang `renderASub()` **hanya ada 1 kali**, fisiknya sudah pindah 100% ke `analyze-form-render.js` — tapi di dalamnya masih 1 switch raksasa berisi ~44 cabang inline yang belum dibungkus jadi `render<Nama>Form()` sendiri-sendiri (6 cabang yang sudah dibungkus dari sesi sebelumnya tetap dipanggil dari dalam switch ini, tidak berubah). **Diverifikasi**: diff byte-exact antara badan fungsi lama (app.js baris 531–4129) dan hasil pindahan di `analyze-form-render.js` — 0 selisih; `node --check` lolos di kedua file; grep memastikan `renderASub` + 8 fungsi meta hanya terdeklarasi 1x total (tidak lagi dobel di `app.js`); 31 call-site `renderASub()` yang tersisa di `app.js` dicek masih utuh (resolve ke definisi baru lewat scope-fallback global, aman karena `analyze-form-render.js` dimuat sebelum `app.js` di `index.html`). `app.js` turun dari 10.070 → 6.468 baris.
>
> **D1 SELESAI (2026-09-19)** — breakdown per-cabang tuntas (commit `d306c31` & `5976b6d` di repo). `renderASub()` sekarang murni **dispatcher** 217 baris (baris ~3707–3923 di `analyze-form-render.js`): 46 cabang `else if(currentASub===...)`, masing-masing cuma 1 baris `html+=render<Nama>Form();`, ditutup `el.innerHTML=html;`. 46 fungsi unik yang dipanggil dispatcher = 45 di `analyze-form-render.js` + `renderMissingDataAnalysis` di `missing-data.js` (C11); tiap fungsi terdeklarasi tepat 1x di seluruh `js/`. Sub-tab yang berbagi 1 form (`glm-*`, `hlm-*`, `roc`/`roc_compare`, `km`/`logrank`/`cox`, `poweranalysis`/`powerplot`/`sensitivity`, `moderation`/`simpleslopes`/`jn`) tetap dibedakan lewat `currentASub===` DI DALAM fungsi form-nya sendiri — itu percabangan internal by design, bukan sisa cabang inline. 6 fungsi UI meta-analysis (`metaSetModel`, `metaSetEffect`, `metaAddStudy`, `metaRemoveStudy`, `metaLoadExample`, `runMetaAnalysis`) tinggal di `analyze-form-render.js` di bawah `renderASub()` (2 fungsi SVG-nya, `svgMetaForestPlot`/`svgFunnelPlot`, sudah pindah ke `js/charts/forest-funnel-plot.js` di E4/E5). ⚠️ **Temuan D1 (tidak diubah, cuma dicatat)**: baris `const nF=numFields(),aF=allFields();` di awal `renderASub()` sekarang tidak dipakai lagi (hanya dideklarasikan, sisa dari saat cabang masih inline) — dead code kosmetik, aman dihapus kapan saja.

### E. Charts (SVG Rendering)

> ⚠️ **Temuan E (2026-09-19)**: (1) **E4/E5 sempat tidak di `app.js`** — `svgMetaForestPlot`/`svgFunnelPlot` ikut terangkut ke `js/analyze/analyze-form-render.js` saat D1 dipindah fisik; **sudah dipindah ke `js/charts/forest-funnel-plot.js` (2026-09-19)**. (2) ✅ **KEPUTUSAN (2026-09-19)**: 15 fungsi `svg*` di `app.js` yang sebelumnya TIDAK punya baris di tabel E sudah dipetakan ke **E8–E14**, dikelompokkan per fitur — 1 file chart per file `stats-engine` pasangannya (keputusan user; alternatif menggabung E13+E14 jadi 1 file "misc" ditolak karena isinya tidak berhubungan). Detail fungsi + nomor baris `app.js` ada di tabel di bawah. Dua catatan pemetaan: `svgForestPlot` (forest plot hazard ratio Cox) masuk `survival-plot.js` (E11), BUKAN `forest-funnel-plot.js` — nama mirip dengan `svgMetaForestPlot` (E4) tapi fungsi lain; `svgSimpleSlopesPlot` cuma wrapper 4 baris yang memanggil `svgModerationPlot`, jadi satu paket di E9. Status semuanya ⬜ BELUM — dependency tiap grup dicek saat dipindah (pola E4/E5). (3) `svg-charts-core.js` (E1) isinya bukan "bar/line/pie" seperti baseline lama — tidak ada chart bar/line/pie generik sama sekali; deskripsi E1 & tree Bagian 2 sudah dikoreksi. (4) **Temuan E2 — ✅ SUDAH DIPERBAIKI (2026-09-20, atas permintaan user)**: `svgInteractionPlot` sempat memasukkan `depLabel`, `lineLabel`, `row.level` (tooltip + legend), dan `p.lbl` (level Factor B di tooltip — ditemukan tambahan saat perbaikan, belum tercatat di temuan awal) ke SVG/tooltip tanpa `escHtml` (sementara `xLabel` dan label sumbu X sudah lebih dulu di-escape). Sekarang semuanya dibungkus `escHtml(...)` di `js/charts/interaction-plot.js`. (5) **Temuan E6 — ✅ SUDAH DIPERBAIKI (2026-09-20, atas permintaan user)**: `svgMediationPath` sempat memasukkan `xName`, `mName` (per mediator), dan `yName` ke teks SVG tanpa `escHtml` — pola temuan yang sama dengan E2, nama variabel berasal dari data user. Sekarang ketiganya dibungkus `escHtml(...)` di `js/charts/sem-mediation-diagram.js`. **E2/`svgInteractionPlot` juga sudah diperbaiki (lihat poin 4 di atas dan changelog terbaru)**, tapi itu terjadi belakangan (sesi upload `interaction-plot.js`) — jangan disamakan tanggalnya. (6) **Temuan E7 — ✅ SUDAH DIPERBAIKI (2026-09-20, atas permintaan user)**: `svgSEMDiagram` sempat memasukkan nama konstruk laten (`ln`, dari parameter `latents`) dan nama indikator (`v`, dari `latentMap[ln]`) ke teks SVG tanpa `escHtml` — pola temuan yang sama persis dengan E2/E6, kedua nama berasal dari data user (pilihan variabel di form SEM). Sekarang keduanya dibungkus `escHtml(...)` di `js/charts/sem-mediation-diagram.js`. **E2/`svgInteractionPlot` juga sudah diperbaiki (lihat poin 4 di atas dan changelog terbaru).** (7) **Temuan E9 — ✅ SUDAH DIPERBAIKI (2026-09-20, atas permintaan user)**: di `svgJohnsonNeymanPlot`, `wName` (nama variabel moderator, dari data user) dimasukkan ke judul sumbu X tanpa `escHtml` — pola sama persis dengan E2/E6/E7. Beda dengan `xName` di `svgModerationPlot` yang SUDAH di-escape. `yName` diterima sebagai parameter di kedua fungsi tapi tidak pernah dirender ke SVG sama sekali, jadi aman, tidak perlu escHtml. Sekarang `wName` dibungkus `escHtml(...)` di `js/charts/moderation-plot.js`; detail + verifikasi di entri changelog Bagian 6 terbaru ("E9 — temuan escHtml diperbaiki").

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| E1 | SVG Charts umum (scree plot, histogram, boxplot, scatter, QQ plot, residual plot, correlation matrix + palet warna `PAL2`) | ~~baris 3671–3785~~ (grep marker: `SVG CHARTS`, baris 160–164 header+`PAL2` dan 267–464 fungsi di app.js snapshot commit `5976b6d`, 6.468 baris — 198 baris fungsi + 1 baris `PAL2`, terpotong-potong oleh E2/E3 yang nyelip di antaranya) | `js/charts/svg-charts-core.js` | ✅ SUDAH (2026-09-19) |
| E2 | Interaction Plot (Two-Way / Three-Way ANOVA) | ~~baris 3676–3750~~ (grep marker: `Interaction Plot (Two-Way or Three-Way ANOVA)`, baris 165–238 di app.js snapshot commit `5976b6d`, 6.468 baris — 74 baris) | `js/charts/interaction-plot.js` | ✅ SUDAH (2026-09-19) |
| E3 | ANOVA Source Table renderer (`renderAnovaTable`) | ~~baris 3751–3976~~ (grep marker: `ANOVA Source Table renderer`, baris 160–185 di app.js snapshot sesi ini, 6.190 baris — hanya 26 baris, jauh dari estimasi baseline 226 baris) | `js/output/output-render-basic.js` (file baru; F3 nanti ikut masuk file ini) | ✅ SUDAH (2026-09-19) |
| E4 | Forest Plot SVG (`svgMetaForestPlot`) | ~~baris 9878–9959~~ (grep marker: `Forest Plot SVG`, baris 3930–4010 di `js/analyze/analyze-form-render.js` snapshot sesi ini, 4.141 baris — 81 baris; ikut terangkut ke sana saat D1, BUKAN `app.js`) | `js/charts/forest-funnel-plot.js` | ✅ SUDAH (2026-09-19) |
| E5 | Funnel Plot SVG (`svgFunnelPlot(res)` — signature diubah dari `(studies, res)` pada 2026-09-19, lihat changelog) | ~~baris 9960–10028~~ (grep marker: `Funnel Plot SVG`, baris 4012–4079 di `js/analyze/analyze-form-render.js` snapshot sesi ini, 4.141 baris — 68 baris; BUKAN `app.js`) | `js/charts/forest-funnel-plot.js` | ✅ SUDAH (2026-09-19) |
| E6 | Mediation path diagram SVG (`svgMediationPath`) | ~~baris 10091–10136~~ (grep marker: `function svgMediationPath`, `app.js` baris 231 per 2026-09-19, 6.169 baris) | `js/charts/sem-mediation-diagram.js` | ✅ SUDAH (2026-09-20) |
| E7 | SEM Path Diagram SVG (`svgSEMDiagram`) | ~~baris 12159–12244~~ (grep marker: `function svgSEMDiagram`, `app.js` baris 992 per 2026-09-20, 6.130 baris) | `js/charts/sem-mediation-diagram.js` | ✅ SUDAH (2026-09-20) |
| E8 | Power Analysis chart — `svgPowerCurve`, `svgSensitivityCurve` *(tidak ada di baseline asli — dipetakan 2026-09-19, lihat Temuan E)* | ~~grep marker: `function svgPowerCurve` (`app.js` baris 314) dan `function svgSensitivityCurve` (baris 373) per 2026-09-19~~ (app.js baris 277 & 336 per 2026-09-20, 6.057 baris) | `js/charts/power-plot.js` | ✅ SUDAH (2026-09-20) |
| E9 | Moderation chart — `svgModerationPlot`, `svgSimpleSlopesPlot` (wrapper 4 baris → `svgModerationPlot`), `svgJohnsonNeymanPlot` *(tidak ada di baseline asli)* | ~~grep marker: `function svgModerationPlot` (`app.js` baris 428), `svgSimpleSlopesPlot` (482), `svgJohnsonNeymanPlot` (486; fungsi terbesar di grup E8–E14, ~74 baris)~~ (app.js baris 300, 354, 358 per 2026-09-20, 5.966 baris) | `js/charts/moderation-plot.js` | ✅ SUDAH (2026-09-20) |
| E10 | ROC chart — `svgROC`, `svgROCCompare` *(tidak ada di baseline asli)* | grep marker: `function svgROC` (`app.js` baris 606), `function svgROCCompare` (647) | `js/charts/roc-plot.js` | ⬜ BELUM |
| E11 | Survival chart — `svgKaplanMeier`, `svgForestPlot` (forest plot HR Cox, BUKAN meta-analysis) *(tidak ada di baseline asli)* | grep marker: `function svgKaplanMeier` (`app.js` baris 692), `function svgForestPlot` (752) | `js/charts/survival-plot.js` | ⬜ BELUM |
| E12 | Discriminant & Cluster chart — `svgDiscriminantPlot`, `svgClusterPlot`, `svgElbow`, `svgDendrogram` *(tidak ada di baseline asli)* | grep marker: `function svgDiscriminantPlot` (`app.js` baris 1584), `svgClusterPlot` (1638), `svgElbow` (1668), `svgDendrogram` (1700) | `js/charts/discriminant-cluster-plot.js` | ⬜ BELUM |
| E13 | Bayesian chart — `svgBayesPosterior` (prior vs posterior) *(tidak ada di baseline asli)* | grep marker: `function svgBayesPosterior` (`app.js` baris 1278) | `js/charts/bayesian-plot.js` | ⬜ BELUM |
| E14 | Multiple Imputation chart — `svgConvergence` (data `computeMICE`, B16; sempat dicatat di B18 karena lokasinya nyempil antara blok Survival dan polyfill `chi2CDF`) *(tidak ada di baseline asli)* | grep marker: `function svgConvergence` (`app.js` baris 788) | `js/charts/imputation-plot.js` | ⬜ BELUM |

### F. Output View (tampilan hasil analisis)

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| F1 | Output View — Tab Group System (shell/container) | baris 13935–13951 | `js/output/output-view-shell.js` | ⬜ BELUM |
| F2 | Ringkasan per-tipe output (dispatcher singkat by `o.type`) | baris 13952–14212 | `js/output/output-view-shell.js` | ⬜ BELUM |
| F3 | `renderOutput()` — render detail basic (descriptive s/d GLM/Poisson/MANOVA) | baris 14213–14966 | `js/output/output-render-basic.js` | ⬜ BELUM |
| F4 | `renderOutput()` lanjutan — HLM, Alpha, Kappa, EFA, CFA, SEM, Mediation, Discriminant, Cluster | baris 14967–15414 | `js/output/output-render-advanced.js` | ⬜ BELUM |
| F5 | `renderOutput()` lanjutan — Power, Moderation, MI, ROC, Survival, Cox, Bayesian, Time Series, Meta-analysis | baris 15415–15701 | `js/output/output-render-advanced.js` | ⬜ BELUM |
| F6 | `renderPowerOutput()` khusus + helper lain sesudahnya | baris 15702–16329 | `js/output/output-power-render.js` | ⬜ BELUM |
| F7 | APA-format table copy (`buildAPAText`, per-output & bulk copy) | tersebar di dalam F3–F5 (cari via `buildAPAText`) | `js/output/apa-copy.js` | ⬜ BELUM |

### G. Export

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| G1 | Build & download satu output sebagai `.doc` | baris 17075–17099 | `js/export/export-word.js` | ⬜ BELUM |
| G2 | Build HTML untuk satu output (mode table/chart/both) | baris 17100–17193 | `js/export/export-word.js` | ⬜ BELUM |
| G3 | Get chart SVG string untuk output tertentu | baris 17194–17239 | `js/export/export-word.js` | ⬜ BELUM |
| G4 | Per-Output Export Dialog | baris 16963–17075, 16966–17239 | `js/export/export-dialog.js` | ⬜ BELUM |
| G5 | Export Word Dialog (pilih group/sub-type) | baris 17240–17459 | `js/export/export-dialog.js` | ⬜ BELUM |
| G6 | Build & download output terpilih sebagai `.doc` (bulk) | baris 17460–17519 | `js/export/export-word.js` | ⬜ BELUM |
| G7 | `exportToWord()` — assembly HTML final + trigger download | baris 17520–17642 | `js/export/export-word.js` | ⬜ BELUM |
| G8 | Export ke Excel (SheetJS) | baris 16850–16962 | `js/export/export-excel.js` | ⬜ BELUM |

### H. Session, File System, & Autosave

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| H1 | `saveSession()` (versi ringkas, quick save) | baris 16382–16420 | `js/session/session-save-restore.js` | ⬜ BELUM |
| H2 | `saveSession()` (versi lengkap + export) | baris 16721–16788 | `js/session/session-save-restore.js` (gabung dgn H1, cek duplikasi!) | ⬜ BELUM |
| H3 | Dirty-flag tracking (tandai ada perubahan belum tersimpan) | baris 17928–17972 | `js/session/session-save-restore.js` | ⬜ BELUM |
| H4 | Build session snapshot (struktur sama dgn saveSession) | baris 17973–17984 | `js/session/session-save-restore.js` | ⬜ BELUM |
| H5 | Restore snapshot dari object | baris 17985–18000 | `js/session/session-save-restore.js` | ⬜ BELUM |
| H6 | File System — Save/Open `.oss` (project file penelitian) | baris 17918–18001 | `js/session/oss-filesystem.js` | ⬜ BELUM |
| H7 | `saveToFile()` | baris 18001–18053 | `js/session/oss-filesystem.js` | ⬜ BELUM |
| H8 | Save-As (selalu buka picker) | baris 18054–18059 | `js/session/oss-filesystem.js` | ⬜ BELUM |
| H9 | Import/Open File (`importFromFile`, `handleOSSImport`) | baris 18060–18151 | `js/session/oss-filesystem.js` | ⬜ BELUM |

> ⚠️ **Perhatian saat mengerjakan H1 & H2**: ada 2 fungsi `saveSession()` di baseline (baris 16382 dan 16721) — kemungkinan salah satunya sisa/duplikat lama yang belum dibersihkan. **Cek dulu isi keduanya, tanya ke user kalau ragu mana yang dipakai**, jangan langsung digabung sembarangan.

### I. UI Misc (fitur pendukung lain)

| # | Fitur / Section | Lokasi di `app.js` (baseline) | File Target | Status |
|---|---|---|---|---|
| I1 | Syntax View (tampilan ala syntax SPSS) | baris 13819–13906 | `js/ui-misc/syntax-view.js` | ⬜ BELUM |
| I2 | Pivot View | baris 13907–13932 | `js/ui-misc/pivot-view.js` | ⬜ BELUM |
| I3 | AI View (chat history + UI tab AI) | baris 16330–16379 | `js/ui-misc/ai-view.js` | ⬜ BELUM |
| I4 | Command Palette (Ctrl+K) | baris 16544–16718 | `js/ui-misc/cmd-palette.js` | ⬜ BELUM |
| I5 | Activity Loading Popup System (popup "sedang memproses...") | baris 17643–17917 | `js/ui-misc/activity-loading-popup.js` | ⬜ BELUM |
| I6 | PWA — Service Worker registration, install prompt, tombol Install App | baris 18152–18287 | `js/ui-misc/pwa-install.js` | ⬜ BELUM |

---

## 4. ATURAN TEKNIS SAAT MEMISAHKAN FILE

1. **Tidak pakai ES Modules** (`import`/`export`) kecuali diminta lain — semua file baru tetap `<script>` biasa yang dimuat berurutan lewat `index.html`, supaya semua fungsi & variabel tetap global dan bisa dipanggil dari `onclick="..."` hasil `innerHTML`.
2. **Urutan `<script>` di `index.html` itu penting** — file yang isinya dipakai duluan harus dimuat duluan. Urutan yang disarankan:
   ```html
   <!-- 1. Core & state paling awal -->
   <script src="js/core/icons.js"></script>
   <script src="js/core/state.js"></script>
   <script src="js/core/dialog.js"></script>
   <script src="js/core/watercolor-bg.js"></script>

   <!-- 2. Stats engine (murni komputasi, tidak butuh DOM) -->
   <script src="js/stats-engine/stats-distributions.js"></script>
   <script src="js/stats-engine/stats-core-basic.js"></script>
   <script src="js/stats-engine/stats-core-advanced.js"></script>
   <script src="js/stats-engine/stats-nonparametric.js"></script>
   <!-- ...dst sesuai yang sudah dipisah... -->

   <!-- 3. Data layer -->
   <script src="js/data/dataset-manager.js"></script>
   <script src="js/data/data-view.js"></script>
   <!-- ...dst... -->

   <!-- 4. Analyze, charts, output, export -->
   <!-- 5. Session & file system -->
   <!-- 6. UI misc + router (paling akhir, karena manggil semua di atas) -->
   <script src="js/core/router.js"></script>
   ```
   Setiap kali satu file baru ditambahkan, **update urutan ini juga** di `index.html` dan di bagian ini.
3. **Setiap file baru dimulai dengan header komentar** singkat: nama modul, fitur yang dicover, dan dependency-nya (fungsi/var global apa dari file lain yang dia butuhkan). Contoh:
   ```js
   // ════════════════════════════════════════════════════════════
   // js/data/variable-view.js
   // Fitur: tab Variables — edit mode, inline add/change, picker TYPE/MEASURE/ROLE
   // Depends on: vars[], IC (icons.js), ossDialog (dialog.js), showToast()
   // ════════════════════════════════════════════════════════════
   ```
4. **Jangan ubah logic saat memindahkan** — pemisahan tahap ini murni "potong-tempel" + rapikan dependency. Kalau nemu bug/duplikat (seperti kasus H1/H2 di atas), catat & tanya user dulu, jangan sekaligus refactor logic tanpa izin.
5. **Setelah setiap pemisahan, wajib dites**: buka app di browser, cek console tidak ada `ReferenceError`/`is not defined`, dan coba jalankan fitur yang baru dipindah.
6. **Commit / simpan per-bagian kecil.** Jangan pernah mencoba memisahkan lebih dari 1-2 baris tabel di Bagian 3 dalam satu sesi kalau bagian itu besar (seperti D1 yang ~4000 baris) — pecah jadi beberapa sesi.
7. **WAJIB: setiap temuan (bug, duplikat, dead code, dependency aneh, penamaan bentrok, dll) yang muncul saat mengerjakan SATU bagian tabel HARUS dicatat**, tidak boleh cuma disimpan di kepala/percakapan lalu hilang di sesi berikutnya. Formatnya:
   - **Di catatan atas tabel section yang relevan (Bagian 3)** — pakai format `⚠️ **Temuan [kode bagian]**: ...` tepat di atas tabel A/B/C/dst yang sedang dikerjakan, supaya sesi berikutnya yang buka bagian itu langsung lihat sebelum mulai kerja (bukan cuma terkubur di changelog panjang Bagian 6).
   - **Di baris changelog Bagian 6** milik bagian yang sedang dikerjakan saat itu — jelaskan apa yang ditemukan, kenapa itu temuan (bukan cuma dipindah), dan status-nya (belum diperbaiki / sudah diperbaiki + alasan/konfirmasi user).
   - **Di header komentar file baru** kalau temuan itu langsung relevan dengan fungsi yang ada di file itu (biar siapapun yang buka filenya langsung tahu, tanpa perlu balik baca ARCHITECTURE.md).
   - Temuan TIDAK BOLEH diperbaiki diam-diam tanpa dicatat + tanpa konfirmasi user (lihat poin 4) — tapi mencatatnya WAJIB dilakukan meski keputusan perbaikannya masih menunggu.
   - Kalau temuan itu berupa 2 baris tabel (misal B23/C3) yang ternyata menunjuk ke kode yang sama persis, itu juga temuan yang wajib dicatat (bukan cuma dipisah diam-diam & ditandai selesai tanpa penjelasan).

---

## 5. URUTAN PRIORITAS DISARANKAN

Karena dependency paling sedikit → paling banyak, urutan pengerjaan yang disarankan:

1. **A (Core)** — paling independen, base untuk semua.
2. **B (Stats Engine)** — pure functions, tidak sentuh DOM, resiko break paling kecil.
3. **C (Data Management)** — depends on A + sedikit B.
4. **E (Charts)** — depends on B (data hasil komputasi) + A (icons).
5. **G, H (Export & Session)** — depends on state dari C.
6. **F (Output View)** — depends on B, E.
7. **D (Analyze Form Render)** — paling besar & paling banyak dependency, cocok dikerjakan terakhir & bertahap per kelompok analisis.
8. **I (UI Misc)** — bisa disisipkan kapan saja karena relatif independen.

Silakan menyimpang dari urutan ini kalau user minta fitur spesifik duluan — urutan ini cuma saran default.

---

## 6. RIWAYAT PEMISAHAN (CHANGELOG)
| 2026-09-16 | B26 | `js/stats-engine/stats-timeseries.js` (baru) | Dipindah sbg fungsi global, pola sama B1-B25: `tsACF`, `tsPACF`, `tsDiff`, `tsARIMA` (AR via Yule-Walker, MA via ACF residual, AIC/BIC, forecast 1-step + 95% CI, ACF/PACF/residual-ACF), `tsDecomp` (Classical Decomposition additive/multiplicative — moving average trend, seasonal index, remainder, seasonal strength). Dependency `SE.mean`/`SE.f4`/`SE.vari` diakses lewat `SE.` di dalam function body (runtime), aman dipindah ke file pre-`app.js`, pola sama B13/B15/B19/B21. **Scope B26 murni lokasi preview** (`analyze-form-render`, sub-tab Time Series) sesuai kolom "Lokasi" di tabel roadmap — `runTimeSeries()` (fungsi terpisah untuk tombol "▶ Run", ada implementasi duplikat sendiri `tsACF2`/`tsDiff2` + inline ARIMA/decomp) **BUKAN** bagian lokasi ini dan sengaja **tidak disentuh**, biar scope tetap 1 lokasi per sesi. **Temuan penting (✅ DIPERBAIKI 2026-09-16, sesi ini juga)**: `SE.vari` ternyata **tidak pernah** dimasukkan ke `return {...}` objek `SE` di `app.js` — persis pola bug `SE.matInv` (ditemukan & diperbaiki di sesi lain, lihat Bagian 8) dan `SE.fCDF`/`SE.chiCDF` (B1). Dampaknya: mode **Decomposition** pada Time Series Analysis (baik preview maupun tombol "Run", dua-duanya manggil `SE.vari`) selalu gagal diam-diam (`tsRes._err` di preview, kemungkinan error ter-catch di `runSafe` untuk tombol Run) — mode **ARIMA tidak terdampak** (tidak pakai `SE.vari`). Dikonfirmasi BUKAN regresi dari split ini: diuji dgn Node `vm` module load urutan file persis seperti `index.html`, `typeof SE.vari` sudah `undefined` SEBELUM `stats-timeseries.js` disentuh sama sekali. **Sudah diperbaiki** atas permintaan user: `vari` ditambahkan ke return list `SE` di `app.js` (lihat Bagian 8). Diverifikasi ulang setelah fix via Node `vm` module: `SE.vari(Y)` sekarang mengembalikan nilai identik dengan implementasi manual, `tsDecomp` jalan normal untuk additive maupun multiplicative tanpa workaround, `tsARIMA` (tidak pakai `SE.vari`) dicek tetap tidak berubah. Diverifikasi via Node `vm` module (urutan load persis `index.html`, `stats-timeseries.js` dimuat sebelum `app.js`): `tsARIMA` konvergen pada data sintetis trend+seasonal (AIC/BIC/forecast masuk akal, ACF/PACF meluruh sesuai pola AR), `tsDecomp` diuji terpisah dengan `SE.vari` di-patch sementara (khusus test awal sebelum fix, bukan bagian file yang dipindah) untuk membuktikan logic dekomposisinya sendiri sudah benar & tidak berubah dari versi asli — hasil `strength`/`sse` masuk akal untuk data dengan pola trend+seasonal jelas. `index.html` diupdate: `stats-timeseries.js` (baru) dimuat setelah `stats-discriminant-cluster.js`, sebelum `app.js`. |

> Tambahkan 1 baris baru di sini SETIAP KALI selesai memisahkan sesuatu. Format: tanggal, bagian yang dipisah (kode # dari tabel Bagian 3), file tujuan, catatan singkat.

| Tanggal | Bagian (#) | File Tujuan | Catatan |
|---|---|---|---|
| 2026-08-07 | — | — | README arsitektur ini dibuat pertama kali. Belum ada file yang dipisah — semua kode masih 100% di `app.js` monolith (18.345 baris). |
| 2026-09-13 | A1 | `js/core/watercolor-bg.js` | Watercolor canvas animation, murni IIFE self-contained, tidak ada dependency ke file lain. |
| 2026-09-13 | A2, A6 | `js/core/router.js` | Digabung sesuai rencana Bagian 2: page router (`appReady`, `enterApp`, `toggleSide`, `closeSide`) + tab switching (`switchTab`, `TAB_LABELS`, `SUB_TO_GROUP`, `openAnalyze`, dst). Boot sequence akhir app.js (`updateBadges(); renderDsSidebar(); loadSession(); enterApp();`) juga dipindah ke sini karena isinya trigger routing awal. `index.html` diupdate: `router.js` dimuat **setelah** `app.js` karena masih depend ke banyak fungsi render yang belum dipisah. |
| 2026-09-13 | A3 | `js/core/icons.js` | Objek `IC` (SVG icon system), pure data, tidak ada dependency. |
| 2026-09-13 | A4 | `js/core/dialog.js` | `escDlg()` + `ossDialog()`. Catatan: ada komentar sisa di app.js "`Duplikat ossDialog dihapus`" dekat akhir file — dicek, itu memang cuma komentar penanda lama, tidak ada kode duplikat aktif untuk A4 ini. |
| 2026-09-13 | A5 | `js/core/loading-screen.js` | Loading screen progress bar dismiss logic, self-contained IIFE. |
| 2026-09-13 | A7 | `js/ui-misc/badges.js` | User konfirmasi pakai versi aktif (~baris lama 16150). Versi lama/dead code (~baris lama 3315) **dihapus permanen**, bukan dipindah. `index.html` diupdate: `badges.js` dimuat setelah `app.js`, sebelum `router.js`. |
| 2026-09-13 | B8 | `js/stats-engine/stats-core-advanced.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B7: `efa`, `cfa`, `kmoLabel`, `jacobiEigen`, `varimaxRotate` — ditambahkan (append) ke `stats-core-advanced.js`. **Sesuai rencana**: duplikat kedua `matMul/matT/matDet/matInv` (signature array 2D, dipakai EFA/CFA) sekarang **menimpa** versi B3 (signature flat-array, dipakai `multipleReg`) di file yang sama — persis mereplikasi behavior asli app.js monolith, bukan bug baru. Tidak ada perubahan `index.html` (append ke file yang sudah dimuat). |
| 2026-09-13 | B7 | `js/stats-engine/stats-core-basic.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B6: `lsdPosthoc`, `bonferroniPosthoc`, `holmBonferroni` — ditambahkan (append) ke `stats-core-basic.js` yang sudah ada dari B2/B5, tidak perlu tag `<script>` baru di `index.html`. **Temuan**: `holmBonferroni` dipanggil `SE.lsdPosthoc`/`SE.bonferroniPosthoc` ada di UI (pilihan post-hoc method ANOVA), tapi **`holmBonferroni` sendiri tidak dipanggil di mana pun** (tidak ada opsi 'holm' di UI) — sepertinya dead code / fitur belum di-wire ke UI. Belum disentuh, cuma dicatat. |
| 2026-09-13 | B27 (baru) | `js/stats-engine/stats-crosstab.js` | Fungsi `chiSquare` (Chi-Square Test of Independence untuk Crosstab) ternyata **tidak tercatat** di tabel roadmap manapun sejak awal (bukan bagian B1-B26 baseline). Ditambahkan sebagai row baru **B27** dan dipisah jadi file sendiri sesuai permintaan user, karena murni fungsi komputasi tanpa DOM (sama seperti B1-B4), bukan digabung ke `stats-nonparametric.js`. |
| 2026-09-13 | B6 | `js/stats-engine/stats-glm-hlm.js` (baru) + `js/stats-engine/stats-core-advanced.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B4: `glmUnivariate`, `bonferroni` → file baru `stats-glm-hlm.js`; `partialCorr`, `canonicalCorr` → ditambahkan (append) ke `stats-core-advanced.js` yang sudah ada dari B3, sesuai pemetaan tabel B6 (GLM & korelasi target file beda). `index.html` diupdate: `stats-crosstab.js` dimuat setelah `stats-nonparametric.js`, `stats-glm-hlm.js` setelah itu — keduanya sebelum `app.js`. |
| 2026-09-13 | B4 | `js/stats-engine/stats-nonparametric.js` | Dipindah sbg fungsi global (bukan IIFE), pola sama B1-B3: `mwExactDist`, `mannWhitney`, `kruskalWallis`, `wilcoxon`, `cronbachAlpha`, `cohenKappa`, `fisherExact`. **Temuan baru**: `chiSquare` (chi-square untuk crosstab), yang lokasinya persis nempel setelah `fisherExact`, ternyata **tidak ada di tabel roadmap Bagian 3 manapun** (bukan bagian B1-B26 yang tercatat) — sengaja **tidak** diikutkan dan dibiarkan di app.js sampai ada keputusan mau dipetakan ke mana (kemungkinan perlu ditambah row baru di tabel B atau masuk section C sebagai bagian dari Crosstab UI). `index.html` diupdate: `stats-nonparametric.js` dimuat setelah `stats-core-advanced.js`, sebelum `app.js`. |
| 2026-09-13 | B3 | `js/stats-engine/stats-core-advanced.js` | Dipindah sbg fungsi global (bukan IIFE), pola sama B1/B2: `matMul`, `matT`, `matDet`, `matInv` (signature `matMul(A,m,n,B,p)` dkk, dipakai `multipleReg`), `multipleReg`, `repeatedMeasuresAnova`. Sesuai konfirmasi user, duplikat `matMul/matT/matDet/matInv` **dibiarkan apa adanya** — versi kedua (dipakai EFA/CFA/manovaProper, B8/B9) belum dipindah, nanti kalau dipindah ke file yang sama akan menimpa versi ini persis seperti behavior asli di app.js monolith (bukan bug baru). `index.html` diupdate: `stats-core-advanced.js` dimuat setelah `stats-core-basic.js`, sebelum `app.js`. |
| 2026-09-13 | HOTFIX (B1) | `app.js` | User lapor `SyntaxError: Identifier 'normCDF' has already been declared` yang bikin seluruh `app.js` gagal parse (efek domino: `missCount is not a function` dkk). Penyebab: sisa `function normCDF(z){...}` (dead code) di `app.js` yang lolos tidak terhapus waktu sesi B1, bentrok scope global dengan `normCDF` yang sudah ada di `js/stats-engine/stats-distributions.js`. **Dihapus permanen** dari `app.js` (bukan dipindah — sudah ada versi aktifnya di `stats-distributions.js`). Dicek dgn `node --check`, tidak ada syntax error lagi. |
| 2026-09-13 | B2, B5 | `js/stats-engine/stats-core-basic.js` | Dipindah sbg fungsi global (bukan IIFE), pola sama dgn B1: `sw`, `ksTest`, `levene`, `descriptive`, `tTest`, `onewayANOVA`, `qrangeCDF`, `tukeyHSD`, `qrangeCDF_inv`, `pearsonR`, `spearmanR`, `linearReg`, `oneSampleT`, `pairedTTest` (B2) + `f4`, `f1`, `pFmt`, `dLabel`, `effLabel`, `cohenDCI`, `eta2CI` (B5, versi yang ada di dalam IIFE `SE` — bukan versi duplikat lain di luar `SE`). Duplikat `matMul/matT/matDet/matInv` & `f1` (temuan B1) **dibiarkan apa adanya** sesuai konfirmasi user (2026-09-13) — belum diperbaiki, cuma dipindahkan. Ditemukan temuan baru: 2 deklarasi `pFmt` lain di luar `SE` (beda scope, dicatat di atas tabel B, belum diperbaiki). `index.html` diupdate: `stats-core-basic.js` dimuat setelah `stats-distributions.js`, sebelum `app.js`. |\n| 2026-09-13 | B1 | `js/stats-engine/stats-distributions.js` | Dipindah sbg fungsi global (bukan IIFE) — lihat catatan strategi di atas tabel B. Ditemukan & diselesaikan: (1) duplikat `fCDF` — versi lama (`betaInc`/`lnGamma`, dead code) **dihapus permanen** sesuai konfirmasi user, cuma versi aktif (Wilson-Hilferty/`incompleteBeta`) yang dipindah; (2) `fCDF` & `chiCDF` ternyata dipanggil di banyak tempat via `SE.fCDF`/`SE.chiCDF` tapi tidak pernah ada di `return {...}` objek `SE` (bug lama) — sesuai konfirmasi user, **ditambahkan** ke return list di app.js supaya `SE.fCDF`/`SE.chiCDF` benar-benar terisi. `index.html` diupdate: `stats-distributions.js` dimuat SEBELUM `app.js` (karena `return {fCDF, chiCDF,...}` di app.js butuh fungsi ini sudah ada). Ditemukan juga (belum diperbaiki, lihat catatan ⚠️ di atas tabel B): duplikat `matMul/matT/matDet/matInv` (signature beda, kemungkinan bug di Multiple Regression) dan duplikat `f1`. |
| 2026-09-15 | B13 | `js/stats-engine/stats-mediation-sem.js` (baru) | Dipindah sbg fungsi global: `computeMediation` (Baron-Kenny 4-step, Sobel test, Bootstrap CI untuk indirect effect, mendukung multi-mediator). Depends on global `data` (dataset aktif) dan namespace `SE.*` (`pearsonR`, `mean`, `std`, `tP`, `normCDF`, `f4`, `matInv`) — semua diakses lewat `SE.` bukan pemanggilan langsung, jadi aman dipindah ke file yang dimuat SEBELUM `app.js`: fungsi baru benar-benar jalan saat dipanggil user (runtime, setelah `SE` terbentuk), bukan saat file di-parse. File ini rencananya juga jadi tujuan B15 (Moderation) & B19 (SEM core) sesuai peta Bagian 2 — belum dikerjakan, baru B13 yang masuk. **Catatan**: rendering SVG path diagram (`svgMediationPath`) BUKAN bagian B13, tetap di `app.js`. `index.html` diupdate: `stats-mediation-sem.js` (baru) dimuat setelah `stats-metaanalysis.js`, sebelum `app.js`. |
| 2026-09-15 | B12 | `js/stats-engine/stats-metaanalysis.js` (baru) | Dipindah sbg fungsi global: `computeMetaAnalysis` (Fixed-Effect Inverse-Variance + Random-Effects DerSimonian-Laird, Cochran's Q, I², tau², data per-studi untuk Forest/Funnel Plot). Fungsi ini **self-contained** — semua helper (`f4`, `f3`, `pFmt`, `pChiSquare`, `normCDF2`) didefinisikan lokal di dalamnya, tidak pakai `isV`/`req`/`mean`/dll dari `stats-distributions.js`, jadi dipindah apa adanya tanpa perlu cek dependency. **Catatan penting**: rendering SVG-nya (`svgMetaForestPlot`, `svgMetaFunnelPlot`), yang lokasinya persis nempel setelah `computeMetaAnalysis` di app.js, **BELUM** ikut dipindah — itu rencananya masuk `js/charts/forest-funnel-plot.js` (bagian E, belum ada nomor row di tabel), dibiarkan dulu di `app.js`. `index.html` diupdate: `stats-metaanalysis.js` (baru) dimuat setelah `stats-glm-hlm.js`, sebelum `app.js`. |
| 2026-09-15 | B11 | `js/stats-engine/stats-core-basic.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B10: `threewayANOVA` (Three-Way ANOVA Factorial A×B×C, Type I SS — 3 main effect + 3 interaksi two-way + 1 interaksi three-way + error), termasuk sistem sparse-cell 3-tier warning (empty cell = hard stop model singular, singleton cell = caution non-blocking). Helper privat `cellVals`, `cellMean`, `effect` tetap sebagai closure lokal di dalam fungsi (bukan dipindah terpisah) — tidak ada konflik nama dengan fungsi lain di file. Dependency (`isV`, `req`, `mean`, `fCDF`, `f4`, `pFmt`) semuanya sudah global dari B1/B2/B5. Tidak ada perubahan `index.html` (append ke file yang sudah dimuat sebelum `app.js`). |
| 2026-09-15 | B10 | `js/stats-engine/stats-core-basic.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B9: `twowayANOVA` (Two-Way ANOVA Factorial 2×k, Type I SS — main effect A, main effect B, interaksi A×B, error). Fungsi ini self-contained, tidak ada helper privat yang perlu dipindah/di-rename. Dependency (`isV`, `req`, `mean`, `fCDF`, `f4`, `pFmt`) semuanya sudah global dari B1/B2/B5 sebelumnya, tidak ada konflik nama ditemukan. Tidak ada perubahan `index.html` (append ke file yang sudah dimuat sebelum `app.js`). |
| 2026-09-15 | B9 | `js/stats-engine/stats-core-advanced.js` (ditambah) | Dipindah sbg fungsi global (bukan IIFE), pola sama B3/B6/B8: `manovaProper` (Pillai, Wilks, Hotelling-Lawley, Roy, Box's M). **Temuan 1**: `manovaProper` punya 3 helper privat yang cuma dipakai olehnya sendiri — `chi2CDF`, `regularizedGammaP`, `regularizedGammaQ` (dicek: tidak dipakai di tempat lain dalam closure `SE` app.js, beda dari `SE.chi2CDF` yang di-fallback terpisah dgn implementasi sendiri di baris lain app.js) — ketiganya ikut dipindah bersama `manovaProper`. **Temuan 2 (setelah `stats-core-advanced.js` asli diupload user)**: file tujuan **sudah punya fungsi global `chi2CDF`** yang dipakai Bartlett's test (EFA, ~baris 589) & fit index CFA (~baris 775) — sumbernya bukan dari B9, kemungkinan sudah ada sejak B8/sebelumnya tapi tidak tercatat di changelog manapun. Kalau helper privat `manovaProper` (nama sama: `chi2CDF`) ditempel apa adanya, dia akan **menimpa** `chi2CDF` yang sudah dipakai Bartlett/CFA tsb (beda dari kasus duplikat `matMul` B8 yang memang sengaja meniru bug asli monolith). Untuk MENGHINDARI regresi ke fitur B8 yang sudah jalan, 3 helper privat itu **di-rename** (bukan cuma potong-tempel): `chi2CDF→manovaChi2CDF`, `regularizedGammaP→manovaRegGammaP`, `regularizedGammaQ→manovaRegGammaQ`, dan 1 titik pemanggilan internal di `manovaProper` (perhitungan p-value Box's M) diupdate mengikuti nama baru. **Nilai/rumus perhitungan `manovaProper` sendiri tidak berubah sama sekali** — murni rename identifier privat untuk hindari tabrakan scope global, bukan perubahan logic. Dependency lain (`isV`, `mean`, `req`, `matMul`/`matT`/`matDet`/`matInv` versi 2D — sudah ada di file ini dari B8 —, `jacobiEigen` — juga sudah ada dari B8 —, `f4`, `pFmt`, `lnG`) sudah global/tersedia, tidak perlu dipindah ulang. Tidak ada perubahan `index.html`. |
| 2026-09-15 | B14 | `js/stats-engine/stats-poweranalysis.js` (baru) | Dipindah sbg fungsi global (bukan IIFE): `normCDFpw`, `normInvpw`, `powerFromNcpT`, `powerFromNcpF`, `computePower`, `effectLabels2`. Semua dependency ke `SE.*` (`normCDF`, `normInv`, `fInv`, `fCritApprox`, `chiCritApprox`, `f4`) diakses lewat `SE.` di dalam function body, bukan di top-level, jadi aman dipindah ke file yang dimuat SEBELUM `app.js` (baru benar-benar jalan saat dipanggil user, setelah `SE` terbentuk). **Temuan penting — TIDAK ikut dipindah**: 3 blok `if(!SE.fInv){...}` / `if(!SE.fCritApprox){...}` / `if(!SE.chiCritApprox){...}` yang nempel persis setelah `effectLabels2` di app.js lama. Ini beda dari isi `computePower` — ini top-level statement yang langsung dieksekusi saat script diparse (bukan di dalam function), dan langsung menulis ke objek `SE` itu sendiri (bukan cuma membaca lewat `SE.`). Karena `var SE = (() => {...})();` baru terbentuk di awal eksekusi `app.js` (baris 4-543), kalau 3 blok ini dipindah ke file yang dimuat SEBELUM `app.js`, akan langsung `ReferenceError: SE is not defined` saat file itu diparse — beda kasus dengan B1 (`fCDF`/`chiCDF` yang butuh dimuat sebelum app.js) karena di sana yang butuh urutan itu adalah *pembaca* (`return {...}` di app.js), bukan *penulis* top-level ke `SE` dari file lain. Ketiganya **dibiarkan tetap di app.js**, di lokasi yang sama persis (tidak dipindah posisi), karena aman & tidak berubah perilaku — sudah tereksekusi sebelum user sempat memanggil `computePower` yang butuh `SE.fInv`/dst. Juga **tidak dipindah** (sesuai pola B12/B13, rencana masuk bagian charts/section E nanti): `svgPowerCurve`, `svgSensitivityCurve` (rendering SVG), dan `runPowerAnalysis` (UI wiring, depends `aState`/`addOutput`) — ketiganya tetap di `app.js`. **Temuan minor**: `powerFromNcpT` ternyata tidak dipanggil di mana pun (baik di app.js lama maupun setelah dipindah) — sepertinya dead code, dipindahkan apa adanya tanpa dihapus, cuma dicatat di sini. `index.html` diupdate: `stats-poweranalysis.js` (baru) dimuat setelah `stats-mediation-sem.js`, sebelum `app.js`. |
| 2026-09-15 | B15 | `js/stats-engine/stats-mediation-sem.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B14: `computeModeration` (OLS dengan interaction term X×W + covariates, simple slopes W Mean±1SD, Johnson-Neyman region of significance). Ditambahkan (append) ke `stats-mediation-sem.js` yang sudah ada dari B13 (`computeMediation`) — sesuai rencana peta Bagian 2 (file target sama untuk B13/B15/B19). Dependency (`data` global, `SE.isV`, `SE.mean`, `SE.std`, `SE.matInv`, `SE.fP`, `SE.tP`, `SE.normInv`, `SE.validNums`, `SE.f4`, `SE.pFmt`) semua diakses lewat `SE.` di dalam function body, aman dipindah ke file pre-app.js (pola sama B13). Helper privat `olsAug`/`matInvLocal` di dalam `computeModeration` tetap closure lokal (tidak dipindah terpisah) — dicek, namanya kebetulan sama dengan `matInvLocal` milik `computeMediation` (juga closure lokal di fungsinya sendiri), tapi karena keduanya scoped di dalam function masing-masing (bukan global), **tidak ada konflik/tabrakan** — dua-duanya aman hidup berdampingan di file yang sama. **Tidak ikut dipindah** (pola sama B12/B13/B14, rencana masuk bagian charts/section E nanti): `svgModerationPlot`, `svgSimpleSlopesPlot`, `svgJohnsonNeymanPlot` (rendering SVG) dan `runModeration` (UI wiring, depends `aState`/`addOutput`) — ketiganya tetap di `app.js`. Tidak ada perubahan `index.html` (append ke file yang sudah dimuat sebelum `app.js` sejak B13). |
| 2026-09-15 | B16 | `js/stats-engine/stats-imputation.js` (baru) | Dipindah sbg fungsi global: `computeMICE` (MICE — Multivariate Imputation by Chained Equations, metode PMM & Normal, pooling lewat Rubin's Rules). Dependency: `vars` (global metadata variabel) dan `isMiss` (global helper cek missing, `var isMiss=v=>...` didefinisikan di awal app.js baris 803) — dua-duanya diakses langsung by-name (bukan lewat `SE.`), tapi tetap aman dipindah ke file pre-app.js karena hanya dipanggil di dalam body `computeMICE` (runtime, setelah `vars`/`isMiss` terbentuk saat app.js jalan), bukan top-level. Dependency lain (`SE.matInv`, `SE.mean`, `SE.std`, `SE.validNums`, `SE.f4`) juga lewat `SE.`, pola sama B1-B15. Helper privat `olsPredict`, `pmm`, `normalImpute` tetap closure lokal di dalam `computeMICE`, tidak ada konflik nama global. **Tidak ikut dipindah**: `toggleMIVar`, `runMultipleImputation` (UI wiring, depends `aState`/`renderASub`/`addOutput`/`updateBadges`/`showToast`) — tetap di `app.js`. `index.html` diupdate: `stats-imputation.js` (baru) dimuat setelah `stats-poweranalysis.js`, sebelum `app.js`. |
| 2026-09-15 | B17 | `js/stats-engine/stats-roc.js` (baru) | Dipindah sbg fungsi global: `computeROC` (kurva TPR/FPR per threshold, AUC trapezoidal, 95% CI Hanley-McNeil, titik optimal Youden index) dan `deLongTest` (test pembanding 2 AUC untuk ROC Compare). Dependency: `isMiss` (global by-name, sama pola B16) dan `SE.*` (`isV`, `normInv`, `normCDF`, `f4`, `pFmt`) lewat `SE.` — semua hanya diakses di dalam function body (runtime), aman dipindah ke file pre-app.js. **Tidak ikut dipindah** (pola sama B12-B16, rencana masuk bagian charts/section E nanti): `svgROC`, `svgROCCompare` (rendering SVG) dan `toggleROCCompare`, `runROC` (UI wiring, depends `aState`/`addOutput`) — tetap di `app.js`. `index.html` diupdate: `stats-roc.js` (baru) dimuat setelah `stats-imputation.js`, sebelum `app.js`. |
| 2026-09-15 | B18 | `js/stats-engine/stats-survival.js` (baru) | Dipindah sbg fungsi global: `computeKaplanMeier` (Greenwood SE, 95% CI, median survival, Log-Rank Mantel-Haenszel multi-grup) dan `computeCoxRegression` (Newton-Raphson partial likelihood, HR + 95% CI, Harrell's C, LR test). Dependency: `isMiss` (global by-name) dan `SE.*` (`isV`, `mean`, `std`, `matInv`, `normInv`, `normCDF`, `f4`, `pFmt`, `chi2CDF`) lewat `SE.` — semua hanya diakses di dalam function body (runtime), aman dipindah. **Temuan penting — TIDAK ikut dipindah**: blok `if(!SE.chi2CDF){...}` beserta helper privat `lnGammaSimple` yang dipakainya. Sama persis dengan temuan B14 (`SE.fInv`/`fCritApprox`/`chiCritApprox`) — top-level write ke `SE` yang butuh `SE` sudah terbentuk (baru ada setelah app.js mulai jalan), jadi kalau dipindah ke file pre-app.js akan `ReferenceError: SE is not defined`. Keduanya dibiarkan tetap di app.js, posisi sama persis (`chi2CDF` ternyata **tidak pernah ada** di `return {...}` object SE utama — cuma di-polyfill lewat blok ini, beda dari `fCDF`/`chiCDF` yang sudah ada dari awal). **Temuan lain**: `svgConvergence` (baris di antara Cox regression dan polyfill chi2CDF) ternyata bukan bagian Survival Analysis — isinya me-render `pooled[i].convergence` yaitu struktur data dari `computeMICE` (B16, Multiple Imputation), sepertinya cuma nempel lokasinya di app.js karena urutan penulisan kode lama, bukan salah taruh oleh sesi split ini. Tidak dipindahkan (tetap SVG, tetap di app.js), cuma dicatat lokasinya sedikit membingungkan untuk sesi berikutnya. **Tidak ikut dipindah** (pola sama B12-B17): `svgKaplanMeier`, `svgForestPlot` (rendering SVG — nama beda dari `svgMetaForestPlot` milik B12, tidak ada konflik) dan `toggleSurvCov`, `runSurvival`, `runCoxRegression` (UI wiring) — tetap di `app.js`. `index.html` diupdate: `stats-survival.js` (baru) dimuat setelah `stats-roc.js`, sebelum `app.js`. |
| 2026-09-15 | B19 | `js/stats-engine/stats-mediation-sem.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B18: `computeSEM` (measurement model lewat `SE.cfa` per konstruk, structural path OLS terstandardisasi + indirect effect, fit indices CFI/TLI/RMSEA(+CI)/SRMR/chi-square & label overall fit) dan `generateLavaanSyntax` (generator syntax R lavaan: measurement `=~` + structural `~`). Ikut dipindah juga 3 helper **top-level** yang dipakai `computeSEM`: `logDet`, `traceRinvImpl`, `pChiSquare` — dicek dengan grep, ketiganya **hanya** dipanggil dari dalam `computeSEM` (masing-masing cuma 1 titik deklarasi di app.js, tidak ada duplikat global lain), jadi aman ikut pindah. **Catatan `pChiSquare`**: nama ini sama dengan helper lokal di dalam `computeMetaAnalysis` (B12, `stats-metaanalysis.js`), tapi yang di B12 adalah closure lokal di dalam fungsinya sendiri — bukan global — jadi **tidak ada tabrakan scope**, beda kasus dengan `chi2CDF` di B9 yang perlu di-rename. Tidak ada identifier yang di-rename di B19, murni potong-tempel. Dependency (`data` global, `SE.cfa`, `SE.f4`, `normCDF` dari `stats-distributions.js`) semua diakses di dalam body fungsi (runtime), aman di file pre-`app.js` — pola sama B13/B15. Helper `mean`, `std`, `corr` di dalam `computeSEM` tetap closure lokal (tidak dibuat global, menghindari tabrakan dengan `SE.mean`/`SE.std`). **Tidak ikut dipindah** (pola sama B12–B18): `svgSEMDiagram` (rendering SVG path diagram → rencana `js/charts/sem-mediation-diagram.js`, bagian E7) dan semua UI wiring (`semAddLatent`, `semRemoveLatent`, `semRenameLatent`, `semToggleIndicator`, `semAddPath`, `semRemovePath`, `runSEM`) — tetap di `app.js`. Tidak ada perubahan `index.html` (append ke file yang sudah dimuat sebelum `app.js` sejak B13). |
| 2026-09-15 | B20 | `js/stats-engine/stats-bayesian.js` (baru) | Dipindah: `SE.bayesTTest` (Bayesian Independent T-Test, Cauchy prior pada effect size, Rouder et al. 2009), `SE.bayesPearson` (Bayesian Correlation, JZS prior, Ly et al. 2016 approximation), `SE.bayesPosteriorNormal` (Bayesian Posterior, Normal-Inverse-Gamma conjugate). **Beda pola dari B1-B19**: seluruh blok ini adalah 1 IIFE top-level yang langsung menulis `SE.bayesTTest = function(){...}` dkk **saat file di-parse** (bukan cuma fungsi global yang membaca `SE.*` di dalam body saat dipanggil user) — persis kasus yang sama dengan polyfill `SE.fInv`/`SE.chi2CDF` di temuan B14/B18, yang butuh `SE` sudah terbentuk lebih dulu. Karena itu file **tidak** dimuat sebelum `app.js` seperti file stats-engine lain, melainkan **setelah** `app.js` (pola sama `js/ui-misc/badges.js` di A7). Dicek dengan grep: semua 3 titik pemanggilan `SE.bayesTTest`/`SE.bayesPearson`/`SE.bayesPosteriorNormal` di app.js (preview UI & `runBayes*`) ada di dalam function body (runtime, dipicu user), bukan top-level, jadi aman file-nya dimuat setelah app.js. Helper privat closure lokal di dalam IIFE (`_lnG`, `_req`, dst — mirror internal SE) tetap ikut pindah bersama IIFE-nya, tidak diubah. **Tidak ikut dipindah** (pola sama B12-B19): `svgBayesPosterior` (rendering SVG prior-vs-posterior → rencana bagian charts/section E nanti) dan UI wiring `runBayesTTest`/`runBayesCorr`/`runBayesPosterior` (nama pasti dicek ulang saat bagian I/UI-misc dikerjakan) — tetap di `app.js`. `index.html` diupdate: `stats-bayesian.js` ditambahkan SETELAH tag `<script src="app.js">`, SEBELUM `js/ui-misc/badges.js`. |
| 2026-09-15 | B21 | `js/stats-engine/stats-discriminant-cluster.js` (baru) | Dipindah sbg fungsi global: `computeLDA` (Linear Discriminant Analysis — canonical discriminant functions via eigen-decomposition, standardized coefficients, structure matrix, Wilks' Lambda + p-value, klasifikasi nearest centroid + confusion table, discriminant scores per kasus). Dependency: `SE.chi2CDF` diakses lewat `SE.` di dalam function body (runtime), aman dipindah ke file pre-`app.js`, pola sama B13/B15/B19/B21 (bukan pola B20 yang menulis top-level ke `SE`). **Temuan (belum diperbaiki, cuma dicatat)**: baris `wilksP=pFmt(1-SE.chi2CDF?SE.chi2CDF(chiSq,dfW_chi):NaN)` kemungkinan bug operator-precedence — `1-SE.chi2CDF` (function dikurangi angka) selalu `NaN`, jadi kondisi ternary selalu falsy dan `wilksP` kemungkinan **selalu** `pFmt(NaN)='—'`, tidak pernah menghitung p-value Wilks' Lambda yang sebenarnya. Sama kasusnya dengan temuan `matMul` (B1) & `saveSession` (H1/H2) — dibiarkan apa adanya sampai ada konfirmasi user, tidak diubah saat potong-tempel. **Tidak ikut dipindah** (pola sama B12-B19): `svgDiscriminantPlot` (rendering SVG scatter 2 discriminant function pertama → rencana `js/charts/`, belum ada nomor row di tabel E) dan `runDiscriminant` (UI wiring, depends `aState`/`addOutput`/`tryStats`/`showToast`) — tetap di `app.js`. `index.html` diupdate: `stats-discriminant-cluster.js` (baru) dimuat setelah `stats-survival.js`, sebelum `app.js`. File ini rencananya juga jadi tujuan B22 (Cluster Analysis) sesuai peta Bagian 2 — belum dikerjakan, baru B21 yang masuk. |
| 2026-09-15 | B22 | `js/stats-engine/stats-discriminant-cluster.js` (ditambah) | Dipindah sbg fungsi global, pola sama B1-B21: `computeKMeans` (Lloyd's algorithm dengan multiple random init, WSS/BSS, silhouette, ANOVA per variabel, data elbow chart) dan `computeHierarchical` (Agglomerative Hierarchical Clustering — single/complete/average/ward linkage, dendrogram merge history, cut di k cluster, silhouette, ANOVA). Ditambahkan (append) ke `stats-discriminant-cluster.js` yang sudah ada dari B21 (`computeLDA`), sesuai rencana peta Bagian 2 (file target sama). **Kedua fungsi murni self-contained** — dicek dengan grep, tidak ada dependency ke `SE.*`/`data`/`vars`/`aState`/DOM sama sekali (semua helper `f4`/`isV` closure lokal), jadi dipindah apa adanya tanpa perlu cek urutan muat sama sekali. **Tidak ikut dipindah** (pola sama B12-B21): `svgClusterPlot`, `svgElbow`, `svgDendrogram` (rendering SVG → rencana `js/charts/`, belum ada nomor row di tabel E) dan `runCluster` (UI wiring, depends `aState`/`data`/`vars`/`addOutput`) — tetap di `app.js`. Tidak ada perubahan `index.html` (append ke file yang sudah dimuat sebelum `app.js` sejak B21). |
| 2026-09-15 | B23, C3 | `js/data/weight-cases.js` (baru) | **Ternyata B23 dan C3 adalah baris duplikat yang menunjuk ke kode yang SAMA PERSIS** — dicek di app.js hanya ada 1 pasang fungsi `getNEff()`/`getWeightedRows()` (bukan 2 pasang terpisah di 2 lokasi baseline berbeda seperti tersirat di tabel), jadi dipisah 1x saja dan kedua row ditandai selesai. Dipindah sbg fungsi global: `getNEff` (hitung N efektif ΣW dari variabel bobot aktif tanpa mengubah data fisik) dan `getWeightedRows` (hasilkan array virtual berbobot untuk perhitungan statistik). Dependency `aState.wcActive`/`aState.wcVar`/`data` semua dibaca di dalam function body (runtime), aman dipindah ke file pre-`app.js`, pola sama B13/B15/B19/B21. **Temuan penting (belum diperbaiki, cuma dicatat)**: `getWeightedRows()` dicek dengan grep di SELURUH app.js — ternyata **tidak dipanggil di mana pun** kecuali disebut di komentar dokumentasi. Fitur Weight Cases yang aktif sekarang cuma memakai `getNEff()` untuk badge "N efektif"; tidak ada satupun fungsi analisis statistik (t-test, ANOVA, regresi, dll) yang benar-benar memanggil `getWeightedRows()` untuk menerapkan weighting ke perhitungannya. Kemungkinan fitur "Weight Cases" baru menghitung N efektif secara kosmetik, belum benar-benar mempengaruhi hasil analisis manapun — sepertinya belum selesai diwire ke stats engine. Mirip kasus `holmBonferroni` dead code di B7, dibiarkan apa adanya sampai ada konfirmasi user. **Tidak ikut dipindah** (UI wiring, depends `document.getElementById`/`updateBadges`/`showToast`/`renderASub`): `runWeightCases`, `clearWeightCases` — tetap di `app.js`. `index.html` diupdate: `weight-cases.js` (baru, kategori "3. Data layer") dimuat setelah `stats-discriminant-cluster.js`, sebelum `app.js`. |
| 2026-09-15 | B24 (sebagian) | `js/stats-engine/_B24_append_to_stats-core-advanced.js` (fragmen, untuk digabung ke `stats-core-advanced.js` yang tidak ikut diupload sesi ini) | Dipindah sbg fungsi global, pola sama B1-B23: `logisticReg` (IRLS binary + multinomial logistic regression, koefisien standar & odds ratio, Cox & Snell / Nagelkerke R², LR chi-square test, classification accuracy). **Hanya bagian computation** dari row B24 di tabel — bagian "Hierarchical Regression toggle helper" (`toggleHrBlock`) SENGAJA TIDAK ikut dipindah, lihat catatan ⚠️ di atas tabel B untuk alasan lengkap (konsisten dengan keputusan B19 soal UI-state helper). Dependency `f4`/`f1`/`pFmt` dibaca lewat scope-fallback ke versi global (bukan `SE.` eksplisit) di dalam function body, aman dipindah ke file pre-`app.js`. **Temuan**: `function f1` lokal yang sebelumnya nempel di scope yang sama dengan `logisticReg` (duplikat isi sama dari temuan B1) sekarang jadi benar-benar dead code karena satu-satunya pemakainya sudah pindah — dibiarkan apa adanya, tunggu konfirmasi user. Return list `SE.logisticReg` di `app.js` (baris `return{...logisticReg, poissonReg, negbinReg...}`) TIDAK diubah — tetap jalan lewat JS scope-fallback ke fungsi global baru, pola sama semua fungsi B1-B23 lain di return list yang sama. `index.html` TIDAK diubah — menunggu file dasar `stats-core-advanced.js` diupload user untuk digabung (sama seperti kasus B19). |
| 2026-09-18 | C1 | `js/data/dataset-manager.js` (baru) | Dipindah sbg var/fungsi global, pola sama B13/B15/B19/B21/B23: `var datasets`/`activeDatasetId` (state array dataset), `var data`/`vars`/`outputs` (working globals yang selalu nunjuk ke dataset aktif — dipindah apa adanya, TIDAK di-refactor jadi getter/setter), `getActiveDs`, `_dsSyncSave`, `_dsSyncLoad`, `switchDataset`, `addDataset`, `deleteDataset`, `renameDataset`, `showAddDatasetModal`, `renderDsSidebar`. Dicek grep di seluruh `app.js`: tidak ada duplikat deklarasi (`var datasets`/`var data`/`var vars`/`var outputs` masing-masing cuma 1 titik), dan tidak ada pemanggilan top-level (parse-time) terhadap fungsi manapun di blok ini — semua pemanggilan (`renderDsSidebar()`, `switchDataset(...)`, dst) ada di dalam function body lain di `app.js`, dipicu interaksi user, jadi aman lewat scope-fallback ke global baru tanpa ubah call-site. Dependency keluar (`ossDialog`, `showToast`, `switchTab`, `updateBadges`, `escDlg`, `escHtml`, `escHtmlAttr`) semua diakses di dalam function body (runtime), bukan ditulis top-level ke objek lain — aman dipindah ke file pre-`app.js`, bukan pola B20 (yang butuh dimuat setelah `app.js`). **Diverifikasi via Node `vm` module** (di luar browser, dengan stub `document.getElementById`/`ossDialog`/`showToast`/`switchTab`/`updateBadges`): `addDataset` menambah dataset baru & mengosongkan `data`/`vars`/`outputs` global sambil menyimpan state dataset lama dengan benar; `switchDataset` mengembalikan isi dataset yang benar; `deleteDataset` menolak menghapus dataset terakhir (guard `datasets.length<=1` jalan, `showToast` error terpanggil) dan berhasil menghapus dataset non-aktif; `renameDataset` mengubah `ds.name`; `renderDsSidebar()` jalan tanpa exception. Semua skenario lulus tanpa error. `index.html` diupdate: `dataset-manager.js` (baru, kategori "3. Data layer") dimuat sebelum `weight-cases.js`, sebelum `app.js` — karena `weight-cases.js` (`getNEff`/`getWeightedRows`) membaca `data` global di dalam function body (runtime, bukan top-level), urutan relatif terhadap `dataset-manager.js` sebenarnya tidak kritikal, tapi ditaruh lebih dulu supaya sesuai urutan logis (deklarasi state dataset sebelum konsumennya). `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.603 → 12.479 baris). |
| 2026-09-19 | (bug fix, bukan split) | `app.js` (dibersihkan) | **Deklarasi ganda `numFields`/`allFields`** (dicatat pas C1, sengaja dibiarkan waktu itu) diperbaiki. Ada 2 deklarasi top-level: versi lama `function numFields(){...}`/`function allFields(){...}` (baris lama ~76-77, tanpa exclude `id`) dan versi baru `var numFields=()=>...`/`var allFields=()=>...` (baris lama ~178-179, exclude field bernama `id` dari `numFields`). Dicek grep: pemanggilan pertama kedua fungsi ini (baris ~2165) baru terjadi jauh setelah baris 178-179 — jadi versi lama **selalu tertimpa** (var assignment jalan sebelum ada pemanggilan apa pun) dan 100% dead code, sama sekali tidak pernah aktif dipakai. Versi lama (2 baris + komentar header "Field helper functions") dihapus. **Tidak ada perubahan perilaku** — versi yang aktif dipakai sekarang persis sama seperti sebelum fix (tetap exclude `id` dari `numFields`). Diverifikasi: `node --check` lolos, dan test isolasi (Node) membuktikan output `numFields()`/`allFields()` versi arrow function identik sebelum & sesudah baris lama dihapus. |
| 2026-09-20 | C2 | `js/analyze/analyze-subtab-ui.js` (baru) | Dipindah sbg var/fungsi global, pola sama C1: `var aState` (object literal murni, 140 keys, state semua sub-tab Analyze — descriptive s/d weight cases, TIDAK baca `vars`/`data` sama sekali jadi tidak ada isu urutan muat) dan 4 field helper (`numFields`, `allFields`, `isMiss`, `missCount`) yang baca `vars`/`data` di dalam function body (runtime, bukan top-level) — aman dipindah ke file pre-`app.js`. Dicek grep di seluruh `app.js`: tidak ada deklarasi ganda untuk `aState`/`numFields`/`allFields`/`isMiss`/`missCount` (masing-masing cuma 1 titik), dan semua pemanggilan/pembacaan ada di dalam function body lain, dipicu render/interaksi user, jadi aman lewat scope-fallback ke global baru tanpa ubah call-site. **Diverifikasi via Node `vm` module** (di luar browser, urutan muat persis `analyze-subtab-ui.js` → `app.js` seperti `index.html`, dengan stub minimal untuk fungsi stats-engine dari file lain yang tidak ikut diupload sesi ini): `app.js` lolos load tanpa error setelah split; `aState` object ke-load dengan benar (`typeof aState==='object'`, 140 keys, nilai default seperti `dFld:''`/`pwN:30` sesuai baseline); `numFields()`/`allFields()`/`isMiss()`/`missCount()` diuji dengan `vars`/`data` sintetis (3 variabel, 3 baris, ada missing value) dan mengembalikan hasil yang benar (`numFields()` exclude `id`, `isMiss(null)===true`/`isMiss(0)===false`, `missCount()` menghitung jumlah missing per variabel dengan tepat). Ekstraksi juga dicek byte-exact (isi file baru = potongan app.js lama apa adanya, tanpa modifikasi). `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.475 → 12.382 baris). `index.html` diupdate: `analyze-subtab-ui.js` (baru, kategori "4. Analyze layer") dimuat setelah `weight-cases.js`, sebelum `app.js`; kategori-kategori setelahnya (Sisa monolith, post-app.js stats-engine, UI misc, Router) diberi nomor ulang (4→5, 4b→5b, 5→6, 6→7) supaya urutan tetap konsisten. |
| 2026-09-20 | C4 | `js/core/html-helpers.js` (baru) | Dipindah sbg fungsi global, pola sama C1/C2: `sigBadge`, `stCard`, `mkTable`, `selOpts` — 4 fungsi rendering HTML kecil yang dipakai lintas fitur (Output View, Data View, dll). `stCard`/`mkTable` memanggil `escHtml` yang masih tetap di `app.js` (function declaration, hanya dipanggil di runtime saat render, bukan saat parse file baru ini) — aman dipindah ke file pre-`app.js`, pola sama dependency lintas-file lain (mis. `dataset-manager.js` memanggil `ossDialog`/`showToast` yang didefinisikan di `app.js`). Dicek grep: tidak ada deklarasi ganda `sigBadge`/`stCard`/`mkTable`/`selOpts` di `app.js`. **Diputuskan taruh di file baru `js/core/html-helpers.js`** ("util bersama") alih-alih digabung ke `js/core/state.js` (opsi kedua di tabel Bagian 3), karena isinya murni fungsi render HTML tanpa state — konsisten dengan pemisahan `analyze-subtab-ui.js` (state) vs helper murni lain di C1-C3. **Diverifikasi via Node `vm` module**, urutan muat `html-helpers.js` → `analyze-subtab-ui.js` → `app.js` persis `index.html`: semua 3 file lolos load tanpa error; `sigBadge(0.03)` menghasilkan badge "p = 0.03 *" (kuning), `sigBadge(0.5)` menghasilkan "ns" (abu-abu), `sigBadge('n/a')` menghasilkan string kosong (non-finite guard jalan); `selOpts`/`stCard`/`mkTable` menghasilkan HTML yang benar, termasuk `stCard`/`mkTable` yang berhasil memanggil `escHtml` dari `app.js` tanpa error (membuktikan cross-file dependency-nya aman). Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.382 → 12.365 baris). `index.html` diupdate: `html-helpers.js` (baru) dimuat di kategori "1. Core & state", setelah `watercolor-bg.js`, sebelum semua stats-engine & sebelum `app.js`. |
| 2026-09-20 | C5 | `js/ui-misc/custom-select.js` (baru) | Dipindah sbg fungsi/var global, pola sama C1/C2/C4: `var _cR`/`var _cActive` (registry & active picker state), `varBdg`, `mkCsel`, `mkSelect`, `openCselById`, `_buildCselList`, `filterCsel`, `closeCsel`, `openOptCselById`, `mkOptCsel`, plus listener `document.addEventListener('keydown', e=>{if(e.key==='Escape'){closeCsel();closeCmd();}})` di baris terakhir blok. `varBdg` baca `vars` (dari `dataset-manager.js`, C1) di dalam function body (runtime); `mkCsel`/`mkOptCsel` memanggil `escHtml` (masih di `app.js`, pola sama C4). **Beda dari file pre-app.js lain**: blok ini punya 1 baris **top-level call** (`document.addEventListener(...)`) yang langsung eksekusi saat file di-parse — dicek aman karena baris ini cuma MEMASANG listener (bukan memanggil `closeCsel`/`closeCmd` langsung); kedua fungsi itu baru betul-betul dipanggil di dalam callback saat user menekan Escape, jauh setelah semua script (termasuk `app.js`, tempat `closeCmd` didefinisikan) selesai dimuat — scope-fallback ke global aman, sama prinsipnya dengan dependency runtime di C1/C4, bukan kasus B20/badges.js (yang top-level-nya nulis balik ke `SE`/butuh `SE` sudah terbentuk). Dicek grep: tidak ada deklarasi ganda untuk 11 identifier di atas. **Diverifikasi via Node `vm` module**, urutan muat `html-helpers.js` → `custom-select.js` → `analyze-subtab-ui.js` → `app.js` persis `index.html`: semua 4 file lolos load tanpa error; `varBdg('gender')`/`varBdg('age')` menghasilkan badge kategori yang benar (cat/num, warna sesuai measure); `mkCsel(...)` menghasilkan HTML trigger yang benar dan mendaftarkan entry ke `_cR` dengan isi tepat; `openCselById(...)` jalan tanpa exception; listener `keydown` terkonfirmasi terpasang (1 listener terdaftar di `document`), dan memanggilnya dengan `{key:'Escape'}` berhasil tanpa exception — membuktikan `closeCsel`/`closeCmd` (dari `app.js`) resolve dengan benar lewat scope-fallback. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.365 → 12.248 baris). `index.html` diupdate: `custom-select.js` (baru) dimuat di kategori "1. Core & state" (BUKAN kategori "6. UI misc" tempat `badges.js` berada — tidak ada dependency top-level ke `SE`/`app.js` yang mengharuskan dimuat belakangan), setelah `html-helpers.js`, sebelum semua stats-engine & sebelum `app.js`. |
| 2026-09-20 | C6 | `js/data/data-view.js` (baru) | Dipindah sbg var/fungsi global, pola sama C1/C2/C4/C5: `var sortCol`/`sortDir`/`searchQ`/`selRows`(`new Set()`)/`sprMode`/`_ossDataPageThreshold`/`_ossDataPageSize`/`_ossDataShown` + `function renderData(el)`. Blok ini persis 137 baris di app.js snapshot sesi ini (baris 486–622, header komentar "DATA VIEW" s/d penutup `renderData`), pas sama dengan rentang baseline C6 (4131–4267 = 137 baris) — tidak ada percampuran dengan kode C7 (Spreadsheet Mode) yang dimulai tepat setelahnya. `renderData` membaca `missCount`, `aState`, `getNEff`, `IC`, `escHtml`, `escHtmlAttr`, `vars`, `data`, dan `editMode` di dalam function body (runtime, dipanggil saat tab Data dirender) — semua masih di `app.js`/file lain, aman lewat scope-fallback; string `onclick="..."` di dalam HTML yang di-generate (`addDataRowInline`, `searchData`, `toggleSort`, `toggleAllRows`, `toggleRow`, `deleteSelected`, `toggleEditMode`, `enterCombinedEditMode`, `saveSprMode`, `exitSprMode`, `_ossLoadMoreRows`, dll — semua masih di app.js, bagian C7 yang belum dipisah) juga aman karena baru resolve saat user klik, jauh setelah semua script dimuat. **Temuan dicatat (bukan dikerjakan sesi ini)**: fungsi-fungsi konseptual "Data View" lain (`searchData`, `toggleSort`, `toggleRow`, `toggleAllRows`, `syncHeaderCheckbox`, `deleteSelected`, `toggleEditMode`, `enterCombinedEditMode`, `addDataRowInline`, `_ossLoadMoreRows`, `handleCSV`, `addDataRow`/`editRowModal`/`confirmAddRow`/`confirmEditRow`, var `editMode`) ternyata secara baseline line-range masuk C7 (4268–4898), bercampur fisik dengan kode Spreadsheet Mode asli — lihat catatan detail di Bagian 5. Dicek grep: tidak ada deklarasi ganda untuk 8 identifier di atas. **Diverifikasi via Node `vm` module** (di luar browser, stub minimal `document`/`IC`/`escHtml`/`escHtmlAttr`/`missCount`/`isMiss`/`aState`/`getNEff`/`vars`/`data`/`editMode` — file-file pre-app.js lain seperti `dataset-manager.js`/`analyze-subtab-ui.js` tidak ikut diupload sesi ini jadi tidak bisa dites gabungan penuh, sama kasusnya dengan C2/C4/C5): `renderData(el)` jalan tanpa exception untuk dataset sintetis (3 baris, 1 nilai missing), menghasilkan HTML tabel dengan header kolom (dot warna sesuai measure Scale/Nominal), baris data (cell missing tampil `—` lewat class `td-miss`), dan footer info "3/3 rows" yang benar; `sortCol`/`sortDir`/`searchQ`/`selRows`/`sprMode`/`_ossDataPageThreshold`/`_ossDataPageSize`/`_ossDataShown` semua ter-load dengan nilai default sesuai baseline. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.248 → 12.118 baris). `index.html` diupdate: `data-view.js` (baru) dimuat di kategori "3. Data layer", setelah `weight-cases.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. |
| 2026-09-20 | C7 | `js/data/spreadsheet-mode.js` (baru) | Dipindah sbg var/fungsi global apa adanya (byte-exact), pola sama C1-C6, blok 630 baris (baris 493–1122 di app.js snapshot sesi ini) — pas dengan rentang baseline C7 (4268–4898). **Konfirmasi temuan yang dicatat pas C6**: blok ini memang campuran, bukan Spreadsheet Mode murni: (1) Spreadsheet Mode asli — `sprBuffer`, `enterSprMode`/`exitSprMode`/`saveSprMode`, `sprCellChange`/`sprCellInput`/`sprKeyNav`, `colFillModal`/`cfSetMode`/`applyColFill`/`applyColSeq`, `addMultipleRows`; (2) sisa fungsi konseptual Data View (C6) yang baseline line range-nya ternyata masuk C7 — `searchData`, `toggleSort`, `toggleRow`, `toggleAllRows`, `syncHeaderCheckbox`, `deleteSelected`, `toggleEditMode`, `enterCombinedEditMode`, `addDataRowInline`, `_ossLoadMoreRows`, `handleCSV`, `addDataRow`/`editRowModal`/`confirmAddRow`/`confirmEditRow`, `startEdit`, var `editMode`; (3) `escHtmlAttr` — utility escape HTML attribute yang dipakai lintas fitur (bukan spesifik Spreadsheet), tapi lokasinya di awal blok ini di baseline; (4) `toggleCoefView` — TIDAK berhubungan sama sekali dengan Data View/Spreadsheet (toggle tampilan OLS vs HC3 robust SE di hasil regresi), kemungkinan salah taruh di monolith sejak lama, dipindah byte-exact apa adanya tanpa direlokasi (keputusan taruh di file mana ditunda, cukup dicatat). Semua dependency (`vars`/`data`/`aState`/`showToast`/`updateBadges`/`switchTab`/`renderTab`/`isMiss`/`getActiveDs`/`addDataset`/`datasets`/`selRows`/`sortCol`/`sortDir`/`searchQ`/`_ossDataShown`/`_ossDataPageSize` — sebagian dari `data-view.js` C6, sebagian masih di `app.js`) dibaca di dalam function body (runtime), tidak ada top-level call di blok ini. Dicek grep: tidak ada deklarasi ganda untuk 30 identifier di blok ini. **Diverifikasi via Node `vm` module** (di luar browser, urutan muat `data-view.js` → `spreadsheet-mode.js`, stub `document`/`IC`/`escHtml`/`isMiss`/`missCount`/`aState`/`getNEff`/`showToast`/`updateBadges`/`switchTab`/`renderTab`/`getActiveDs`/`addDataset`/`datasets`/`vars`/`data`): kedua file lolos load tanpa error; `escHtmlAttr` meng-escape `&"'<>` dengan benar; `renderData` jalan di mode normal maupun sprMode (menghasilkan cell `<input class="spr-cell">` saat sprMode aktif); `enterCombinedEditMode` men-set `editMode`/`sprMode` jadi `true`; `toggleSort`/`searchData` mengubah `sortCol`/`sortDir`/`searchQ` dengan benar; `toggleRow`/`toggleAllRows` mengubah `selRows` (Set) dengan benar; `deleteSelected` menghapus row yang dipilih dari `data`; `addDataRowInline` menambah row baru dengan `id` auto-increment; `sprKeyNav`/`toggleCoefView` jalan tanpa exception dengan stub minimal. `colFillModal` (modal DOM-heavy, `document.createElement`/`querySelector` beruntun) tidak bisa ditest penuh dengan stub headless sederhana (butuh DOM asli seperti jsdom/browser) — konsisten dengan fungsi-fungsi modal serupa di split sebelumnya, cukup dicek `node --check` + tidak ada deklarasi ganda; **tetap perlu dites manual di browser sebelum dianggap 100% aman**, sesuai aturan workflow. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.118 → 11.497 baris). `index.html` diupdate: `spreadsheet-mode.js` (baru) dimuat di kategori "3. Data layer", setelah `data-view.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. |
| 2026-09-20 | C8 | `js/data/variable-view.js` (baru) | Dipindah sbg var/fungsi global apa adanya (byte-exact), pola sama C1-C7, blok 551 baris (baris 503–1053 di app.js snapshot sesi ini) — mendekati rentang baseline C8 (4899–5692 = 794 baris; selisih karena `renderAnalyze` yang menyusul di baseline TIDAK ikut dipindah, lihat temuan di bawah). Isi: `var _varTableEditMode`/`_varChecked` + `renderVars`, `addVarInline`, `_varInlineChange`, `_varCheckAll`, `_varUpdateHeaderChk`, `_varToggleCheck`, `_varSyncDeleteBtn`, `_varPickerOpen` (in-web picker TYPE/MEASURE/ROLE), `_varDeleteSelected`, `_varSaveEditMode`, `editVarField`, `cycleVarType`, `cycleMeasure`, `cycleRole`, `moveVar`, `deleteVar`, `clearAllData`, `addVarModal`, `confirmAddVar`. **Temuan baru**: header komentar baseline "VARIABLE VIEW" ternyata membungkus fisik lebih dari isi C8 — begitu `confirmAddVar` selesai, fungsi berikutnya adalah `renderAnalyze(el)` (wrapper render tab Analyze: sidebar grup + sub-tab, dipanggil sebelum `renderASub()`/D1 switch raksasa) yang **TIDAK terdaftar di roadmap manapun** (bukan C8, bukan C9 sliding-pill, bukan D1). **Sengaja TIDAK ikut dipindah** di sesi ini — dibiarkan di `app.js`, ditandai sebagai temuan yang perlu didiskusikan sebelum/saat D1 dikerjakan (kandidat: digabung ke `js/analyze/analyze-form-render.js` atau file baru `analyze-tab-shell.js`). Dicek grep: tidak ada deklarasi ganda untuk 20 identifier di file baru ini. **Diverifikasi via Node `vm` module** (di luar browser, stub `document` termasuk `document.head`/`document.body`, `IC`/`escHtml`/`isMiss`/`missCount`/`aState`/`showToast`/`updateBadges`/`switchTab`/`renderTab`/`ossDialog`(stub memanggil `onOk`)/`vars`/`data`): `renderVars(el)` jalan tanpa exception menghasilkan HTML tabel; `cycleVarType`/`cycleMeasure`/`cycleRole` mengubah properti var yang benar (Numeric→String, Scale→Ordinal, Input→Target); `moveVar(1,-1)` menukar urutan array `vars` dengan benar; `deleteVar(2)` (lewat `ossDialog` konfirmasi) menghapus var & field terkait dari semua row `data`; `addVarInline` menambah var baru dengan nama auto (`var1`); `_varCheckAll`/`_varToggleCheck` mengubah `_varChecked` (Set) dengan benar; `clearAllData` (lewat `ossDialog`) mengosongkan `data`. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (11.497 → 10.956 baris). `index.html` diupdate: `variable-view.js` (baru) dimuat di kategori "3. Data layer", setelah `spreadsheet-mode.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. |
| 2026-09-20 | C10 | `js/data/data-transform.js` (baru) | Dipindah sbg fungsi global apa adanya (byte-exact), pola sama C1-C9, blok 143 baris (baris 6406–6548 di app.js snapshot sesi ini, mulai komentar "// Transform & Compute" s/d akhir `removeOutlierFilter`) — jauh lebih panjang dari estimasi baseline (6493–6557 = 65 baris), sama seperti beberapa rentang baseline lain yang sudah ditemukan basi di split sebelumnya (C6/C9), tapi isi & urutan tetap dipindah byte-exact apa adanya. Isi: `runTransform` (z-score/log/sqrt/dll), `runCompute`, `runRecodeRange`, `runRecodeBinary`, `runRecodeExact`, `safeFilter`, `applyFilter`, `var _origData` (backup data sebelum filter diterapkan), `clearFilter`, `completeCases`, `removeOutlierFilter`. **Temuan baru**: tepat setelah blok ini di baseline, ada 2 kelompok fungsi lain yang juga TIDAK terdaftar di roadmap manapun — (1) `runWeightCases`/`clearWeightCases` (di bawah komentar "── WEIGHT CASES ──") — UI action utk fitur Weight Cases, BEDA dari C3 (Weight *helpers* seperti `getWeightedRows`, sudah selesai di `weight-cases.js`); kandidat target digabung ke `weight-cases.js` yang sama, tapi belum dikonfirmasi user; (2) `runImpute`/`imputeAllVars` (di bawah komentar "// Imputation") — UI action utk fitur Imputation, tidak ada file target di roadmap sama sekali; kandidat file baru `imputation.js` atau digabung ke `data-transform.js` ini. **Keduanya sengaja TIDAK ikut dipindah** di sesi ini, tetap di `app.js`. Dicek grep: tidak ada deklarasi ganda untuk 11 identifier di blok C10 (termasuk `var _origData`, dideklarasikan di dalam blok ini sendiri, bukan di tempat lain). **Diverifikasi via Node `vm` module** (di luar browser, stub `aState`/`vars`/`data`/`isMiss`/`numFields`/`showToast`/`updateBadges`/`renderASub`/`SE`(`validNums`/`mean`/`std`/`isV`/`quantile`)): `runTransform` (z-score) menghasilkan nilai standar yang benar; `runCompute` mengevaluasi ekspresi (`age+1`) dengan benar; `safeFilter` mengevaluasi kondisi (`age>20`) dengan benar per-baris; `applyFilter` memfilter `data` & menyimpan backup ke `_origData`; `clearFilter` mengembalikan `data` dari backup; `completeCases` membuang baris dengan missing value; `removeOutlierFilter` jalan tanpa exception (metode IQR). Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (10.930 → 10.796 baris). `index.html` diupdate: `data-transform.js` (baru) dimuat di kategori "3. Data layer", setelah `variable-view.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. |
| 2026-09-20 | C10★ (temuan) | `js/data/imputation.js` (baru) | Tindak lanjut temuan yang dicatat pas C10: `runImpute`/`imputeAllVars` (UI action Imputation) tidak terdaftar di roadmap manapun. User memutuskan dibuat **file baru** (bukan digabung ke `data-transform.js`). Dipindah byte-exact, blok 15 baris (baris 6454–6468 di app.js snapshot sebelum dipotong). Isi: `runImpute` (impute 1 variabel terpilih dengan metode mean/median/mode/zero/min/max) + `imputeAllVars` (impute semua variabel numerik sekaligus, metode mean). Dicek grep: tidak ada deklarasi ganda `runImpute`/`imputeAllVars`. **Diverifikasi via Node `vm` module** (stub `aState`/`data`/`isMiss`/`numFields`/`showToast`/`updateBadges`/`renderASub`/`SE`(`validNums`/`mean`/`quantile`)): `runImpute` (metode mean) mengganti nilai missing dengan mean yang benar (mean dari 20,30=25, kedua baris missing terisi 25); `imputeAllVars` menghasilkan hasil yang sama untuk kasus satu-variabel-numerik. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (10.797 → 10.786 baris). `index.html` diupdate: `imputation.js` (baru) dimuat di kategori "3. Data layer", setelah `data-transform.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. |
| 2026-09-20 | C8★ (temuan) | `js/analyze/analyze-tab-shell.js` (baru) | Tindak lanjut temuan yang dicatat pas C8: `renderAnalyze(el)` (wrapper render tab Analyze — sidebar grup + daftar sub-tab, dipanggil sebelum `renderASub()`/D1) tidak terdaftar di roadmap manapun. Sempat basi juga catatannya sempat mengarah ke "belum diputuskan" — sekarang user memutuskan dibuat **file terpisah** `analyze-tab-shell.js` (bukan digabung ke `analyze-form-render.js`/D1). Dipindah byte-exact, blok 241 baris (baris 514–754 di app.js snapshot sebelum dipotong) — 1 fungsi saja: `renderAnalyze(el)`, berisi definisi `GROUP_TABS` (peta sidebar grup → sub-tab, ikon SVG, label), logika resolve grup aktif, render HTML sidebar+sub-tab, lalu panggil `positionSubTabIndicator` (C9) dan `renderASub()` (D1). **Catatan dependency**: `currentGroup` DAN `currentASub` sama-sama var global yang tidak pernah dideklarasikan dengan `var` eksplisit di app.js (cuma assignment biasa) — pola pre-existing yang sama seperti ditemukan pas C9, bukan bug baru. `SUB_TO_GROUP`/`switchASub`/`renderASub` masih di app.js (D1 belum dipisah). Dicek grep: tidak ada deklarasi ganda `renderAnalyze`. **Diverifikasi via Node `vm` module** (stub `document`/`switchASub`/`renderASub`/`positionSubTabIndicator`/`numFields`/`allFields`/`aState`): `renderAnalyze(el)` jalan tanpa exception, menghasilkan HTML berisi `.sub-tabs` dan `#a-content`, memanggil `positionSubTabIndicator` dan `renderASub` di akhir sesuai urutan aslinya. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (10.787 → 10.551 baris). **Komentar pointer C8 di `variable-view.js` dan `app.js` diupdate** — sebelumnya bilang "renderAnalyze TIDAK ikut dipindah", sekarang diperbarui supaya tidak jadi catatan basi seperti kasus `chiSquare`/B4 dulu (ditemukan user saat review kode). `index.html` diupdate: `analyze-tab-shell.js` (baru) dimuat di kategori "4. Analyze layer", setelah `analyze-subtab-ui.js`, sebelum `app.js`. |
| 2026-09-20 | C11 | `js/data/missing-data.js` (baru) | **Beda struktur dari C1-C10**: C11 bukan fungsi berdiri sendiri di baseline, melainkan 1 cabang `else if(currentASub==='missinganalysis'){...}` di tengah switch raksasa `renderASub()` (D1, belum dipisah). Ditemukan via grep marker "MISSING DATA ANALYSIS" di baris 3109–3371 app.js snapshot sesi ini (263 baris) — jauh dari estimasi baseline (8348–8615), konsisten dengan pola pergeseran baseline yang basi di split-split sebelumnya. Supaya bisa dipisah tanpa merusak alur if/else-if `renderASub()`, badan cabang **dibungkus jadi fungsi baru** `renderMissingDataAnalysis()` dengan `var html=''` lokal di awal dan `return html;` di akhir — SATU-SATUNYA perubahan struktural; isi logika (kalkulasi Little's MCAR chi-squared, pattern matrix, rekomendasi) 100% byte-exact tanpa diubah sedikit pun. Call-site di `renderASub()` (masih di app.js) diubah dari 263 baris kode inline jadi `else if(currentASub==='missinganalysis'){ html+=renderMissingDataAnalysis(); }`. **Pengecekan keamanan sebelum membungkus**: dipastikan cabang aslinya (a) tidak pernah membaca `html` (cuma `html+=`, tidak ada `html=` reset atau pembacaan isi/panjang untuk logika apa pun), dan (b) tidak memakai variabel scope `renderASub()` lain seperti `nF`/`aF`/`aState` — cuma `vars`/`data`/`escHtml` (lintas file) plus variabel lokal (`nF2`, `n2`, dll, dideklarasikan di dalam cabang itu sendiri) — dua syarat ini memastikan pembungkusan jadi fungsi terpisah dijamin tidak mengubah perilaku (bukan refactor logika, cuma ubah bentuk pembungkus). Dicek grep: tidak ada deklarasi ganda `renderMissingDataAnalysis`, dan cabang `currentASub==='missinganalysis'` cuma muncul 1x di `app.js`. **Diverifikasi via Node `vm` module** (stub `vars`/`data`/`escHtml`): `renderMissingDataAnalysis()` diuji 3 skenario — data dengan missing value di beberapa variabel (hasil: HTML berisi judul "Missing Data Analysis", "Little's MCAR Test", kartu "Total Cases" dengan nilai `n2` yang benar), dataset kosong (hasil: pesan "No numeric data loaded"), dataset tanpa missing value sama sekali (hasil: pesan "No missing data found. MCAR test not applicable."). **Dites integrasi tambahan**: simulasi persis pola call-site baru (`if(false){}else if(currentASub==='missinganalysis'){html+=renderMissingDataAnalysis();}`) di dalam 1 konteks `vm` yang sama dengan `missing-data.js` sudah dimuat — hasil `html` gabung dengan benar, mengonfirmasi wiring call-site ↔ fungsi baru bekerja end-to-end. Ekstraksi badan fungsi dicek byte-exact terhadap `app.js` lama (di luar baris wrapper `function renderMissingDataAnalysis(){var html='';` di awal dan `return html;}` di akhir, yang memang baris baru). `app.js` lolos `node --check` sebelum & sesudah perubahan (10.551 → 10.295 baris). `index.html` diupdate: `missing-data.js` (baru) dimuat di kategori "3. Data layer", setelah `imputation.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. **Section C (Data Management) SEKARANG 100% SELESAI** (C1-C11 + 2 temuan tambahan C8★ analyze-tab-shell dan C10★ imputation). Temuan `toggleCoefView` (dicatat pas C7, masih nyangkut di `spreadsheet-mode.js`, kandidat pindah ke `analyze-form-render.js`/D1) dan `runWeightCases`/`clearWeightCases` (kandidat gabung ke `weight-cases.js`) masih ditunda, belum ditindaklanjuti — user memilih prioritaskan C11 dulu di sesi ini. |
| 2026-09-20 | C9 (gabung) | `js/analyze/analyze-subtab-ui.js` (digabung, fragmen dihapus) | User upload ulang `analyze-subtab-ui.js` (hasil C2) di sesi ini, sehingga fragmen `_C9_append_to_analyze-subtab-ui.js` dari entri sebelumnya bisa langsung digabung ke akhir file aslinya — bukan dipindah baru dari `app.js` (itu sudah terjadi di entri C9 sebelumnya). Isi hasil gabung: bagian atas (`aState` + `numFields`/`allFields`/`isMiss`/`missCount`, dari C2) tetap utuh, ditambah pemisah komentar, lalu bagian C9 (`var _subTabAnim` + `positionSubTabIndicator` + listener `resize`) di akhir file — total 159 baris. Dicek: isi awal file (sampai sebelum pemisah C9) match byte-exact dengan file yang diupload user, dan bagian akhir file match byte-exact dengan kode C9 yang sudah diekstrak sebelumnya (tidak ada perubahan konten, cuma digabung). `node --check` lolos. **Diverifikasi via Node `vm` module** memuat file gabungan utuh sekaligus (bukan cuma potongan C9 sendirian seperti verifikasi sebelumnya): `aState` ter-load sebagai object dengan semua field default; `numFields`/`allFields`/`isMiss`/`missCount` jalan benar terhadap data sintetis; `positionSubTabIndicator` menempatkan indikator sesuai posisi tombol aktif; listener `resize` terpasang & jalan tanpa exception — konfirmasi tidak ada konflik antara isi C2 dan C9 dalam 1 file yang sama. `js/analyze/_C9_append_to_analyze-subtab-ui.js` (fragmen) **dihapus** dari output karena sudah tidak relevan. `app.js`/`index.html` tidak berubah di entri ini (sudah benar sejak entri C9 sebelumnya). Status C9 di tabel Bagian 3 diupdate jadi ✅ SUDAH. |
| 2026-09-17 | D1 (sebagian: 4 dari ~50 cabang) | `js/analyze/analyze-form-render.js` (baru) | **Pemisahan bertahap dimulai** untuk D1 (`renderASub()` — switch raksasa render form per `currentASub`, ~3.606 baris, fungsi ini sendiri di baris 531–4136 app.js snapshot sesi ini sebelum dipotong, 10.295 baris). Karena ukurannya (~50 cabang `else if`), **diputuskan dipecah per beberapa cabang tiap sesi**, bukan sekaligus. Pola ikut C11 (`renderMissingDataAnalysis`): tiap cabang `else if(currentASub==='X'){...}` dibungkus jadi fungsi berdiri sendiri `render<Nama>Form()` — `nF`/`aF` (kalau dipakai cabang itu) dihitung ulang lokal via `numFields()`/`allFields()`, `html=''` lokal di awal, `return html;` di akhir — SATU-SATUNYA perubahan struktural, isi logika 100% byte-exact. **4 cabang pertama dipindah sesi ini** (~105 baris isi cabang, dari total ~3.606 baris D1 → progress D1 ~2,9%): `renderDescriptiveForm()` (cabang `descriptive`, pakai `nF`), `renderTtestForm()` (cabang `ttest`, pakai `nF`+`aF`), `renderOnesampForm()` (cabang `onesamp`, pakai `nF`), `renderPairedForm()` (cabang `paired`, pakai `nF`). Call-site di `renderASub()` (tetap di app.js) masing-masing jadi 1 baris `html+=render<Nama>Form();`. **Dicek dulu sebelum dibungkus**: keempat cabang ini cuma baca `nF`/`aF` (dihitung ulang via fungsi yang sama persis, aman dipanggil dari scope manapun), plus `data`/`aState`/`SE`/helper global (`tryStats`, `mkSelect`, `stCard`, `sigBadge`, `svgBoxplot`, `svgHistogram`, `svgQQ`, `svgScatter`) — tidak ada pembacaan variabel scope `renderASub()` lain (`el`, atau `html` milik cabang lain sebelum/sesudahnya), jadi pembungkusan dijamin tidak mengubah perilaku. Dicek grep: tidak ada deklarasi ganda `renderDescriptiveForm`/`renderTtestForm`/`renderOnesampForm`/`renderPairedForm`, dan masing-masing cabang `currentASub==='descriptive'/'ttest'/'onesamp'/'paired'` cuma muncul 1x di `app.js`. **Diverifikasi via Node `vm` module** (stub `SE`/`aState`/`data`/`numFields`/`allFields`/`tryStats`/`mkSelect`/`stCard`/`sigBadge`/`svgBoxplot`/`svgHistogram`/`svgQQ`/`svgScatter`): keempat fungsi baru dipanggil langsung tanpa exception, masing-masing menghasilkan string HTML (1127/595/1664/595 karakter) — belum sempat dites terintegrasi penuh via `renderASub()` dalam 1 konteks `vm` yang sama dengan seluruh `app.js` (file besar, stub SE penuh belum dibuat sesi ini), disarankan dites manual di browser sebelum dianggap 100% aman, sesuai aturan workflow. Ekstraksi tiap fungsi dicek byte-exact (substring match) terhadap isi cabang `app.js` lama sebelum dipotong. `app.js` lolos `node --check` sebelum & sesudah pemotongan (10.295 → 10.205 baris). `index.html` diupdate: `analyze-form-render.js` (baru) dimuat di kategori "4. Analyze layer", setelah `analyze-tab-shell.js`, sebelum `app.js`. **D1 status di tabel Bagian 3 diupdate jadi 🟡 SEBAGIAN** — sisa ~46 cabang (mulai dari `rmanova`, persis setelah `paired`) masih di `app.js`, lanjutkan sesi berikutnya dari situ. |
| 2026-09-17 | D1 (sebagian: 5 dari ~50 cabang, lanjutan) | `js/analyze/analyze-form-render.js` | **Lanjutan pemisahan bertahap D1**, cabang ke-5: `renderRmanovaForm()` (cabang `currentASub==='rmanova'`, Repeated Measures ANOVA — 94 baris isi cabang, dari baris 554–647 `app.js` snapshot sebelum dipotong). Pola persis sama seperti 4 cabang sebelumnya: cabang hanya pakai `nF` (dihitung ulang lokal via `numFields()`), tidak pakai `aF`/`el`/`html` milik cabang lain — dibungkus jadi fungsi berdiri sendiri dengan `html=''` lokal di awal dan `return html;` di akhir, isi logika 100% byte-exact (kalkulasi RM-ANOVA, slot variabel dinamis T1/T2/T3..., profile plot SVG, tabel deskriptif per titik waktu, catatan sphericity + post-hoc pairwise). Call-site di `renderASub()` (tetap di app.js) jadi `else if(currentASub==='rmanova'){ html+=renderRmanovaForm(); }`. **Dicek dulu sebelum dibungkus**: cabang ini cuma baca `nF`, `aState.rmVars`, `data`, `SE`, `tryStats`, `mkSelect`, `stCard`, `sigBadge`, `escHtml` — semua global/scope-independent, tidak menyentuh `aF`/`el`/variabel scope `renderASub()` lain. Dicek grep: tidak ada deklarasi ganda `renderRmanovaForm`, dan cabang `currentASub==='rmanova'` cuma muncul 1x di `app.js`. **Diverifikasi via Node `vm` module** (stub `numFields`/`allFields`/`aState`/`data`/`SE.validNums`/`SE.repeatedMeasuresAnova`/`SE.fmt4`/`SE.pairedTTest`/`tryStats`/`mkSelect`/`stCard`/`sigBadge`/`escHtml`): `renderRmanovaForm()` dipanggil langsung tanpa exception, menghasilkan HTML 4.021 karakter berisi "Repeated Measures ANOVA" dan "Profile Plot" sesuai isi cabang asli. Ekstraksi dicek byte-exact (substring match) terhadap isi cabang `app.js` lama sebelum dipotong. `app.js` lolos `node --check` sebelum & sesudah pemotongan (10.205 → 10.112 baris). `index.html` tidak perlu diupdate (file `analyze-form-render.js` sudah dimuat dari sesi sebelumnya). **D1 progress sekarang 5 dari ~50 cabang (~199 dari ~3.606 baris isi cabang, ~5,5%)** — lanjutkan sesi berikutnya dari cabang `anova` (persis setelah `rmanova`). |
| 2026-09-17 | D1 (sebagian: 6 dari ~50 cabang, lanjutan) | `js/analyze/analyze-form-render.js` | **Lanjutan pemisahan bertahap D1**, cabang ke-6: `renderAnovaForm()` (cabang `currentASub==='anova'`, One-Way ANOVA — 43 baris isi cabang, baris 558–600 `app.js` snapshot sebelum dipotong). Pola sama seperti 5 cabang sebelumnya: dihitung ulang lokal `nF`/`aF` via `numFields()`/`allFields()` (dipakai keduanya, sama seperti `ttest`), `html=''` lokal di awal, `return html;` di akhir — isi logika 100% byte-exact (perhitungan One-Way ANOVA per grup, toggle post-hoc + picker custom-select `av-ph` dengan 4 metode Tukey/Bonferroni/LSD/Holm, hint teks per metode, boxplot distribusi grup). Call-site di `renderASub()` (tetap di app.js) jadi `else if(currentASub==='anova'){ html+=renderAnovaForm(); }`. **Dicek dulu sebelum dibungkus**: cabang ini baca `nF`, `aF`, `data`, `aState`, `SE`, `tryStats`, `mkSelect`, `stCard`, `sigBadge`, `svgBoxplot`, plus nulis ke `_cR['av-ph']` (registry custom-select global, dari C5/`custom-select.js`) — tidak menyentuh `el`/`html` milik cabang lain. Dicek grep: tidak ada deklarasi ganda `renderAnovaForm`, dan cabang `currentASub==='anova'` cuma muncul 1x di `app.js`. **Diverifikasi via Node `vm` module** (stub `numFields`/`allFields`/`aState`/`data`/`SE.validNums`/`SE.onewayANOVA`/`tryStats`/`mkSelect`/`stCard`/`sigBadge`/`svgBoxplot`/`_cR`): `renderAnovaForm()` dipanggil langsung tanpa exception dengan data sintetis 3 grup, menghasilkan HTML 1.699 karakter berisi "One-Way ANOVA" dan "Group Distributions", serta `_cR['av-ph']` terisi dengan benar (registry post-hoc picker ter-daftar). Ekstraksi dicek byte-exact (diff hanya menambah 1 baris `return html;`) terhadap isi cabang `app.js` lama sebelum dipotong. `app.js` lolos `node --check` sebelum & sesudah pemotongan. `index.html` tidak perlu diupdate (file `analyze-form-render.js` sudah dimuat dari sesi sebelumnya). **D1 progress sekarang 6 dari ~50 cabang (~242 dari ~3.606 baris isi cabang, ~6,7%)** — lanjutkan sesi berikutnya dari cabang `anova2` (Two-Way ANOVA, persis setelah `anova`). |
| 2026-09-21 | D1 (pindah fisik whole-function, bukan per-cabang) | `js/analyze/analyze-form-render.js` | **Perubahan strategi**: alih-alih lanjut breakdown per-cabang dari `anova2` sesuai rencana sesi sebelumnya, user memindahkan **seluruh badan `renderASub()` sekaligus** (bukan cuma cabang yang belum dipisah) — copy-paste manual oleh user, lalu diverifikasi Claude. Blok yang dipindah: `app.js` baris 531–4129 (snapshot 10.070 baris) = badan `renderASub()` (531–3911, 3.381 baris) + 8 fungsi meta-analysis yang nempel tepat setelahnya tanpa terpisah fungsi lain di antaranya (`svgMetaForestPlot`, `svgFunnelPlot`, `metaSetModel`, `metaSetEffect`, `metaAddStudy`, `metaRemoveStudy`, `metaLoadExample`, `runMetaAnalysis`, baris 3919–4129). Ditempel di `analyze-form-render.js` setelah 6 fungsi `render<Nama>Form()` yang sudah ada dari sesi-sesi sebelumnya (`descriptive`/`ttest`/`onesamp`/`paired`/`rmanova`/`anova`) — isi switch raksasa di dalam `renderASub()` yang baru dipindah ini tetap memanggil keenam fungsi itu seperti biasa (`html+=renderDescriptiveForm();` dst), tidak ada duplikasi definisi nested. **Proses 2 langkah** (dicek Claude di masing-masing langkah): (1) user copy-paste blok ke `analyze-form-render.js` TANPA hapus dari `app.js` dulu — diverifikasi diff byte-exact 0 selisih antara isi lama di `app.js` dan hasil tempelan di file baru, `node --check` lolos kedua file, grep pastikan tidak ada fungsi nested yang ikut dobel ter-declare; sempat ada duplicate declaration sementara untuk 9 nama fungsi (`renderASub` + 8 fungsi meta) di kedua file — aman karena `analyze-form-render.js` dimuat sebelum `app.js` di `index.html` jadi definisi `app.js` yang menang selama transisi. (2) user hapus blok baris 531–4129 dari `app.js` — diverifikasi ulang: sambungan baris tempat blok dihapus rapi (komentar sebelum `renderASub()` lama langsung nyambung ke fungsi `svgMediationPath` berikutnya, tidak ada baris nyangkut), `node --check` lolos, grep pastikan ke-9 nama fungsi itu sekarang 0 kemunculan sebagai definisi di `app.js` (cuma tersisa 1x di `analyze-form-render.js`), dan 31 call-site `renderASub()` yang tersebar di `app.js` (dipanggil dari handler switch-tab dll, bukan definisi) tetap utuh — aman resolve ke definisi baru lewat scope-fallback global karena urutan muat script tidak berubah. `app.js` turun dari 10.070 → 6.468 baris (selisih 3.602 baris). `index.html` TIDAK perlu diubah (`analyze-form-render.js` sudah dimuat dari sesi-sesi sebelumnya, urutan relatif terhadap `app.js` tidak berubah). **Implikasi untuk breakdown D1 selanjutnya**: pemindahan fisik ke file target sekarang SELESAI 100% — tapi progress breakdown per-cabang (fungsi `render<Nama>Form()` berdiri sendiri) TIDAK berubah, masih 6 dari ~50 cabang. Sisa ~44 cabang inline sekarang lokasinya pindah dari `app.js` ke dalam switch raksasa `renderASub()` di `analyze-form-render.js` — kalau mau lanjut breakdown, kerjanya sekarang di file itu, bukan lagi di `app.js`. **Belum dites manual di browser** sesi ini (hanya diverifikasi statis: diff byte-exact + `node --check` + grep dobel-deklarasi + integritas call-site) — disarankan dites manual (buka tab Analyze, coba beberapa jenis analisis termasuk Meta-Analysis) sebelum dianggap 100% aman, sesuai aturan workflow. |
| 2026-09-19 | D1 (SELESAI — breakdown per-cabang tuntas) | `js/analyze/analyze-form-render.js` | Status D1 dinaikkan dari 🟡 SEBAGIAN ke ✅ SUDAH **atas konfirmasi user**; ~40 cabang inline yang tersisa sudah dibungkus jadi fungsi `render<Nama>Form()` berdiri sendiri lewat commit `d306c31` dan `5976b6d` di repo. **Diverifikasi statis oleh Claude** (file upload dicek `cmp` identik dengan repo HEAD): (a) `renderASub()` tinggal dispatcher 217 baris — 46 cabang `else if`, tiap cabang 1 baris `html+=render<Nama>Form();`, penutup `el.innerHTML=html;`; (b) 46 fungsi unik yang dipanggil dispatcher (45 di `analyze-form-render.js` + `renderMissingDataAnalysis` di `missing-data.js`) masing-masing terdeklarasi tepat 1x di seluruh `js/`, tidak ada nama yang tidak terdefinisi; (c) `currentASub===` di luar dispatcher hanya muncul DI DALAM fungsi form untuk sub-tab yang berbagi 1 form (`glm-*`, `hlm-*`, `roc`/`roc_compare`, `km`/`logrank`/`cox`, `poweranalysis`/`powerplot`/`sensitivity`, `moderation`/`simpleslopes`/`jn`) — percabangan internal by design; (d) `node --check` lolos. **Temuan kecil (tidak diubah)**: `const nF=numFields(),aF=allFields();` di awal `renderASub()` sudah tidak dipakai (cuma dideklarasikan). **Tidak diuji di browser oleh Claude** — cek manual tetap disarankan: buka tab Analyze dan klik tiap grup sub-tab (terutama yang berbagi form: GLM, HLM, ROC, Survival, Power, Moderation) lalu pastikan console bersih. 8 fungsi meta-analysis (`svgMetaForestPlot` dst) tetap di file ini di bawah `renderASub()`, lihat catatan E4/E5. |
| 2026-09-19 | E1 | `js/charts/svg-charts-core.js` (baru) | Dipindah sbg var/fungsi global apa adanya, pola sama seksi C: `var PAL2` (palet 8 warna) + 7 fungsi chart generik `svgScreePlot`, `svgHistogram`, `svgBoxplot`, `svgScatter`, `svgQQ`, `svgResidual`, `svgCorrMatrix`. Ditemukan via grep marker `SVG CHARTS` di app.js snapshot commit `5976b6d` (6.468 baris): header+`PAL2` di baris 160–164, fungsi di baris 267–464 (198 baris). **Temuan**: (a) isi aslinya BUKAN "bar/line/scatter/pie" seperti tertulis di baseline — tidak ada chart bar/line/pie generik; deskripsi baris E1 dan tree Bagian 2 dikoreksi. (b) Di app.js lama blok ini tidak menyatu: E2 (165–238) dan E3 (240–266) nyelip di antara `PAL2` dan `svgScreePlot`, jadi E1/E2/E3 tidak bisa satu potongan utuh. (c) Dependency (`SE.validNums`/`quantile`/`isV`/`pearsonR`/`mean`/`std`/`normInv`, `escHtml`) hanya dibaca di dalam body fungsi (runtime), tidak ada top-level call — aman dimuat sebelum `app.js`. `PAL2` dipakai juga oleh E2, jadi file ini WAJIB dimuat sebelum `interaction-plot.js`. **Diverifikasi**: 198 baris fungsi + `PAL2` byte-exact terhadap app.js commit `5976b6d` (0 selisih); `node --check` lolos; tidak ada deklarasi ganda (grep `app.js` dan `analyze-form-render.js`; yang tersisa di sana hanya pemanggil); load seluruh script urut `index.html` di Node `vm` tanpa error; tes fungsional dengan `SE` asli dan data sintetis 48 baris — histogram, boxplot (2 grup), scatter/QQ/residual (48 titik), corr matrix (9 sel), scree (4 titik) semuanya menghasilkan `<svg>` valid dengan jumlah elemen sesuai; guard data-kurang jalan (`svgHistogram` dengan 1 nilai mengembalikan `chart-empty`). `index.html`: `svg-charts-core.js` dimuat di kategori 4 (Analyze layer) setelah `analyze-tab-shell.js`, sebelum `interaction-plot.js`, `analyze-form-render.js`, dan `app.js`. Pemanggil: cabang-cabang form di `analyze-form-render.js` (descriptive, ttest, anova, dll, `efa` untuk scree) dan `renderOutput()` di `app.js`. |
| 2026-09-19 | E2 | `js/charts/interaction-plot.js` (baru) | Dipindah sbg fungsi global apa adanya: `svgInteractionPlot(cellMeansTable, xLabel, lineLabel, depLabel, W, H)`, 74 baris (app.js snapshot commit `5976b6d` baris 165–238, di bawah komentar `Interaction Plot (Two-Way or Three-Way ANOVA)`). Dependency `PAL2` (dari E1) dan `escHtml` dibaca di dalam body (runtime) — file dimuat SETELAH `svg-charts-core.js` (sudah benar di `index.html`). Pemanggil (4 titik): preview Two-Way/Three-Way di `analyze-form-render.js` (`renderAnova2Form`/`renderAnova3Form`) dan `renderOutput()` di `app.js` (kasus anova2/anova3). ⚠️ **Temuan (belum diperbaiki, cuma dicatat — sengaja tidak diubah saat potong-tempel)**: `depLabel` (judul sumbu Y), `lineLabel` (legenda), dan `row.level` (legenda + tooltip `<title>`) dimasukkan ke string SVG **tanpa `escHtml`**, padahal `xLabel` dan label sumbu X sudah di-escape. Nama variabel/level berasal dari data user → karakter `<` atau `&` di nama variabel/level bisa merusak SVG. **Diverifikasi**: 74 baris byte-exact terhadap app.js commit `5976b6d` (0 selisih, tidak ada sisa lain di file); `node --check` lolos; tidak ada deklarasi ganda; tes fungsional dengan `SE.twowayANOVA` dan `SE.threewayANOVA` asli (data sintetis 2×3, n=48): Two-Way menghasilkan `<svg>` valid berisi 2 garis (level A) dan 8 lingkaran (6 titik data + 2 penanda legenda), Three-Way (`cellMeansAB`) juga `<svg>` valid. `app.js` 6.468 → 6.190 baris setelah E1+E2 keluar (dihitung pada snapshot upload sesi ini). |
| 2026-09-19 | E3 | `js/output/output-render-basic.js` (baru — file pertama kategori Output) | Dipindah sbg fungsi global apa adanya: `renderAnovaTable(effects, error, total)`, 26 baris termasuk komentar header (baris 160–185 app.js snapshot sesi ini, 6.190 baris) — jauh lebih kecil dari estimasi baseline 3751–3976 (226 baris), sama seperti rentang baseline basi lain. Murni string builder: 0 dependency, 0 top-level call → aman dimuat sebelum `app.js`. Pemanggil (2 titik, runtime): `renderOutput()` di `app.js`, kasus anova2 dan anova3 — call-site tidak diubah, resolve lewat scope-fallback global. File diberi header komentar sesuai Bagian 4 poin 3; F3 (`renderOutput()` bagian basic) nantinya masuk ke file yang sama. Di `app.js` ditinggal komentar pointer 5 baris (pola C4/C5). **Diverifikasi**: blok byte-exact terhadap app.js lama (substring match); `node --check` lolos untuk `app.js` baru dan file baru; `renderAnovaTable` 0 deklarasi di `app.js` baru dan 1 di file baru, 2 pemanggil utuh; load 38 script urut `index.html` di Node `vm` tanpa error (perbandingan sebelum vs sesudah E3: sama-sama 0 error, semua `typeof` fungsi E1/E2/E3/`renderASub` = function); tes fungsional dengan `SE.twowayANOVA`/`SE.threewayANOVA` asli: tabel Two-Way (3 efek + baris Error + Total) dan Three-Way (7 efek, ada baris Total) ter-render sebagai `<table>` valid. `app.js` lolos `node --check` sebelum & sesudah pemotongan (6.190 → 6.169 baris). `index.html` diupdate: `output-render-basic.js` (baru) dimuat di kategori baru "4b. Output layer", setelah `analyze-form-render.js`, sebelum `app.js`. **Belum dites manual di browser** — sesuai aturan workflow, tes: Analyze → Two-Way ANOVA lalu Three-Way ANOVA → Run → cek tab Output (tabel sumber variansi + interaction plot muncul, console bersih). |
| 2026-09-19 | E4, E5 | `js/charts/forest-funnel-plot.js` (baru) | Dipindah sbg fungsi global apa adanya, dua fungsi digabung 1 file sesuai target tabel: `svgMetaForestPlot(studies, res, effectType)` (E4, 81 baris, baris 3930–4010) dan `svgFunnelPlot(studies, res)` (E5, 68 baris, baris 4012–4079) — sumbernya `js/analyze/analyze-form-render.js`, BUKAN `app.js` (ikut terangkut saat D1 dipindah fisik; baseline 9878–10028 sudah basi). Blok 150 baris (dua fungsi + komentar header + baris kosong di antaranya). Dependency cuma `escHtml` (app.js), dibaca di runtime; tidak ada top-level call, tidak butuh `PAL2` — aman dimuat sebelum `analyze-form-render.js`. Data masukan dari `computeMetaAnalysis` (`res.studyData`, `pooledEffect`, `ci_lo`/`ci_hi`, `model`). Pemanggil (4 titik, semua runtime): `renderMetaanalysisForm` di `analyze-form-render.js` (preview) dan `renderOutput()` di `app.js` (kasus metaanalysis), call-site tidak diubah. Di `analyze-form-render.js` ditinggal komentar pointer 5 baris (pola C4/C5/E3); banner `RUN ANALYSIS FUNCTIONS` di atasnya dibiarkan karena masih membungkus `metaSetModel` dst. **Tidak ikut dipindah**: 6 fungsi UI meta-analysis (`metaSetModel`, `metaSetEffect`, `metaAddStudy`, `metaRemoveStudy`, `metaLoadExample`, `runMetaAnalysis`) — tetap di `analyze-form-render.js`. Nama mirip tapi BEDA fungsi: `svgForestPlot` (forest plot Cox, `app.js`) — tidak ada tabrakan nama. ⚠️ **Temuan kecil (saat dipindah sengaja tidak diubah; ✅ SUDAH DIPERBAIKI atas permintaan user — lihat entri "E4/E5 perbaikan" di bawah)**: (a) `svgMetaForestPlot` mendefinisikan `<marker id="fp-arrow">` yang tidak pernah dipakai di SVG-nya; (b) parameter `studies` di `svgFunnelPlot` tidak dipakai (data diambil dari `res.studyData`); (c) `effectType` masuk ke teks sumbu-X tanpa `escHtml` (nilainya kode internal `d`/`r`/`or`/`g`/`other` dari `metaSetEffect`, atau `yi` sebagai fallback di `renderOutput`, jadi risikonya kecil). **Diverifikasi**: 150 baris byte-exact terhadap `analyze-form-render.js` upload (substring match); `node --check` lolos untuk kedua file; fungsi masing-masing 0 deklarasi di `analyze-form-render.js` baru dan 1 di file baru, pemanggil utuh; load 39 script urut `index.html` di Node `vm` tanpa error; tes fungsional dengan `computeMetaAnalysis` asli (7 studi contoh dari `metaLoadExample`, model fixed dan random, satu nama studi sengaja berisi `<x&y>` dan panjang): output kedua fungsi **identik karakter-per-karakter** dengan versi lama yang di-eval dari file upload (forest 8.610 karakter, funnel 4.459 karakter), forest berisi 7 kotak studi + 1 diamond + label `FE`/`RE Summary`, funnel berisi 7 titik + 2 polyline; `renderMetaanalysisForm()` menghasilkan HTML berisi forest dan funnel. `analyze-form-render.js` 4.141 → 3.997 baris. `index.html` diupdate: `forest-funnel-plot.js` (baru) dimuat di kategori 4, setelah `interaction-plot.js`, sebelum `analyze-form-render.js`. **Belum dites manual di browser** — tes: Analyze → Meta-Analysis → Load Example → cek preview Forest Plot dan Funnel Plot, lalu Run → cek tab Output. |
| 2026-09-19 | E8–E14 (perencanaan dokumen, BUKAN pemisahan kode) | `js/charts/power-plot.js`, `moderation-plot.js`, `roc-plot.js`, `survival-plot.js`, `discriminant-cluster-plot.js`, `bayesian-plot.js`, `imputation-plot.js` (semuanya belum dibuat) | Tindak lanjut Temuan E poin (2): 15 fungsi `svg*` di `app.js` yang tidak punya baris di tabel E diputuskan user dikelompokkan **per fitur, 1 file chart per file `stats-engine` pasangannya** (pola sama seperti pemisahan engine, jadi kode 1 fitur gampang dicari: hitungan di `stats-*.js`, gambar di `*-plot.js`). Dipetakan jadi 7 baris baru: E8 Power (`svgPowerCurve`, `svgSensitivityCurve`), E9 Moderation (`svgModerationPlot`, `svgSimpleSlopesPlot`, `svgJohnsonNeymanPlot`), E10 ROC (`svgROC`, `svgROCCompare`), E11 Survival (`svgKaplanMeier`, `svgForestPlot` Cox), E12 Discriminant+Cluster (`svgDiscriminantPlot`, `svgClusterPlot`, `svgElbow`, `svgDendrogram`), E13 Bayesian (`svgBayesPosterior`), E14 MICE (`svgConvergence`). Keputusan pemetaan: (a) `svgForestPlot` (forest plot hazard ratio Cox, ukuran fungsi ~36 baris, signature `(res,W,H)`) sengaja TIDAK digabung ke `forest-funnel-plot.js` (E4/E5) — beda sumber data (hasil Cox, bukan `computeMetaAnalysis`) meski namanya mirip; (b) `svgSimpleSlopesPlot` cuma wrapper 4 baris yang memanggil `svgModerationPlot`, jadi satu file dengan E9; (c) E13 dan E14 masing-masing cuma 1 fungsi, tapi TIDAK digabung jadi file "misc" karena isinya tidak berhubungan. Tree Bagian 2 diupdate (7 file baru di `js/charts/`), catatan Temuan E poin (2) ditandai selesai, ringkasan Bagian 7 diupdate. Nomor baris `app.js` di tabel = snapshot 2026-09-19 (6.169 baris, sesudah E3) dan akan bergeser tiap ada yang dipindah — cari lokasi pasti lewat grep `function svg...`. **Belum ada kode yang dipindah di entri ini; dependency tiap grup (`escHtml`, `SE.*`, variabel lokal `app.js`) belum dicek dan akan dicek saat masing-masing dipindah** (aturan Bagian 4 poin 3–4). |
| 2026-09-19 | E4/E5 perbaikan (bug fix kecil, bukan split) | `js/charts/forest-funnel-plot.js`, `app.js`, `js/analyze/analyze-form-render.js` | Atas permintaan user, 3 temuan kecil yang dicatat di entri E4/E5 diperbaiki. (1) **Marker `fp-arrow` tidak terpakai** — baris `<defs><marker id="fp-arrow">...</marker></defs>` di `svgMetaForestPlot` dihapus. Dicek grep di seluruh repo (`.js`/`.html`/`.css`): id itu tidak pernah direferensikan (`marker-end`/`url(#fp-arrow)`) di mana pun, jadi tidak ada efek visual; sekaligus menghilangkan id duplikat kalau beberapa forest plot muncul di 1 halaman. (2) **Parameter `studies` di `svgFunnelPlot` tidak dipakai** — signature diubah dari `svgFunnelPlot(studies, res)` jadi `svgFunnelPlot(res)` (data sudah dari `res.studyData`); **2 call-site disesuaikan**: `js/analyze/analyze-form-render.js` (`svgFunnelPlot(metaRes)`, preview) dan `app.js` di `renderOutput()` (`svgFunnelPlot(r)`, kasus metaanalysis) — grep memastikan hanya 2 pemanggil, tidak ada yang lain (export/dll). Ini SATU-SATUNYA perubahan signature/API di antara ketiganya. (3) **`effectType` masuk teks sumbu-X tanpa `escHtml`** — sekarang `Effect Size (`+`escHtml(effectType)`+`)`; untuk nilai normal (`d`/`r`/`or`/`g`/`other`/`yi`) hasilnya identik karena tidak ada karakter yang di-escape. **Diverifikasi**: `node --check` lolos untuk ketiga file; `svgMetaForestPlot` baru = versi lama dengan baris `<defs>` fp-arrow dibuang (string identik karakter-per-karakter, model fixed dan random); `svgFunnelPlot(res)` baru = `svgFunnelPlot(studies, res)` lama (identik karakter-per-karakter); `effectType` jahat (`<img src=x onerror=alert(1)>&`) sekarang ter-escape (tidak ada tag mentah di SVG); load 39 script urut `index.html` di Node `vm` tanpa error; kedua call-site dites end-to-end dengan `computeMetaAnalysis` asli (7 studi contoh): `renderMetaanalysisForm()` (preview) dan `renderOutput()` dengan output bertipe metaanalysis sama-sama menghasilkan forest + funnel tanpa `undefined`/`NaN` di SVG. `forest-funnel-plot.js` 163 → 162 baris; header komentar file diupdate (`svgFunnelPlot(res)`). **Belum dites manual di browser** — tes: Analyze → Meta-Analysis → Load Example (preview forest + funnel), lalu Run → cek tab Output. |
| 2026-09-20 | E6 | `js/charts/sem-mediation-diagram.js` (baru) | Dipindah sbg fungsi global apa adanya: `svgMediationPath(xName,mNames,yName,preview)`, 44 baris (app.js snapshot upload sesi ini, 6.169 baris, baris 231–274, di bawah komentar `Mediation path diagram SVG`). Dependency: tidak ada — murni string builder dari argumen yang dioper (`preview.barronKenny.*`), tidak baca `SE`/`PAL2`/`escHtml`/var global lain, jadi urutan muat tidak kritis (ditaruh setelah `forest-funnel-plot.js`, sebelum `analyze-form-render.js`, mengikuti pola E4/E5). Pemanggil yang **bisa diverifikasi sesi ini**: `renderOutput()` di `app.js`, kasus `mediation` (1 titik, baris ~3307 setelah pemotongan) — call-site tidak diubah, resolve lewat scope-fallback global. **Tidak bisa diverifikasi sesi ini**: kemungkinan ada pemanggil preview lain di `js/analyze/analyze-form-render.js` (form Mediation, pola sama seperti E4/E5 dulu) — file itu TIDAK ikut diupload sesi ini (hanya `app.js`, `index.html`, `ARCHITECTURE.md`), jadi tidak bisa digrep; kalau ada, panggilannya tidak perlu berubah (signature sama persis, fungsi tetap global) tapi tetap disarankan digrep manual oleh user untuk memastikan tidak ada `svgMediationPath` lain yang terdeklarasi ganda di file tsb. ⚠️ **Temuan E6 (dicatat, belum diperbaiki)**: `xName`, tiap `mName`, dan `yName` masuk ke teks SVG tanpa `escHtml` — pola sama dengan Temuan E2 (`svgInteractionPlot`), lihat catatan Temuan E di atas tabel; menunggu keputusan user apakah E2+E6 diperbaiki sekaligus atau ditunda sampai semua chart selesai dipindah. **Diverifikasi**: 44 baris byte-exact terhadap `app.js` upload sesi ini (`diff` 0 selisih); `node --check` lolos untuk `app.js` baru dan file baru; `svgMediationPath` 0 deklarasi di `app.js` baru dan 1 di file baru (grep), 1 pemanggil di `app.js` utuh; output fungsi dites dengan 3 skenario (1 mediator, 3 mediator, `preview=null`) — SVG valid (`<svg>...</svg>`), tidak ada `undefined`/`NaN`; dibandingkan langsung dengan hasil eval fungsi versi lama dari `app.js` upload (input identik) — **keluaran identik karakter-per-karakter**. **Tidak bisa dites**: load penuh urutan `<script>` `index.html` di Node `vm` (file-file lain yang direferensikan `index.html` — `state.js`, `missing-data.js`, dst — tidak ikut diupload sesi ini, hanya 3 file disebut di atas), dan tes manual di browser. `app.js` 6.169 → 6.130 baris. `index.html` diupdate: `sem-mediation-diagram.js` (baru) dimuat di kategori 4, setelah `forest-funnel-plot.js`, sebelum `analyze-form-render.js`. **Tes yang disarankan untuk user**: Analyze → Mediation Analysis → isi X/M/Y → Run → cek tab Output (path diagram X→M→Y muncul dengan panah + label a/b/c', console bersih); juga grep manual `svgMediationPath` di `js/analyze/analyze-form-render.js` untuk pastikan tidak ada deklarasi ganda. |
| 2026-09-20 | E6 — `analyze-form-render.js` diupload, temuan escHtml diperbaiki | `js/analyze/analyze-form-render.js` (grep saja, tidak diedit), `js/charts/sem-mediation-diagram.js`, `app.js` (komentar pointer) | User upload `js/analyze/analyze-form-render.js`. **Digrep**: `svgMediationPath` dipanggil di 1 titik (baris ~2063, `renderMediationForm()`, preview) dengan argumen `aState.medX||'X'`, `aState.medM`, `aState.medY||'Y'` — nama variabel dari pilihan user (`mkSelect`), bukan hardcode, jadi bisa memuat karakter `<`/`&` kalau nama variabel di dataset user memang mengandung itu; `svgMediationPath` sendiri **tidak terdeklarasi** di file ini (0 deklarasi ganda dengan `sem-mediation-diagram.js`, aman). Call-site ini tidak diubah — signature `svgMediationPath` sama persis, resolve lewat scope-fallback global. Atas permintaan user, temuan escHtml dari entri E6 di atas **langsung diperbaiki** (bukan cuma dicatat): di `js/charts/sem-mediation-diagram.js`, `xName.slice(0,8)` → `escHtml(xName.slice(0,8))`, `yName.slice(0,8)` → `escHtml(yName.slice(0,8))`, `mName.slice(0,8)` → `escHtml(mName.slice(0,8))` (3 titik). Dependency baru: `escHtml()` dari `app.js`, dibaca di runtime (bukan top-level) — aman, file tetap dimuat sebelum `app.js` (urutan `index.html` tidak berubah). Header komentar file diupdate (dependency + status temuan). **Diverifikasi**: `node --check` lolos; tes dengan nama normal (`'Stress'`/`['Coping']`/`'Wellbeing'`) menghasilkan output **identik karakter-per-karakter** dengan versi sebelum fix (tidak ada regresi untuk kasus normal, karena tidak ada karakter yang di-escape); tes dengan nama jahat (`'<img src=x>'`/`['A&B']`/`'</svg><script>'`) menghasilkan SVG yang aman — tidak ada tag `<img`/`<script` mentah lolos ke output, cuma 1 `</svg>` penutup (tidak premature-closed). E2/`svgInteractionPlot` (temuan serupa, file `interaction-plot.js`) **sengaja TIDAK ikut diperbaiki** sesi ini — file itu tidak diupload, statusnya tetap belum diperbaiki, jangan disamakan. |
| 2026-09-20 | E7 | `js/charts/sem-mediation-diagram.js` (append ke file E6, bukan file baru) | Dipindah sbg fungsi global apa adanya: `svgSEMDiagram(latents,latentMap,paths,semResult,semColors)`, 84 baris (`app.js` snapshot sesi ini setelah E6, 6.132 baris, baris 992–1075, di bawah komentar `SEM Path Diagram SVG`). Ditumpuk di bawah `svgMediationPath` di file yang sama (satu file untuk E6+E7, sesuai rencana tabel E), dipisah baris kosong. Dependency: tidak ada (tidak pakai `escHtml`, tidak akses `SE`/`PAL2`/vars/data global) — beda dari `svgMediationPath` yang butuh `escHtml`. Tidak ada perubahan `index.html` (file target sudah dimuat sejak E6, urutan tidak berubah). **Pemanggil, digrep di `app.js` + `analyze-form-render.js` (upload sesi lalu)**: HANYA 1 titik — preview di `renderSEMForm()`, `js/analyze/analyze-form-render.js` baris ~2350, argumen `aState.semLatents`, `aState.semLatentMap`, `aState.semPaths`, `semResult`, `semColors`. **Tidak ada pemanggil di `app.js`** (dicek dengan grep `svgSEMDiagram(` dan variasi case-insensitive `semdiagram` — 0 hasil selain deklarasi) — beda dari `svgMediationPath` (E6) yang punya pemanggil ganda (preview + output). Kemungkinan artinya fitur output SEM belum/tidak menampilkan diagram path, hanya form preview-nya; tidak diverifikasi lebih lanjut karena di luar cakupan pemindahan kode. `svgSEMDiagram` 0 deklarasi ganda dengan file lain (grep). ⚠️ **Temuan E7 (dicatat, BELUM diperbaiki, beda dari E6)**: nama konstruk laten (`ln`) dan nama indikator (`v`) masuk ke teks SVG tanpa `escHtml` — pola sama persis dengan Temuan E2/E6, keduanya dari data user (pilihan variabel form SEM); sengaja tidak diperbaiki sesi ini karena user cuma minta "lanjut E7", bukan sekalian perbaiki — beda dari E6 kemarin yang user minta eksplisit. **Diverifikasi**: 84 baris byte-exact terhadap `app.js` snapshot sesi ini (`diff` 0 selisih); `node --check` lolos untuk `app.js` baru dan file target; grep deklarasi `svgSEMDiagram`/`svgMediationPath` = 0 di `app.js`, 2 di file target (sesuai, keduanya ada di sana), tidak ada duplikat; output fungsi dites dengan data contoh (2 laten, masing-masing 1–2 indikator, 1 path terstruktur + `semResult` dengan beta signifikan) — dibandingkan langsung dengan hasil eval fungsi versi lama dari `app.js` sebelum dipotong (input identik) — **keluaran identik karakter-per-karakter**; guard clause `latents` kosong dites terpisah — tetap mengembalikan `<div class="chart-empty">...</div>` seperti semula. **Tidak bisa dites**: load penuh urutan `<script>` `index.html` di Node `vm` (file lain yang direferensikan belum semua diupload sesi ini), dan tes manual di browser. `app.js` 6.132 → 6.056 baris (termasuk update pointer comment E6 yang jadi basi karena E7 sudah pindah). **Tes yang disarankan untuk user**: Analyze → SEM → tambah ≥2 konstruk laten + indikator + 1 path structural → cek preview diagram SEM di form (panah antar-laten + indikator + label β, console bersih); kalau ada halaman Output SEM yang seharusnya menampilkan diagram serupa tapi ternyata tidak (karena 0 pemanggil di app.js di atas), itu kemungkinan gap fitur lama yang sudah ada sebelum sesi ini, bukan regresi dari pemindahan kode — laporkan balik kalau perlu ditambahkan. |
| 2026-09-20 | E7 — temuan escHtml diperbaiki | `js/charts/sem-mediation-diagram.js`, `app.js` (komentar pointer) | Atas permintaan user ("sekalian bereskan temuanya"), temuan escHtml dari entri E7 di atas **langsung diperbaiki**: di `svgSEMDiagram`, `ln.slice(0,9)` → `escHtml(ln.slice(0,9))` (nama konstruk laten) dan `v.slice(0,6)` → `escHtml(v.slice(0,6))` (nama indikator) (2 titik). Header komentar file digabung jadi satu bagian dependency (`escHtml` sekarang dipakai KEDUA fungsi di file ini, bukan cuma `svgMediationPath`) dan status kedua temuan (E6+E7) ditandai selesai. **Diverifikasi**: `node --check` lolos; tes nama normal (`'Stress'`/`'Support'`, indikator `'s1'`/`'s2'`/`'sp1'`) menghasilkan output **identik karakter-per-karakter** dengan versi sebelum fix (tidak ada regresi); tes nama jahat (laten `'<b>Evil</b>'`, indikator `'</svg><script>alert(1)</script>'` dan `'x&y'`) menghasilkan SVG aman — tidak ada `<script>` atau `<b>` mentah lolos ke output, cuma 1 `</svg>` penutup (tidak premature-closed), dan ditemukan snippet ter-escape (`&lt;b&gt;`) di output. E2/`svgInteractionPlot` (temuan serupa, file `interaction-plot.js`) **masih sengaja TIDAK diperbaiki** — file itu belum diupload, statusnya tetap belum diperbaiki. Dengan ini **semua temuan escHtml di Section E6/E7 sudah beres**; sisa temuan aktif tinggal E2 (svgInteractionPlot, menunggu file `interaction-plot.js`). |
| 2026-09-20 | E2 — `interaction-plot.js` diupload, temuan escHtml diperbaiki | `js/charts/interaction-plot.js` | User upload `js/charts/interaction-plot.js` dan minta "perbaiki dulu" (temuan E2 lama) sebelum lanjut E8. Fix: `depLabel` (judul sumbu Y) → `escHtml(depLabel)`; `lineLabel` (label legend) → `escHtml(lineLabel)`; `row.level` di 2 titik (tooltip `<title>` dan teks legend) → `escHtml(row.level)`. **Ditemukan tambahan saat pengecekan** (belum tercatat di temuan awal 2026-09-19): `p.lbl` (level Factor B, sumber sama dengan `levB` yang sumbu-X-nya sudah di-escape sejak awal) juga tidak di-escape di tooltip `<title>` — ikut diperbaiki jadi `escHtml(p.lbl)`. Total 5 titik fix. `xLabel` dan `levB` (label sumbu X) tidak disentuh — sudah escaped sejak awal. Header komentar file diupdate (dependency `escHtml` + status temuan). File `svgInteractionPlot` sendiri sudah dipindah ke `js/charts/interaction-plot.js` sejak sesi 2026-09-19 (lihat entri changelog di atas) — sesi ini murni memperbaiki temuan keamanan di file yang sudah ada, bukan pemindahan baru, jadi tidak mengubah hitungan progress/baris `app.js`. **Diverifikasi**: `node --check` lolos; tes dengan label normal (`'Factor A'`/`'Factor B'`/`'Score'`, level `'A1'`/`'A2'`/`'B1'`/`'B2'`) menghasilkan output **identik karakter-per-karakter** dengan versi upload asli (tidak ada regresi untuk kasus normal); tes dengan label jahat (`'<script>alert(1)</script>'`, `'</title><b>X</b>'`, `'B2&Co'`, `'X<lbl>'`, `'Line&Label'`, `'Dep<Var>'`) menghasilkan SVG/tooltip aman — tidak ada `<script>` atau `</title><b>` mentah lolos ke output, snippet ter-escape (`&lt;script&gt;`, `&lt;/title&gt;&lt;b&gt;`) terkonfirmasi ada, 1 `</svg>` penutup (tidak premature-closed). **Dengan ini semua temuan escHtml yang tercatat di Section E (E2, E6, E7) sudah beres.** Tidak ada temuan escHtml aktif tersisa di file-file yang sudah diupload sejauh ini. |
| 2026-09-20 | E8 | `js/charts/power-plot.js` (baru) | Dipindah sbg 2 fungsi global apa adanya: `svgPowerCurve(test,alpha,tails,W,H)` dan `svgSensitivityCurve(test,alpha,power,tails,nHighlight,W,H)`, total 103 baris (`app.js` snapshot sesi ini, 6.057 baris, baris 277–379, di bawah blok helper `SE.fInv`/`SE.fCritApprox`/`SE.chiCritApprox` yang TIDAK ikut dipindah — itu polyfill SE terpisah, bukan bagian chart, dibiarkan di `app.js`). Dependency: `computePower()` (global) — dicek dengan grep, **tidak ada di `app.js`** (berarti sudah lebih dulu dipindah ke `js/stats-engine/stats-poweranalysis.js` di sesi sebelumnya, file itu dimuat kategori 3, sebelum kategori 4/charts — aman, urutan `index.html` sudah benar tanpa perlu diubah); `aState.pwPower` (svgPowerCurve saja, dibaca runtime); `SE.f4`. `index.html` diupdate: `power-plot.js` dimuat setelah `sem-mediation-diagram.js`, sebelum `analyze-form-render.js`. **Pemanggil, digrep di `app.js` + `analyze-form-render.js` (upload sesi lalu)**: `svgPowerCurve` 2 titik (preview `renderPowerForm()` di `analyze-form-render.js` baris ~3033, dan output `renderOutput()` kasus `poweranalysis` di `app.js` baris ~3552 setelah potong — tidak diubah, resolve scope-fallback global); `svgSensitivityCurve` HANYA 1 titik (preview saja, `analyze-form-render.js` baris ~3086) — **tidak ada pemanggil di `app.js`** (grep case-insensitive `sensitivitycurve` = 0 hasil selain deklarasi), pola sama seperti `svgSEMDiagram`/E7. 0 deklarasi ganda dengan file lain. **Tidak ada temuan escHtml** — diperiksa semua string yang masuk teks SVG: `ef[1]` (label 'Small'/'Medium'/'Large', hardcode internal, bukan dari data user) dan `nHighlight`/angka N (numerik, aman); `test` sendiri (`'ttest_2samp'` dst.) adalah key dropdown tetap, bukan nama variabel dataset, jadi beda kategori dari Temuan E2/E6/E7 — tidak perlu escHtml. **Diverifikasi**: 103 baris byte-exact terhadap `app.js` snapshot sesi ini (`diff` 0 selisih); `node --check` lolos untuk `app.js` baru dan file baru; output kedua fungsi dites dengan `computePower`/`SE.f4`/`aState` yang di-mock deterministik (supaya bisa dibandingkan) — dibandingkan langsung dengan hasil eval fungsi versi lama dari `app.js` sebelum dipotong (mock & input identik) — **keluaran identik karakter-per-karakter** untuk kedua fungsi. **Tidak bisa dites**: load penuh urutan `<script>` `index.html` di Node `vm` (belum semua file direferensikan diupload), fungsi asli `computePower()` yang sebenarnya (belum diupload, dipakai mock), dan tes manual di browser. `app.js` 6.057 → 5.965 baris. **Tes yang disarankan untuk user**: Analyze → Power Analysis → pilih test apa saja → cek preview kurva power & sensitivity di form, lalu Run → cek tab Output (kurva power muncul dengan 3 garis effect size + garis referensi 0.80, console bersih). |
| 2026-09-20 | E9 | `js/charts/moderation-plot.js` (baru) | Dipindah sbg 3 fungsi global apa adanya: `svgModerationPlot(res,xName,wName,yName,W,H)` (53 baris), `svgSimpleSlopesPlot(res,xName,wName,yName,W,H)` (wrapper 3 baris, langsung `return svgModerationPlot(...)`), `svgJohnsonNeymanPlot(res,xName,wName,yName,W,H)` (73 baris) — total 131 baris (`app.js` snapshot sesi ini, 5.966 baris, baris 300–430). Dependency: `escHtml()` (dipakai `svgModerationPlot` untuk `xName`) dan `SE` (`SE.f4`, `SE.tP`) — global, dibaca runtime, aman. `index.html` diupdate: `moderation-plot.js` dimuat setelah `power-plot.js`, sebelum `analyze-form-render.js`. **Pemanggil, digrep di `app.js` + `analyze-form-render.js` (upload sesi lalu)**: `svgModerationPlot` 2 titik (preview `renderModerationForm()` + output `renderOutput()` kasus moderation, tidak diubah); `svgJohnsonNeymanPlot` 2 titik (preview + output, sama pola); `svgSimpleSlopesPlot` **HANYA 1 titik** (preview saja, grep case-insensitive `simpleslopesplot` di `app.js` = 0 hasil selain deklarasi) — pola sama seperti `svgSEMDiagram`/E7 dan `svgSensitivityCurve`/E8. 0 deklarasi ganda dengan file lain. ⚠️ **Temuan E9 baru (dicatat, BELUM diperbaiki)**: di `svgJohnsonNeymanPlot`, `wName` (nama variabel moderator dari data user) dimasukkan ke judul sumbu X **tanpa `escHtml`** — pola sama persis dengan Temuan E2/E6/E7. Beda dengan `xName` di `svgModerationPlot` yang SUDAH di-escape sejak awal (baris `escHtml(xName)`, tidak disentuh). Diperiksa juga: `yName` diterima sebagai parameter di KEDUA fungsi tapi **tidak pernah dirender ke SVG** di salah satunya (tidak ada Y-axis title di kedua chart) — jadi aman, tidak butuh escHtml, bukan bagian dari temuan. Sengaja tidak diperbaiki sesi ini karena user cuma minta "lanjut E9", bukan sekalian perbaiki. **Diverifikasi**: 131 baris byte-exact terhadap `app.js` snapshot sesi ini (`diff` 0 selisih); `node --check` lolos untuk `app.js` baru dan file baru; output ketiga fungsi dites dengan data contoh (`res` sintetis: 4 koefisien regresi + SE, `xArr`/`yArr`/`wArr`, 1 region JN) dan `SE`/`escHtml` yang di-mock deterministik — dibandingkan langsung dengan hasil eval fungsi versi lama dari `app.js` sebelum dipotong (mock & input identik) — **keluaran identik karakter-per-karakter** untuk ketiga fungsi. **Tidak bisa dites**: load penuh urutan `<script>` `index.html` di Node `vm`, fungsi asli `SE.tP()`/`SE.f4()` sebenarnya (dipakai mock), dan tes manual di browser. `app.js` 5.966 → 5.844 baris. **Tes yang disarankan untuk user**: Analyze → Moderation → isi X/W/Y → cek preview (garis regresi 3 level W + scatter, lalu Simple Slopes, lalu Johnson-Neyman dengan band signifikan), lalu Run → cek tab Output (console bersih). |
| 2026-09-20 | E9 — temuan escHtml diperbaiki | `js/charts/moderation-plot.js`, `app.js` | Atas permintaan user ("perbaiki temuan E9"), temuan E9 dari entri di atas **langsung diperbaiki**: di `svgJohnsonNeymanPlot`, `'+wName+' (Moderator)'` → `'+escHtml(wName)+' (Moderator)'` (judul sumbu X, 1 titik). `xName` di `svgModerationPlot` tidak disentuh (sudah escaped sejak awal); `yName` tetap tidak perlu (tidak pernah dirender). Header komentar file diupdate (⚠️ → ✅). File sumber = `js/charts/moderation-plot.js` dari repo GitHub (dicek: identik dengan yang dijelaskan di entri E9 — 131 baris kode + header, dan `app.js` repo identik dengan upload sesi ini). **Ditemukan tambahan saat pengecekan pemanggil** (belum tercatat di temuan awal): `renderModerationOutput()` di `app.js` — pemanggil `svgModerationPlot`/`svgJohnsonNeymanPlot` — memasukkan nama variabel ke HTML output tanpa `escHtml`: `r.xName`/`r.wName`/`r.yName` di header hasil dan kalimat interpretasi (3 kalimat/2 cabang ternary), plus `c.name` di tabel koefisien (`computeModeration` membentuk nama koefisien dari `xName`/`wName`/`covNames` mentah, mis. `Stress×Support`). Ikut diperbaiki jadi `escHtml(...)` (header 3 nama, `c.name` 1 titik, interpretasi 6 titik = 10 titik) dan komentar pointer E9 di `app.js` diupdate supaya tidak jadi catatan basi. **Diverifikasi**: `node --check` lolos untuk `app.js` dan `moderation-plot.js`; ketiga fungsi chart (`svgJohnsonNeymanPlot`, `svgModerationPlot`, `svgSimpleSlopesPlot`) dites dengan `res` sintetis (40 titik, 4 koefisien, 1 region JN) + `SE`/`escHtml` yang di-mock — nama normal (`'Stress'`/`'Support'`) menghasilkan output **identik karakter-per-karakter** dengan versi sebelum fix; nama jahat (`'</svg><script>alert(1)</script>&<b>x</b>'`) di versi lama meloloskan `<script>` mentah dan 2 `</svg>` (SVG ter-close prematur), di versi baru 0 `<script>`/`<b>` mentah, 1 `</svg>`, dan snippet ter-escape terkonfirmasi ada. `renderModerationOutput()` (dipotong dari `app.js` dan dijalankan dengan mock `stCard`/`SE`/`escHtml` + fungsi chart dari file baru) dites sama: nama normal identik karakter-per-karakter dengan versi lama; nama jahat (`'<img src=x onerror=alert(1)>'`, `'W&Co'`, `'</td><script>1</script>'`) lolos mentah di versi lama, 0 lolos di versi baru. **Tidak bisa dites**: tes manual di browser, dan `SE.tP()`/`SE.f4()` asli (dipakai mock). **Belum diperbaiki (menunggu keputusan user)**: pola yang sama (nama variabel mentah ke HTML) juga ada di renderer output lain di `app.js` (mis. `renderOutput` kasus regresi baris ~2342 `o.yName`/`c.name`, CCA baris ~2260–2261 `o.xNames`/`o.yNames`, Mediation baris ~3017 `r.xName`/`r.mNames`) — bukan bagian E9, cakupannya jauh lebih luas dari Section E, jadi sengaja tidak disapu diam-diam. |

---

## 7. RINGKASAN PROGRESS

**Total item di roadmap: 60+ bagian (A1–I6) + B27 (temuan baru)**
**Sudah dipisah: 57 (A1–A7, B1–B23, B25, B26, B24-sebagian, C3, B27, C1, C2, C4, C5, C6, C7, C8, C9, C10, C10★-imputation, C8★-analyze-tab-shell, C11, D1, E1, E2, E3, E4, E5, E6, E7, E8, E9)** — D1 selesai 100% (pindah fisik + breakdown per-cabang, 46 sub-tab), Section E baru 9 dari 14 baris (E10–E14 belum; E10–E14 = 10 fungsi `svg*` sisanya yang sebelumnya tanpa baris, dipetakan 2026-09-19 — lihat Temuan E).
**Progress: ~68% (dihitung 1 − baris `app.js` sekarang / 18.345 baseline; `app.js` = 5.844 baris per 2026-09-20).** Breakdown internal D1 sudah 100% (46 dari 46 cabang jadi fungsi berdiri sendiri, `renderASub()` tinggal dispatcher).

**Section A (Core) SELESAI 100%.** Section B (Statistics Engine) jalan terus — B1-B23, B25, B26, B27 selesai; B24 SEBAGIAN (cuma `logisticReg`, `toggleHrBlock` sengaja ditinggal, lihat catatan). Section C (Data Management) sudah mulai — C3 selesai (digabung dengan B23). Duplikat `matMul` aktif di `stats-core-advanced.js` (2 versi, versi EFA/CFA/MANOVA menang, sesuai rencana). Rendering SVG (Forest/Funnel Plot meta-analysis, Mediation Path Diagram, Power/Sensitivity Curve, Moderation/Simple-Slopes/Johnson-Neyman Plot, ROC/ROC Compare, Kaplan-Meier/Forest Plot Cox, Convergence Plot MI, SEM Path Diagram, Time Series Plot/Correlogram/Decomposition Chart) masih di `app.js`, belum ikut dipisah, ditunggu sampai bagian charts dikerjakan. 4 blok polyfill top-level `SE.fInv`/`SE.fCritApprox`/`SE.chiCritApprox`/`SE.chi2CDF` sengaja dibiarkan di `app.js` (lihat catatan B14 & B18 — tidak bisa dipindah ke file pre-app.js karena top-level write ke `SE`); `lnGammaSimple` (dependency `SE.chi2CDF`) ikut tetap di app.js. `stats-mediation-sem.js` sekarang lengkap sesuai rencana peta Bagian 2: `computeMediation` (B13) + `computeModeration` (B15) + `computeSEM`/`generateLavaanSyntax` (B19) — digabung jadi 1 file utuh oleh user di sesi ini (sebelumnya sempat berupa 2 file terpisah karena `stats-mediation-sem.js` hasil B13/B15 tidak ikut diupload saat B19). `stats-bayesian.js` (B20) adalah file stats-engine PERTAMA yang dimuat SETELAH `app.js` bukan sebelum — lihat catatan B20 kalau ada file `stats-*` lain nanti yang ternyata juga menulis langsung ke `SE` di top-level (bukan cuma di dalam function). `stats-discriminant-cluster.js` sekarang lengkap sesuai rencana peta Bagian 2: `computeLDA` (B21) + `computeKMeans`/`computeHierarchical` (B22). `stats-glm-hlm.js` (B25) sekarang juga lengkap sesuai rencana peta Bagian 2, ditambah `poissonReg`/`negbinReg`/`solveLinear`/`invertMatrix`/`computeHLMBasics`. `stats-timeseries.js` (B26, baru) berisi `tsACF`/`tsPACF`/`tsDiff`/`tsARIMA`/`tsDecomp` — bug `SE.vari` yang bikin mode Decomposition-nya gagal diam-diam sudah **diperbaiki** (lihat Bagian 8). **Section B tersisa (belum dikerjakan penuh): B24 sisa `toggleHrBlock` (sengaja tidak dipindah, tetap di app.js — lihat catatan).** File dasar `js/stats-engine/stats-core-advanced.js` BELUM ikut diupload user di sesi manapun sejauh ini — potongan B24 (`logisticReg`) masih berupa fragmen terpisah `_B24_append_to_stats-core-advanced.js` yang perlu digabung manual oleh user (sama kasusnya dengan B19 dulu sebelum digabung). Section B (Statistics Engine) praktis SELESAI kecuali B24 `toggleHrBlock` yang sengaja ditinggal. **Section C (Data Management) — C1 (Multi-Dataset System) SELESAI (2026-09-18)**, dipindah ke `js/data/dataset-manager.js` bersama C3 (Weight helpers, sudah selesai sejak B23). **C2 (Analyze sub-tab state helper) SELESAI (2026-09-20)**, dipindah ke `js/analyze/analyze-subtab-ui.js` (`aState` + `numFields`/`allFields`/`isMiss`/`missCount`) — file ini nantinya juga jadi rumah C9 (Sliding pill indicator, target file sama, sudah digabung 2026-09-20 setelah user upload ulang file ini). **C4 (HTML Helpers umum) SELESAI (2026-09-20)**, dipindah ke `js/core/html-helpers.js` (`sigBadge`/`stCard`/`mkTable`/`selOpts`), dipilih sbg file "util bersama" tersendiri alih-alih digabung ke `js/core/state.js`. **C5 (Custom Select) SELESAI (2026-09-20)**, dipindah ke `js/ui-misc/custom-select.js` (`_cR`/`_cActive`/`varBdg`/`mkCsel`/`mkSelect`/`openCselById`/`_buildCselList`/`filterCsel`/`closeCsel`/`openOptCselById`/`mkOptCsel` + listener keydown Escape) — dimuat di kategori pre-app.js (bukan kategori 6 seperti `badges.js`) karena top-level call-nya cuma memasang listener, tidak butuh `SE`/`app.js` sudah terbentuk. **C6 (Data View) SELESAI (2026-09-20)**, dipindah ke `js/data/data-view.js` (`sortCol`/`sortDir`/`searchQ`/`selRows`/`sprMode`/`_ossDataPageThreshold`/`_ossDataPageSize`/`_ossDataShown` + `renderData`) — blok ini persis 137 baris, cocok pas dengan rentang baseline (4131–4267), jadi tidak ada percampuran dengan kode C7 di sekitarnya. **Catatan penting**: fungsi-fungsi terkait sort/search/select lain yang secara konsep juga "Data View" — `searchData`, `toggleSort`, `toggleRow`, `toggleAllRows`, `syncHeaderCheckbox`, `deleteSelected`, `toggleEditMode`, `enterCombinedEditMode`, `addDataRowInline`, `_ossLoadMoreRows`, `handleCSV`, `addDataRow`/`editRowModal`/`confirmAddRow`/`confirmEditRow`, var `editMode` — TERNYATA berada di baris baseline yang masuk rentang C7 (4268–4898), bercampur dengan kode Spreadsheet Mode asli (`sprBuffer`, `enterSprMode`/`exitSprMode`/`saveSprMode`, `sprCellChange`/`sprCellInput`/`sprKeyNav`, `colFillModal` dkk). Ini BUKAN dipindah di sesi ini — akan ikut terangkat saat C7 dikerjakan (kemungkinan perlu didiskusikan apakah dipecah lagi jadi 2 file terpisah, karena isinya campuran Data View lanjutan + Spreadsheet Mode murni, atau digabung apa adanya ke `spreadsheet-mode.js` sesuai peta awal). `renderData()` sendiri tetap memanggil semua fungsi itu lewat `onclick="..."` string (bukan reference langsung) sehingga aman scope-fallback ke global begitu C7 juga dipisah nanti. **Diverifikasi via Node `vm` module** (di luar browser, dengan stub `document`/`IC`/`escHtml`/`missCount`/`aState`/`getNEff`/`vars`/`data`/`editMode`): `renderData(el)` jalan tanpa exception, menghasilkan HTML tabel yang benar (header kolom dengan dot warna sesuai measure, missing cell tampil `—`, footer info "3/3 rows"), var `sortCol`/`sortDir`/`searchQ`/`selRows`(Set)/`sprMode`/`_ossDataPageThreshold`/`_ossDataPageSize`/`_ossDataShown` semua ter-load dengan nilai default yang benar. Ekstraksi dicek byte-exact terhadap `app.js` lama. `app.js` lolos `node --check` sebelum & sesudah pemotongan (12.248 → 12.118 baris). `index.html` diupdate: `data-view.js` (baru) dimuat di kategori "3. Data layer", setelah `weight-cases.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. **C9 (Sliding pill indicator) SELESAI (2026-09-20), sudah digabung** ke `js/analyze/analyze-subtab-ui.js` (file yang sama dengan C2) setelah user upload ulang file itu — `var _subTabAnim` + `positionSubTabIndicator` + listener `resize` ditambahkan di akhir file, tidak mengubah isi C2 yang sudah ada. Temuan `renderAnalyze(el)` (dicatat pas C8) masih belum ditindaklanjuti — masih di `app.js`. **Section C (Data Management) SELESAI 100%** (C1-C11 semua sudah dipisah, termasuk 2 temuan tambahan C8★/C10★). **C11 (Missing Data Analysis) SELESAI (2026-09-20)** — beda dari C1-C10, C11 BUKAN fungsi berdiri sendiri di baseline, melainkan 1 cabang `else if(currentASub==='missinganalysis'){...}` di tengah switch raksasa `renderASub()` (D1). Supaya bisa dipisah tanpa merusak `renderASub()`, badan cabang (263 baris) dibungkus jadi fungsi baru `renderMissingDataAnalysis()` yang membangun & me-return string HTML sendiri (`var html=''` lokal, bukan lagi variabel bersama `renderASub()`) — ini SATU-SATUNYA perubahan struktural, isi logika 100% byte-exact. Call-site di `renderASub()` diubah dari 263 baris kode inline jadi `else if(currentASub==='missinganalysis'){ html+=renderMissingDataAnalysis(); }`. Dicek dulu sebelum dibungkus: cabang aslinya tidak pernah membaca `html` (cuma `html+=`, tidak ada `html=` reset atau baca isi/panjang untuk logika) dan tidak memakai variabel scope `renderASub()` lain (`nF`/`aF`/`aState` sama sekali tidak dipakai) — cuma `vars`/`data`/`escHtml` + var lokal (`nF2`/`n2`/dll), jadi pembungkusan dijamin tidak mengubah perilaku. **Diverifikasi via Node `vm` module**: fungsi `renderMissingDataAnalysis()` jalan tanpa exception untuk 3 skenario (data dengan missing values → tampil Little's MCAR Test + pattern matrix + rekomendasi; data kosong → pesan "No numeric data loaded"; data tanpa missing → pesan "No missing data found"); juga dites integrasi simulasi persis pola call-site baru di `renderASub()` (`if(false){}else if(currentASub==='missinganalysis'){html+=renderMissingDataAnalysis();}`) — hasil HTML gabung dengan benar. Ekstraksi badan fungsi dicek byte-exact terhadap `app.js` lama (di luar wrapper `function renderMissingDataAnalysis(){var html='';` ... `return html;}` yang memang baru). `app.js` lolos `node --check` sebelum & sesudah pemotongan (10.551 → 10.295 baris). `index.html` diupdate: `missing-data.js` (baru) dimuat di kategori "3. Data layer", setelah `imputation.js`, sebelum `analyze-subtab-ui.js` dan `app.js`. Temuan `toggleCoefView` (dicatat pas C7, masih di `spreadsheet-mode.js`, kandidat pindah ke `analyze-form-render.js`/D1) dan `runWeightCases`/`clearWeightCases` (kandidat `weight-cases.js`) masih ditunda — user memilih lanjut C11 dulu.


**Update 2026-09-19 — D1, E1, E2, E3.** **D1 SELESAI**: dispatcher `renderASub()` (217 baris) + 45 fungsi `render<Nama>Form()` di `analyze-form-render.js` (+ `renderMissingDataAnalysis` di `missing-data.js`); 6 cabang pertama dari sesi-sesi awal, ~40 sisanya dibungkus per-cabang di sesi-sesi berikutnya (commit `d306c31`/`5976b6d`). **E1** (`js/charts/svg-charts-core.js`: `PAL2` + `svgScreePlot`/`svgHistogram`/`svgBoxplot`/`svgScatter`/`svgQQ`/`svgResidual`/`svgCorrMatrix`) dan **E2** (`js/charts/interaction-plot.js`: `svgInteractionPlot`) selesai, dimuat berurutan `svg-charts-core.js` → `interaction-plot.js` → `analyze-form-render.js` → `app.js` (kategori 4 di `index.html`) karena E2 butuh `PAL2` dari E1. **E3** (`js/output/output-render-basic.js`: `renderAnovaTable`, file baru kategori Output "4b") selesai di sesi yang sama; F3 (`renderOutput()` bagian basic) nantinya ikut masuk file ini. `app.js` 6.468 → 6.169 baris. **E4+E5** (`js/charts/forest-funnel-plot.js`: `svgMetaForestPlot` + `svgFunnelPlot(res)`, diambil dari `analyze-form-render.js`; 3 temuan kecilnya sudah diperbaiki di hari yang sama) selesai, dimuat setelah `interaction-plot.js` dan sebelum `analyze-form-render.js`. `analyze-form-render.js` 4.141 → 3.997 baris. **15 fungsi `svg*` yang tadinya tanpa baris sudah dipetakan ke E8–E14 (per fitur, 7 file chart baru — lihat tabel E dan tree Bagian 2).**

**Update 2026-09-20 — E6.** `svgMediationPath` dipindah ke `js/charts/sem-mediation-diagram.js` (file baru), dimuat setelah `forest-funnel-plot.js` dan sebelum `analyze-form-render.js` (kategori 4 di `index.html`). Lihat entri changelog Bagian 6 untuk detail verifikasi.

**Update 2026-09-20 — E7.** `svgSEMDiagram` dipindah ke file yang sama (`js/charts/sem-mediation-diagram.js`, sekarang isi 2 fungsi: E6+E7). Tidak ada perubahan `index.html` (file sudah dimuat dari E6). Ditemukan: `svgSEMDiagram` cuma punya 1 pemanggil (preview di `renderSEMForm()`, `js/analyze/analyze-form-render.js`) — tidak dipakai `renderOutput()` di `app.js` sama sekali (beda dari `svgMediationPath` yang punya 2 pemanggil). Lihat entri changelog Bagian 6 untuk detail verifikasi.

**Update 2026-09-20 — E8.** `svgPowerCurve` + `svgSensitivityCurve` dipindah ke file baru `js/charts/power-plot.js`, dimuat setelah `sem-mediation-diagram.js` dan sebelum `analyze-form-render.js` (kategori 4). Dependency `computePower()` sudah ada di `js/stats-engine/stats-poweranalysis.js` (kategori 3, dimuat lebih dulu) — aman. Sama seperti E7, `svgSensitivityCurve` cuma punya 1 pemanggil (preview), sementara `svgPowerCurve` punya 2 (preview + output). **Tidak ada temuan escHtml** di file ini — kedua fungsi hanya merender key test internal (`'ttest_2samp'` dst, dari dropdown tetap) dan label hardcode (`'Small'/'Medium'/'Large'`), bukan nama variabel dataset. Lihat entri changelog Bagian 6 untuk detail verifikasi. **Berikutnya di Section E: E9–E14, 13 fungsi `svg*` sisanya, satu grup per sesi (lihat tabel E untuk daftar & target file 6 chart baru yang tersisa).**

**Update 2026-09-20 — E9.** `svgModerationPlot`, `svgSimpleSlopesPlot`, `svgJohnsonNeymanPlot` dipindah ke file baru `js/charts/moderation-plot.js`, dimuat setelah `power-plot.js` dan sebelum `analyze-form-render.js` (kategori 4). Sama seperti E7/E8, `svgSimpleSlopesPlot` (wrapper) cuma punya 1 pemanggil (preview); `svgModerationPlot` dan `svgJohnsonNeymanPlot` masing-masing punya 2 (preview + output). ✅ **Temuan E9** (`wName` di `svgJohnsonNeymanPlot` tidak di-escape) **sudah diperbaiki 2026-09-20** — lihat poin (7) di callout Temuan E di atas dan entri changelog "E9 — temuan escHtml diperbaiki". Lihat entri changelog Bagian 6 untuk detail verifikasi. **Berikutnya di Section E: E10–E14, 10 fungsi `svg*` sisanya, satu grup per sesi.**
*(Update angka ini setiap kali ada perubahan status di Bagian 3.)*

---

## 8. TEMUAN — STATUS PERBAIKAN (2026-09-15, sesi lanjutan)

User minta semua temuan yang tercatat di dokumen ini diperbaiki. Repo asli
(`bayuharlanpriangga/OSS` di GitHub) di-clone langsung untuk verifikasi —
setiap klaim di bawah dicek ulang terhadap kode aktual sebelum diperbaiki
(beberapa klaim lama ternyata sudah tidak akurat, lihat catatan).

| Temuan | Status | Keterangan |
|---|---|---|
| **B1** — `matMul`/`matT`/`matDet`/`matInv` tabrakan nama (flat vs 2D-array) | ✅ **DIPERBAIKI** | Versi flat-array (dipakai `multipleReg`) di-rename jadi `matMulFlat`/`matTFlat`/`matDetFlat`/`matInvFlat` di `stats-core-advanced.js`. Versi 2D-array (EFA/CFA/MANOVA/canonicalCorr) tetap `matMul`/`matT`/`matDet`/`matInv`. Diverifikasi dengan test: `multipleReg` sekarang balik koefisien yang benar (sebelumnya rusak karena signature tertimpa); EFA tetap jalan normal (tidak ada regresi). |
| **(baru, ditemukan saat perbaiki B1)** — `SE.matInv`/`SE.matMul` selalu `undefined` di luar `stats-core-advanced.js` | ✅ **DIPERBAIKI** | `matMul`/`matT`/`matDet`/`matInv`/`matMulFlat`/`matTFlat`/`matDetFlat`/`matInvFlat` tidak pernah dimasukkan ke `return {...}` objek `SE` di `app.js` — jadi `SE.matInv(...)` di 3 file lain selalu gagal diam-diam. **Dampak nyata**: MICE imputation (`stats-imputation.js`) selalu fallback ke mean sederhana (bukan regresi), dan Cox regression (`stats-survival.js`) SE koefisiennya di-hardcode 0.1 (Newton-Raphson tidak pernah update). Mediation/Moderation (`stats-mediation-sem.js`) TIDAK terdampak — sudah ada fallback lokal `matInvLocal` yang benar. Sudah ditambahkan ke `return {...}` di `app.js`, dan 3 file pemanggil sudah diarahkan ke nama yang benar (`matInv` untuk 2D, `matInvFlat` untuk flat). |
| **B21** — `wilksP` operator-precedence bug di `stats-discriminant-cluster.js` | ✅ **DIPERBAIKI** | `pFmt(1-SE.chi2CDF?SE.chi2CDF(...):NaN)` → `pFmt(SE.chi2CDF?1-SE.chi2CDF(...):NaN)`. Dampak sebenarnya kecil (cuma nilai fallback awal, ada perhitungan ulang di try/catch di bawahnya yang sudah benar). |
| **H1/H2** — `saveSession`/`loadSession`/`exportDataCSV`/`generateAPAReport` duplikat | ✅ **DIPERBAIKI** | Blok pertama (versi lama single-dataset) dikonfirmasi 100% dead code (ketimpa deklarasi kedua) dan dihapus. Tidak ada perubahan perilaku — versi aktif (multi-dataset-aware) persis sama seperti sebelumnya. |
| **B7** — `holmBonferroni` dead code, tidak ada di UI post-hoc ANOVA | ✅ **DIPERBAIKI** | Ditambah wrapper `holmBonferroniPosthoc(groups)` di `stats-core-basic.js`, opsi "Holm-Bonferroni" ditambahkan ke dropdown post-hoc ANOVA di `app.js` (`_cR['av-ph']`), wiring `runANOVA()`, dan label render tabel hasil. Diverifikasi: hasil Holm step-down lebih powerful dari Bonferroni flat pada kasus p-value borderline, sesuai teori. |
| **B23/C3** — `getWeightedRows()` (Weight Cases) tidak pernah dipanggil fungsi statistik manapun | ⚠️ **BELUM DIPERBAIKI — butuh keputusan produk, bukan sekadar bug fix** | Ini bukan bug satu-baris: mewiring bobot secara benar ke SEMUA fungsi analisis (t-test, ANOVA, regresi, chi-square, dll — puluhan fungsi lintas banyak file) adalah pekerjaan fitur besar, bukan patch kecil, dan berisiko tinggi kalau ditebak sembarangan (bisa bikin hasil analisis lain jadi salah kalau logic weighting keliru). **Belum disentuh** — disarankan didiskusikan dulu: fungsi statistik mana saja yang perlu mendukung Weight Cases, dan behavior yang diharapkan untuk N/df/SE saat weighted. |
| **"f1 duplikat" (disebut di catatan B24)** | ✅ **DIVERIFIKASI BUKAN BUG** | Klaim lama "f1 lokal di app.js jadi dead code" TERNYATA SALAH — dicek ulang, `SE.f1` (versi lokal, return '—' untuk non-finite) masih aktif dipakai di rendering confusion matrix logistic regression (`app.js`). Berbeda dari `f1` global (`stats-core-basic.js`, dipakai internal perhitungan seperti `pairedTTest`/`logisticReg`). Dua-duanya legitimate, diakses lewat path berbeda (`SE.f1` vs `f1` langsung), tidak pernah tabrakan. Tidak ada perubahan. |
| **pFmt duplikat (2 lokasi)** | ✅ **DIVERIFIKASI BUKAN BUG** | `stats-core-basic.js` (global) dan `stats-metaanalysis.js` (lokal, closure-scoped) — tidak pernah tabrakan karena scope beda. Tidak ada perubahan. |
| **B14/B18 polyfill top-level** (`SE.fInv`/`SE.chi2CDF` dkk di app.js) | ➖ **BUKAN BUG, TIDAK DISENTUH** | Ini keputusan arsitektur yang sudah benar (top-level write ke `SE` tidak bisa dipindah ke file pre-app.js) — bukan temuan yang perlu "diperbaiki". |
| **B26** — `SE.vari` tidak pernah masuk `return {...}` objek `SE`, Time Series Decomposition selalu gagal diam-diam | ✅ **DIPERBAIKI (2026-09-16)** | `vari` ditambahkan ke `return {...}` di `app.js` (baris `validNums, isV, mean, std, vari, quantile, ...`), pola sama persis dengan fix `fCDF`/`chiCDF` (B1) dan `matInv`/`matMul` (temuan sesi ini). Diverifikasi via Node `vm` module: `SE.vari(Y)` sekarang mengembalikan nilai identik dengan implementasi manual (bukan lagi `undefined`), dan `tsDecomp` (dipakai preview & tombol "Run" Time Series) sekarang jalan normal untuk mode additive maupun multiplicative tanpa perlu workaround apa pun. Mode ARIMA (tidak pernah pakai `SE.vari`) dicek tetap sama seperti sebelumnya, tidak ada regresi. |
| **B25 (temuan susulan)** — `negbinReg`: golden-section search θ tanpa batas atas + IRLS tanpa step-halving (akar masalah sesungguhnya, ditemukan pas verifikasi fix θ) | ✅ **DIPERBAIKI (2026-09-17)** | Detail lengkap di Bagian 10. Ringkas: θ di-clamp ke `[1e-3, 1e4]` (dulu tanpa batas atas → dobel terus jadi ~2⁵⁰), DAN loop IRLS ditambah step-halving supaya Newton step tidak overshoot jadi `NaN` sebelum pencarian θ sempat jalan (akar masalah sesungguhnya — batasi θ saja TIDAK cukup, dibuktikan lewat ~10 skenario test yang semuanya masih "N/A" tanpa fix IRLS ini). Diverifikasi konvergen & tidak `NaN` di data overdispersed maupun Poisson murni, diulang beberapa kali dengan data random berbeda. |
| **(dicatat pas C1)** — deklarasi ganda `numFields`/`allFields` (`function` lama tanpa exclude `id` vs `var`+arrow baru yang exclude `id`) | ✅ **DIPERBAIKI (2026-09-19)** | Dicek: versi lama selalu tertimpa versi baru sebelum ada pemanggilan apa pun (100% dead code, beda dari kasus `matMul`/`f1` yang overwrite-nya terjadi SETELAH beberapa pemanggilan). Versi lama dihapus, tidak ada perubahan perilaku. Detail di changelog Bagian 6. |

## 9. B25 — SPLIT SELESAI (2026-09-16)

`poissonReg`, `negbinReg`, helper `solveLinear`/`invertMatrix` (sebelumnya
di dalam IIFE `var SE=(()=>{...})()` di `app.js`), dan `computeHLMBasics`
(sebelumnya nested di dalam function render sub-tab HLM) sudah dipindah ke
`js/stats-engine/stats-glm-hlm.js`, mengikuti pola B1-B24 (dipindah keluar
apa adanya jadi fungsi global, dipanggil balik dari `app.js` tanpa ubah
call-site). Tidak ada tabrakan nama — `solveLinear`/`invertMatrix` beda
dari `matMulFlat`/`matInvFlat` (B1) dan dipakai khusus oleh 2 fungsi ini.

Diverifikasi via Node `vm` module (di luar browser, load urutan file
persis seperti `index.html`):
- `poissonReg` — koefisien pulih akurat pada data sintetis (`x`-coef 0.30
  vs true 0.30, intercept 0.51 vs true 0.50), `converged:true`. ✅
- `computeHLMBasics` — ICC & variance partitioning terhitung benar untuk
  10 grup × 15 observasi. ✅
- `negbinReg` — bug θ tanpa batas atas yang dicatat di sesi split ini
  **✅ SUDAH DIPERBAIKI (2026-09-17)**, lihat Bagian 8 baris terbaru dan
  detail lengkap di bawah (bukan cuma θ — ternyata ada 1 lapis bug lagi
  yang jadi akar masalah sesungguhnya, ditemukan pas verifikasi fix θ).

**File yang berubah**: `app.js`, `js/stats-engine/stats-core-advanced.js`,
`js/stats-engine/stats-core-basic.js`, `js/stats-engine/stats-discriminant-cluster.js`,
`js/stats-engine/stats-imputation.js`, `js/stats-engine/stats-survival.js`,
`js/stats-engine/stats-mediation-sem.js`. Semua perubahan lulus `node --check`
(syntax) dan diverifikasi lewat test langsung (Multiple Regression, EFA,
Holm-Bonferroni) di luar browser menggunakan Node `vm` module.

## 10. `negbinReg` — FIX SELESAI (2026-09-17)

Bug yang dicatat di Bagian 9 (θ tidak ada batas atas) **benar ada**, tapi
ternyata cuma **gejala**. Trace lebih dalam pakai debug log (Node `vm`,
di luar browser) ketemu akar masalah sesungguhnya: loop IRLS
(`for(var iter=0;iter<30;...)`, sebelum pencarian θ pun mulai) tidak
punya step-halving/damping. Dengan θ awal ditebak (1.0) dan beta awal 0,
weight NB2 (`mu/(1+mu/θ)`) sering jauh lebih kecil dari weight Poisson
biasa (`mu`) — bikin matriks informasi (`X'WX`) "kurang mengerem" Newton
step, beta lompat jauh (pernah sampai ribuan) dalam 2-3 iterasi lalu
meledak jadi `NaN`, **sebelum** pencarian θ sempat jalan. Begitu beta
`NaN`, log-likelihood `NaN` untuk semua kandidat θ → golden-section
search selalu ambil cabang yang sama → θ dobel terus tiap outer loop →
persis pola `2⁵⁰` yang sudah didiagnosis sebelumnya. Dibuktikan lewat
test: pasang batas atas θ saja (tanpa benahi IRLS) **tidak cukup** —
semua koefisien tetap tampil "N/A" di ~10 skenario data sintetis yang
dicoba (termasuk kasus paling "jinak": intercept-only, efek nyaris nol,
mu hampir konstan).

**2 perubahan di `negbinReg` (`js/stats-engine/stats-glm-hlm.js`)**:
1. **Batas θ**: `THETA_MAX=1e4`, `THETA_MIN=1e-3`. Golden-section search
   dan hasil akhir tiap outer loop di-clamp ke rentang ini. Kalau θ
   mentok di plafon (`thetaAtCap`), loop berhenti lebih awal (bukan
   lanjut 50x sia-sia) dan hasil dapat `warnings` baru: *"Dispersion
   parameter θ mencapai batas pencarian (10000)... pertimbangkan pakai
   Poisson saja"* — artinya data memang tidak overdispersed, NB2 dengan
   θ≥1e4 praktis identik dengan Poisson.
2. **Step-halving di IRLS** (akar masalah): tiap Newton step dicek dulu
   apakah benar menaikkan log-likelihood (`nbLogLik`); kalau tidak, step
   dikecilkan bertahap (dibagi 2, maks 15x) sebelum diterima. Ini yang
   sebenarnya menghentikan ledakan ke `NaN`.

**Verifikasi** (Node `vm`, load order persis `index.html`, di luar
browser — file baru `stats-glm-hlm.js` sampai `js/data/weight-cases.js`
lalu `app.js`):
- Data overdispersed sintetis (θ_true=1.5–3, campuran gamma-Poisson):
  konvergen ke θ≈1.7–3.2, koefisien dekat nilai sebenarnya
  (`converged:true`), diulang 5x dengan data random baru tiap kali —
  tidak pernah `NaN`. ✅
- Data Poisson murni (θ_true→∞, tidak overdispersed): benar-benar
  berhenti di plafon θ=10000 dengan `warnings` yang jelas, koefisien
  tetap masuk akal (bukan "N/A"). ✅
- Kasus ekstrem (mu hampir konstan, efek prediktor nyaris nol,
  intercept-only): tidak `NaN`, walau kadang koefisien prediktor jadi
  kurang presisi (CI lebar) — ini wajar untuk data hampir tak
  teridentifikasi, bukan bug. ✅
- Performa: n=2000, 3 prediktor, ~2–9 detik (bervariasi tergantung draw
  data) — sekali klik tombol, bukan lupa titik lain, dan yang penting
  sebelumnya fungsi ini **tidak pernah** selesai konvergen sama sekali.
- File lolos `node --check` (syntax), tidak ada tabrakan nama baru.

**File yang berubah (sesi ini)**: `js/stats-engine/stats-glm-hlm.js`
saja. `app.js`/`index.html`/`style.css` tidak disentuh — pemanggilan
`SE.negbinReg(...)` di `app.js` (baris preview & tombol Run GLM) tidak
perlu diubah, tetap jalan lewat scope-fallback ke fungsi global baru
sama seperti pola B1-B26.
